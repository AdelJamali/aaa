# سند واگذاری به کارشناس — افزونه‌ی Golden Importer

**نسخه‌ی فعلی:** 10.12.21 · **تاریخ سند:** ۱۱ سپتامبر ۲۰۲۶
**مخزن:** `AdelJamali/aaa` — شاخه‌ی `arena/01a06882-aaa`
**بسته:** `golden-importer-10.12.21.zip` · ۱۳۲ فایل · sha256 `1b9c89dbf10ec03ad1309a4b3a4deb8b74692626afc2c1a3f7235d64886dcdfd`

---

## ۰) خلاصه‌ی اجرایی — از شما چه می‌خواهیم

افزونه‌ای وردپرسی داریم که از تلگرام محصول وارد ووکامرس می‌کند. خط لوله‌ی
پردازش **متوقف شده** است. دو مسئله‌ی مستقل شناسایی شده:

| # | مسئله | وضعیت ریشه‌یابی |
|---|---|---|
| ۱ | `Fiber stack allocate failed: mmap failed: Cannot allocate memory (12)` | ❌ **ریشه‌یابی نشده — این درخواست اصلی ماست** |
| ۲ | گرسنگی Worker (یک Session صف را قفل می‌کرد) | ✅ اثبات شد · دو اصلاح روی میزبان تأیید شد · **یک نقص باقی‌مانده — بخش ۳-۶** |
| ۳ | ۸۲۵ رکورد یتیم در جدول انتخاب | 🔶 شناسایی شد، دست‌نخورده |

**درخواست مشخص:** مسئله‌ی شماره ۱ را ریشه‌یابی کنید و طبق قالب بخش ۱۰
گزارش بدهید.

نکته‌ی مهم درباره‌ی سطح دسترسی: مالک سامانه برنامه‌نویس نیست و فقط با
پنل کار می‌کند. **شما این محدودیت را ندارید** — بخش ۹ دقیقاً همان
دستورهایی است که ما نتوانستیم اجرا کنیم و شما می‌توانید.

---

## ۱) افزونه چه می‌کند

### ۱-۱) کارکرد

کانال‌های تلگرام را اسکن می‌کند، پیام‌ها را تحلیل می‌کند، فایل‌های مرتبط
را (که معمولاً پشت یک ربات واسط و زنجیره‌ای از دکمه‌ها هستند) پیدا و
دانلود می‌کند، و از آن‌ها محصول ووکامرس می‌سازد.

بخش سختِ کار «زنجیره» است: در بسیاری از کانال‌ها فایل مستقیم نیست. باید
روی دکمه کلیک شود، به ربات دیگری منتقل شد، deep-link باز شود، و گاهی چند
مرحله جلوتر رفت تا به فایل رسید. این منطق در `class-gs-chain-engine.php`
(۷۴ کیلوبایت) پیاده شده است.

### ۱-۲) پشته‌ی فنی

| لایه | جزئیات |
|---|---|
| بستر | وردپرس + ووکامرس |
| زبان | PHP (هدف: ۷.۴+ / اجرا روی ۸.۴.۲۳) |
| تلگرام — کاربر | **MadelineProto** به‌صورت phar (`madeline81.phar` — ۱۹.۵ مگابایت) |
| تلگرام — ربات | Bot API معمولی |
| پایگاه داده | جداول اختصاصی با پیشوند `sti_gs_` |
| زمان‌بندی | WP-Cron (بازدید-محور، **نه** cron سیستمی) |
| حجم کد | ۱۱۴ فایل PHP · ۲۲ مگابایت با phar |

### ۱-۳) نقشه‌ی کد

```
sanil-telegram-importer.php      ← بارگذار
madeline81.phar                  ← کتابخانه‌ی MTProto (محل وقوع خطا)
includes/
  class-sti-mtproto.php    (133K) ← پوشش MadelineProto ★ محل خطا
  class-sti-channel-import.php (194K)
  class-sti-logger.php
  golden-scan/                   ← موتور خط لوله
    class-gs-auto-worker.php  (63K) ← زمان‌بند خودکار
    class-gs-chain-engine.php (74K) ← حل زنجیره‌ی دکمه‌ها
    class-gs-session.php            ← ماشین حالت + قفل
    class-gs-cron-gate.php          ← گیت اتمیک اجرا
    class-gs-env-diag.php           ← ابزار تشخیص OOM ★
    class-gs-chain-audit.php  (86K) ← پنل تشخیص
    ... (۵۱ کلاس در golden-scan)
admin/views/golden-scan/worker.php (87K) ← رابط کاربری
```

### ۱-۴) جداول

`sti_gs_sessions` · `sti_gs_messages` · `sti_gs_channels` ·
`sti_gs_profiles` · `sti_gs_profile_items` · `sti_gs_handoff_steps` ·
`sti_gs_session_events` · `sti_gs_session_runs` · `sti_gs_scan_runs` ·
`sti_gs_scan_segments` · `sti_gs_bot_candidates` · `sti_gs_artifacts` ·
`sti_gs_pipeline_items`

### ۱-۵) ماشین حالت Session

```
SCANNED → FILE_MATCHED → DOWNLOADING → STORED
        → MEDIA_BUILDING → PRODUCT_BUILDING → REVIEW_READY → PUBLISHED
```

حالت‌های انتظار (`const WAITING`، auto-worker L66):
```php
array( 'WAITING_BOT', 'ERROR_BOT_TIMEOUT', 'CHAIN_WAITING' )
```

حالت‌های پایانی (`const TERMINAL`، L63):
```php
array( 'REVIEW_READY', 'PUBLISHED', 'SKIPPED',
       'NEEDS_REVIEW', 'ERROR_FILE_NOT_FOUND', 'DEAD_LETTER' )
```

### ۱-۶) محیط میزبان

| مورد | مقدار |
|---|---|
| دامنه | goldenfile.ir · sanilshop.ir |
| وب‌سرور | **LiteSpeed** (lsphp پایا) |
| PHP | ۸.۴.۲۳ |
| `memory_limit` (admin-ajax) | ۲۸۰ مگابایت |
| `proc_open` | ✅ فعال |
| `exec` / `shell_exec` | ❌ غیرفعال |
| کنترل پنل | cPanel — دسترسی SSH موجود **نیست** برای مالک |

> ✅ **تناقض ۲۸۰/۵۱۲ — حل شد (۱۱ سپتامبر).** پیش‌تر اینجا به‌عنوان
> مجهول ثبت شده بود. علتش در کد خودِ افزونه است: `class-sti-mtproto.php`
> خطوط ۲۴۷ و ۶۵۷ پیش از ساخت client اجرا می‌کنند
> `@ini_set( 'memory_limit', '512M' )`. پس ۲۸۰ مقدار پیکربندی میزبان است
> و ۵۱۲ مقدار مؤثر داخل بستر MTProto.
>
> این سقف heap مدیریت‌شده‌ی PHP است و **سقف `mmap` نیست** — یعنی نه علت
> خطای Fiber است و نه درمانش. ولی دیگر مجهول نیست.
>
> همچنان مفید است که `memory_limit` مؤثر را با PID و مرحله ثبت کنید تا
> ترتیب واقعی اعمال روشن شود.

---

## ۲) ★ مسئله‌ی اصلی — خطای Fiber / mmap

### ۲-۱) متن دقیق خطا

```
Fiber stack allocate failed: mmap failed: Cannot allocate memory (12)
```

این پیام هنگام ساخت client مربوط به MadelineProto پرتاب می‌شود:

`includes/class-sti-mtproto.php` خط ۶۸۹:
```php
$mad = new \danog\MadelineProto\API( self::session_path(), $settings );
```

### ۲-۲) آنچه از منبع PHP قطعی می‌دانیم

از `Zend/zend_fibers.c` (شاخه‌ی PHP-8.4) — تابع
`zend_fiber_stack_allocate()`:

```c
mmap(NULL, stack_size + ZEND_FIBER_GUARD_PAGES * page_size,
     PROT_READ | PROT_WRITE,
     MAP_PRIVATE | MAP_ANONYMOUS | MAP_STACK, -1, 0);
```

در صورت شکست، دقیقاً همین رشته پرتاب می‌شود:
```
"Fiber stack allocate failed: mmap failed: %s (%d)"
```

نتایج قطعی از این کد:

1. **یک `mmap` خام است، نه `emalloc`.** بنابراین `memory_limit` وردپرس/PHP
   نه می‌تواند علتش باشد و نه درمانش. هر توصیه‌ای که به بالا بردن
   `memory_limit` ختم شود، از پایه بی‌ربط است.
2. **هر Fiber دو VMA می‌سازد** (نگاشت اصلی + یک guard page که بعداً
   `mprotect(PROT_NONE)` می‌شود).
3. `fiber.stack_size` پیش‌فرض روی معماری ۶۴ بیتی = **۲ مگابایت**
   (`ZEND_FIBER_DEFAULT_C_STACK_SIZE = 4096 * 512`).
4. **تخصیص تنبل است** — به‌ازای هر `zend_fiber_start()`، نه یک‌بار در آغاز.
5. `ENOMEM` اینجا لزوماً یعنی «حسابداری اجازه نداد»، نه «رم تمام شد».
   OOM-killer درگیر نیست؛ فرآیند زنده می‌ماند و خطی در `dmesg` ثبت نمی‌شود.

### ۲-۳) چهار خانواده‌ی علت ممکن (هیچ‌کدام رد یا تأیید نشده)

| # | فرضیه | شاهد قطعی لازم |
|---|---|---|
| A | سقف `vm.max_map_count` (پیش‌فرض ۶۵۵۳۰) پر شده | `wc -l < /proc/<pid>/maps` در برابر `sysctl vm.max_map_count` |
| B | `RLIMIT_DATA` — از کرنل ۴.۷ نگاشت‌های خصوصیِ نوشتنی را هم می‌شمارد | `/proc/<pid>/limits` → `Max data size` و `VmData` |
| C | `RLIMIT_AS` یا VMEM سطح CloudLinux/LVE | `Max address space` در برابر `VmSize`/`VmPeak` |
| D | سقف حافظه‌ی cgroup یا `vm.overcommit_memory=2` | `memory.max`/`memory.current`/`memory.events` · `CommitLimit` در برابر `Committed_AS` |

> کرنل ۴.۷ کامیت `84638335900f` («mm: rework virtual memory accounting»):
> `RLIMIT_DATA` دیگر فقط `brk` نیست و نگاشت‌های خصوصیِ نوشتنی — یعنی
> دقیقاً همین fiber stack — را هم شامل می‌شود. به همین دلیل B را جدی
> می‌دانیم.

### ۲-۴) یک فرضیه‌ی خاصِ این معماری (پشتیبانی‌شده، اثبات‌نشده)

LiteSpeed فرآیند `lsphp` را **پایا** نگه می‌دارد. MadelineProto در
`Magic::registerFiber()` یک `WeakMap` از fiberها نگه می‌دارد. اگر در طول
عمر یک فرآیند پایا، Fiberها انباشته شوند و VMA آزاد نشود، شمارش نگاشت‌ها
به‌تدریج بالا می‌رود تا به سقف بخورد.

پیش‌بینی قابل ابطال: با ۲ VMA به‌ازای هر Fiber و سقف ۶۵۵۳۰، حدود
**۳۲٬۷۶۵ فیبر** کافی است.

> ⬇️ **تنزل‌یافته به «بدون شاهد» (۱۱ سپتامبر).** دو تصحیح:
>
> ۱. ارجاع قبلی من به `Magic::registerFiber()` **غلط بود**. متد در
>    کلاس `GarbageCollector` است (آفست `3824820` در PHAR).
> ۲. مهم‌تر: وجود `WeakMap` **شاهد نشت نیست** — `WeakMap` دقیقاً برای
>    این طراحی شده که مانع آزادسازی نشود. پایا بودن `lsphp` هم
>    به‌تنهایی چیزی اثبات نمی‌کند.
>
> مقایسه‌ی فرآیند قدیمی و جدید هم بدون کنترل نوع کارِ انجام‌شده،
> نه نشت را اثبات می‌کند نه رد.

**این فرضیه را به‌عنوان نتیجه نپذیرید — در حال حاضر هیچ شاهدی ندارد.**

### ۲-۵) خودِ MadelineProto هم همین را می‌داند

در phar، متد `Exception::exceptionHandler()` متن راهنمایی دارد که کاربر
را به `vm.max_map_count=262144` ارجاع می‌دهد. ابزارهای
`Tools::testFibers()`، `Tools::getMaps()` و `Tools::getMaxMaps()` دقیقاً
برای همین شکست ساخته شده‌اند. **پیشنهاد می‌کنیم این سه را مستقیماً روی
میزبان اجرا کنید** — سریع‌ترین مسیر موجود است.

### ۲-۶) یک باگ در ابزار تشخیص که خودمان ساختیم و رفع کردیم

این را می‌نویسیم چون هر لاگ قدیمی‌تری که ببینید آلوده‌ی آن است:

`Magic::start()` در MadelineProto یک `exceptionErrorHandler` نصب می‌کند
که هر warning را به exception تبدیل می‌کند، مگر `error_reporting()`
صفر باشد. در PHP 8.0+، عملگر `@` دیگر ماسک را صفر نمی‌کند.

نتیجه: `@file_get_contents('/sys/fs/cgroup/memory.max')` در ابزار تشخیصِ
ما خودش پرتاب می‌شد و **خطای واقعی Fiber را بازنویسی می‌کرد**.

در ۱۰.۱۲.۱۷ با `safe_read()` رفع شد (بررسی `is_readable` + نصب موقت
error handler اختصاصی + `try/catch (\Throwable)`). مسیرِ ناخوانا حالا
`unavailable` ثبت می‌شود، نه خطا. در چهار دامپ متوالی طی سه روز، شمار
این خطا روی **۱۶ مورد ثابت** مانده و اضافه نشده — یعنی رفع پابرجاست و
آن ۱۶ مورد تاریخی‌اند.

> ⚠️ اگر لاگی قبل از ۱۰.۱۲.۱۷ دیدید که در آن خطای `memory.max` جای خطای
> اصلی نشسته، آن یک artifact از باگ ماست، نه یک یافته.

### ۲-۷) دام نام‌گذاری

`STI_MTProto::is_fiber_error()` در خط ۲۵۲۰ فقط این دو رشته را می‌گیرد:
```php
strpos( $m, 'must call resume' ) || strpos( $m, 'before calling suspend' )
```

این تابع **با «Fiber stack allocate failed» تطبیق نمی‌خورد** — با وجود
اسمش. اگر روی این تابع حساب کنید، گمراه می‌شوید.

### ۲-۸) وضعیت رسمی

```
FIBER_ENOMEM = ROOT_CAUSE_UNKNOWN
```

تصمیم صریح گرفته‌ایم که تا در دست نداشتن `/proc/self/maps` و
`vm.max_map_count` و `RLIMIT_DATA`، **بین این دو انتخاب نکنیم**:

- **A)** محدودیت محیط/میزبان باعث شکست تخصیص می‌شود.
- **B)** معماری فعلی اجرای Worker/MTProto/Fiber ذاتاً با این runtime
  ناسازگار است.

از شما هم می‌خواهیم تا قبل از داشتن این سه عدد، بین A و B قضاوت نکنید.

---

## ۳) مسئله‌ی دوم — گرسنگی Worker (اثبات‌شده، اصلاح‌شده)

این را برای درک کامل وضعیت می‌نویسیم؛ نیاز به ریشه‌یابی مجدد ندارد.

### ۳-۱) مکانیزم

پرس‌وجوی انتخاب در `class-gs-auto-worker.php` (خطوط ۳۷۵–۴۰۳):

```sql
WHERE state NOT IN (<۶ حالت پایانی>)
  AND (locked_until  IS NULL OR locked_until  < NOW())
  AND (next_retry_at IS NULL OR next_retry_at <= NOW())
ORDER BY (attempts >= 5) ASC, priority DESC, id ASC
```

اما `class-gs-chain-engine.php` خط ۱۳۳۲ این را برمی‌گرداند:
```php
return array( 'state' => 'CHAIN_WAITING', 'waiting' => true );
```
بدون نوشتن هیچ `next_retry_at`. بلافاصله بعدش بلوک `finally` خط ۱۳۳۳
تابع `release()` را صدا می‌زند که `locked_until` را `NULL` می‌کند
(`class-gs-session.php` خط ۱۸۸).

نتیجه: هر دو شرط در هر تیک `NULL` هستند، `id ASC` همان Session را دوباره
انتخاب می‌کند. **حدود ۱۹۰ دور پیاپی `ids=[68]`.**

### ۳-۲) زنجیره‌ی علّی پذیرفته‌شده

```
گرسنگی Worker → Sessionها هرگز جلو نمی‌روند → صف انتشار رشد می‌کند
              → backlog=109 → Watcher ورودی را می‌بندد
```

**Watcher معلول است، نه علت.** فرضیه‌های «Watcher علت است» و «صف انتشار
علت است» هر دو **رد شده‌اند**.

### ۳-۳) اصلاح‌ها

| نسخه | اقدام | نتیجه روی میزبان |
|---|---|---|
| 10.12.18 | مهلت مبتنی بر ثابت قفل | ❌ بی‌اثر |
| 10.12.19 | عقب‌گرد حالت | 🔶 نسبی |
| 10.12.20 | `next_retry_at` با backoff به‌ازای هر حالت | ✅ **مکانیزم اثبات شد** |
| 10.12.21 | اصلاح دو نقص جانبی | ✅ **نصب شد — دامپ `06:11:35` تأیید کرد** |

شاهد قطعی از دامپ میزبان (`2026-09-11 05:55:26`) — اولین تغییر شناسه
بعد از ۱۹۰ دور:
```
01:58:14  DEFER #68 → next=02:04:14
02:02:47  PICK  ids=[69]        ← عوض شد
02:03:34  DEFER #69 → next=02:09:34
```

### ۳-۴) دو نقصی که در ۱۰.۱۲.۲۱ رفع شد

**نقص یک — اندازه‌گیری فاصله از option اشتباه.** کد ما زمان تیک قبلی را
از یک option می‌خواند که **خودش چند خط پایین‌تر در همان تابع** آن را
می‌نوشت. پس همیشه مقدار تیک *جاری* را می‌خواند، نه قبلی. سه روز
`interval=300s` لاگ شد در حالی که فاصله‌های واقعی ۹۱ و ۲۶ و ۳۱ دقیقه
بود. منبع درست حالا ردیف `sti_gs_gate_auto_worker` است که
`Cron_Gate::pass()` با CAS اتمیک نگه می‌دارد، و **قبل** از فراخوانی
`pass()` خوانده می‌شود.

**نقص دو — کامنتی بدون کد.** در `advance_one()` توضیحی وجود داشت که
وعده‌ی «مهلت انتظار» می‌داد، ولی **هیچ کدی برایش نوشته نشده بود**.
Session ‎#68 از ۸ تا ۱۱ سپتامبر (بیش از ۷۲ ساعت) با `attempts=0` گیر ماند —
چون انتظار خطا شمرده نمی‌شود، شمارنده‌ی تلاش بالا نمی‌رفت و حتی سقف تلاش
هم آزادش نمی‌کرد. حالا `WAITING_DEADLINE = 12 * HOUR_IN_SECONDS` اجرا
می‌شود و Session به `NEEDS_REVIEW` می‌رود (حذف نمی‌شود).

### ۳-۵) تأیید روی میزبان — دامپ `2026-09-11 06:11:35`

نسخه ۱۰.۱۲.۲۱ نصب شد و دامپ تازه گرفته شد. **هر دو اصلاح کار کردند:**

```
06:08:08  AUTO_WORKER_PICK: selected=6 ids=[68,69,70,78,79,80]
06:08:27  DEFER #80 state=SCANNED delay=1344s interval=896s factor=1.5
06:08:27  DEFER #79 state=SCANNED delay=1344s interval=896s factor=1.5
```

| سنجه | قبل | بعد |
|---|---|---|
| `interval` | ۳۰۰ ثانیه (عدد تنظیمات) | **۸۹۶ ثانیه (اندازه‌گیری واقعی)** |
| `delay` | ۳۶۰ ثانیه | **۱۳۴۴ ثانیه** |
| `selected` | ۱ | **۶** |
| صف در دسترس | فقط `[68]` | `[68,69,70,78,79,80]` |
| `eligible_queue` | ۱۰۹ | ۱۰۷ |

اندازه‌گیری فاصله حالا درست کار می‌کند و صف دیگر روی یک Session قفل
نیست. اما ⬇️

### ۳-۶) ★ نقص چهارم — کشف‌شده، هنوز رفع‌نشده

`AUTO_WORKER_WAIT_DEADLINE` در دامپ **صفر بار** ظاهر شد، با اینکه ‎#68
بیش از ۸۰ ساعت در `CHAIN_WAITING` است و آستانه ۱۲ ساعت است.

**علت — با ارجاع خطی:**

`class-gs-auto-worker.php` خط ۸۵۳ آستانه را از `updated_at` می‌سنجد:
```php
$updated = isset( $session['updated_at'] ) ? strtotime( (string) $session['updated_at'] ) : 0;
if ( $updated > 0 && ( time() - $updated ) > self::WAITING_DEADLINE ) {
```

اما `class-gs-session.php` خط ۱۶۷ — متد `claim()` — در **هر** تصاحب
`updated_at` را تازه می‌کند:
```sql
UPDATE {$table} SET locked_until = %s, worker_id = %s, updated_at = %s
 WHERE id = %d AND (locked_until IS NULL OR locked_until < %s)
```

و `class-gs-session.php` خط ۱۵۵ — متد `update()` — همین کار را می‌کند:
```php
$row['updated_at'] = current_time( 'mysql' );
```

`class-gs-chain-engine.php` در چهار نقطه (خطوط ۱۱۲، ۳۰۱، ۵۱۰، ۷۸۲)
`claim()` را در هر تیک صدا می‌زند.

**نتیجه:** `updated_at` هرگز کهنه نمی‌شود. اختلاف `time() - $updated`
همیشه چند ثانیه است، نه چند ساعت. **شرط هیچ‌وقت درست نمی‌شود.**

> این دقیقاً **همان الگوی اشتباهِ نقص یک** است: ستونی به‌عنوان مرجع
> زمان انتخاب شد که خودِ چرخه‌ی اندازه‌گیری آن را بازنویسی می‌کند. دو
> بار در یک نسخه.

**نکته‌ی مهم برای شما:** این باگ باعث می‌شود `class-gs-recovery.php`
خطوط ۴۵۳ و ۴۹۳ هم — که با `AND updated_at < %s` دنبال Session یتیم
می‌گردند — هرگز چیزی پیدا نکنند. یک ستون خراب، دو سازوکار بازیابی را
از کار انداخته است.

مسیر اصلاح پیشنهادی (اجرا نشده، منتظر نظر شما): استفاده از ستونی که
چرخه‌ی Worker آن را لمس نمی‌کند — مثل `clicked_at` یا
`last_polled_at` — یا افزودن یک `waiting_since` که فقط در **گذارِ
ورود** به حالت انتظار نوشته شود.

### ۳-۷) نکته‌ی مهم درباره‌ی WP-Cron

زمان‌بندی این سایت WP-Cron است، یعنی **بازدید-محور**. فاصله‌ی تنظیم‌شده
۵ دقیقه است ولی فاصله‌ی واقعی بین ۱۴۶۰ تا ۵۴۶۰ ثانیه نوسان دارد. هر
محاسبه‌ای که «دوره‌ی تنظیم‌شده» را دوره‌ی واقعی فرض کند، غلط از آب
درمی‌آید — سه بار این اشتباه را کرده‌ایم.

---

## ۴) مسئله‌ی سوم — ۸۲۵ رکورد یتیم

| مورد | مقدار |
|---|---|
| کل واجد شرایط | ۵۸۱۰ |
| یتیم | **۸۲۵** |
| معتبر | ۴۹۸۵ |
| ۵۰۰ کاندیدای اول | ۱۰۰٪ `NO_PK` |
| اولین شناسه‌ی معتبر | `23339` — رتبه‌ی ۸۲۶ |

همه‌ی یتیم‌ها: `profile_id=1`، کلیدواژه‌ی `Mockup`، تاریخ ساخت یکسان
`2026-08-15 12:45:48`. الگو نشان می‌دهد یک عملیات دسته‌ای ناقص مانده.

دست‌نخورده رها شده. اگر در ریشه‌یابی به آن خوردید خبر بدهید، ولی
اولویت اول نیست.

---

## ۵) آنچه قبلاً رد شده — لطفاً تکرار نکنید

هر کدام از این‌ها را آزموده‌ایم و نتیجه نداده. برای صرفه‌جویی در وقت شما:

| مسیر | چرا رد شد |
|---|---|
| بالا بردن `memory_limit` | خطا از `mmap` خام است، نه heap مدیریت‌شده‌ی PHP. بی‌ربط. |
| تغییر `fiber.stack_size` | بدون دانستن اینکه کدام سقف خورده، صرفاً حدس است. |
| غیرفعال کردن Fiber | MadelineProto بدون آن کار نمی‌کند. |
| رشته‌گردی روی متن exception | **اثبات‌شده غیرقابل‌اتکا** — `try/catch` ما تشخیص بالادستی را خفه می‌کرد. |
| `@file_get_contents` داخل runtime افزونه | بخش ۲-۶ را ببینید. حتماً `is_readable()` + handler اختصاصی. |
| `pkill` سراسری | ممنوع. |
| بالا بردن `sessions_per_tick` / `max_active_sessions` | `ORDER BY id ASC` و قفل تک-ربات آن را خنثی می‌کند. |
| Watcher به‌عنوان درمان اصلی | Watcher معلول است. |
| تکیه بر `STI_MTProto::is_fiber_error()` | با این خطا تطبیق نمی‌خورد (بخش ۲-۷). |
| `class-gs-recovery.php` مسیر heal | روی این میزبان بی‌اثر است چون `exec` غیرفعال است. |

همچنین دو یافته‌ی داخلی که **دوباره طرح نکنید**، چون بررسی و بسته شده‌اند:

- «سیستم قفل بی‌اثر است» — **غلط**. `STI_GS_Session::claim()` خط ۱۶۰
  با CAS اتمیک `locked_until` می‌نویسد و ۱۶ محل فراخوانی دارد.
- «`selected=1` یعنی منطق دسته‌ای خراب است» — **غلط**. کد واقعاً شش ردیف
  می‌خواهد؛ بقیه `next_retry_at` آینده دارند و شرط `WHERE` درست
  حذفشان می‌کند.

---

## ۶) محدودیت‌های سخت — چه کارهایی مجاز نیست

این‌ها خواسته‌ی صریح مالک سامانه است، نه ترجیح ما:

**داده:**
- ❌ حذف Session
- ❌ پاک کردن backlog
- ❌ حذف یا ویرایش رکوردهای یتیم بدون تأیید جداگانه
- ❌ هرگونه تغییر مخرب در پایگاه داده
- ❌ ریست حساب / logout / حذف فایل session تلگرام

**معماری:**
- ❌ بازطراحی Worker
- ❌ تغییر لایه‌ی MTProto یا Fiber
- ❌ تغییر سقف تلاش (retry limit)
- ❌ بالا بردن `backlog_limit`
- ❌ فعال کردن Watcher به‌عنوان درمان اصلی

**روش:**
- ❌ ویرایش خودکار `wp-config.php`
- ❌ هیچ warning/notice/fatal در رابط کاربری یا خروجی JSON/AJAX
- ❌ حلقه‌ی retry بی‌پایان یا polling سنگین AJAX
- ❌ پردازش هم‌زمان بدون governor

**آزمون‌های تشخیصی شما باید فقط-خواندنی باشند.** اگر برای اثبات یک
فرضیه لازم است چیزی نوشته یا تغییر داده شود، اول بگویید — اجرا نکنید.

---

## ۷) روش کاری‌ای که از شما انتظار داریم

این قواعد را خودمان هم رعایت می‌کنیم:

1. **علامت ≠ ریشه.** «صف پر شده» یک علامت است.
2. **هر ادعا با ارجاع.** فایل + کلاس + متد + شماره‌ی خط. برای رفتار
   زمان اجرا: لاگ یا خروجی دستور با timestamp.
3. **حداقل سه فرضیه‌ی جایگزین** قبل از انتخاب یکی.
4. **درجه‌ی اطمینان بنویسید** — «قطعی / محتمل / ممکن»، نه «۱۰۰٪».
5. **آنچه قابل مشاهده نیست را «UNOBSERVABLE» بنویسید**، حدس نزنید.
6. **تمایز منبع/کسب‌وکار:** محدودیت منابع (رم، نگاشت، rlimit) با باگ
   منطق کسب‌وکار خلط نشود.
7. **گزارش را با یکی از این دو تمام کنید:**
   `ROOT CAUSE PROVEN` یا `ROOT CAUSE NOT YET PROVEN`.

آخرین نکته از تجربه‌ی تلخ خودمان: **کامنت داخل کد، اثبات وجود آن رفتار
نیست.** ما یک بار روی توضیحی حساب کردیم که کدش هرگز نوشته نشده بود
(بخش ۳-۴). هر رفتاری را با `grep` روی خود کد تأیید کنید.

---

## ۸) داده‌ای که خودمان جمع کرده‌ایم

بار تشخیصی `OOM_DIAG` که در نسخه‌ی ۱۰.۱۲.۱۷+ ثبت می‌شود شامل این
فیلدهاست:

```
meminfo · status · limits · cgroup{raw, v2, v1}
maps_count · max_map_count · rlimit_data · rlimit_as · rlimit_stack
memory_limit · memory_usage · memory_peak
ipc_heal_possible · ipc_heal_reason
diagnostic_read_errors[]
sapi · pid · timestamp
oom_gate_result · oom_gate_reason
error_message_raw · error_class_matched
exception_class · exception_message · exception_origin · exception_trace
session_id · stage · action
```

> ⚠️ **مشکل:** این بار فقط **در لحظه‌ی شکست Fiber** نوشته می‌شود. از
> زمان انتشار ۱۰.۱۲.۱۷ تا امروز هیچ شکست تازه‌ای رخ نداده، پس این
> فیلدها **هرگز پر نشده‌اند**. تمام اعداد بخش ۹ برای ما مجهول‌اند.
>
> این دقیقاً شکافی است که شما می‌توانید پر کنید — شما لازم نیست منتظر
> شکست بمانید.

---

## ۹) داده‌ای که از شما می‌خواهیم

### ۹-۱) سه عدد الزامی

بدون این سه، هیچ نتیجه‌ای قابل دفاع نیست:

```bash
# ۱ — تعداد نگاشت‌های حافظه در فرآیند PHP
wc -l < /proc/<PID_of_lsphp>/maps

# ۲ — سقف کرنل
sysctl vm.max_map_count            # یا: cat /proc/sys/vm/max_map_count

# ۳ — سقف داده
grep -E 'Max data size' /proc/<PID_of_lsphp>/limits
```

نکته: PID را از فرآیندی بگیرید که **واقعاً Worker را اجرا می‌کند**.
روی LiteSpeed فرآیند lsphp پایاست؛ لطفاً هم یک فرآیند **تازه** و هم یک
فرآیند **قدیمی** را بسنجید و عمرشان را هم بنویسید. اختلاف بین این دو
مستقیماً فرضیه‌ی ۲-۴ را تأیید یا رد می‌کند.

### ۹-۲) داده‌ی تکمیلی

```bash
grep -E 'VmData|VmSize|VmPeak|VmRSS' /proc/<PID>/status
grep -E 'Max address space|Max stack size' /proc/<PID>/limits
grep -E 'MemAvailable|CommitLimit|Committed_AS|SwapFree' /proc/meminfo
sysctl vm.overcommit_memory vm.overcommit_ratio

cat /sys/fs/cgroup/memory.max
cat /sys/fs/cgroup/memory.current
cat /sys/fs/cgroup/memory.events
# اگر cgroup v1 بود:
cat /sys/fs/cgroup/memory/memory.failcnt

# اگر CloudLinux/LVE است:
cat /proc/bc/$(id -u)/meminfo

php -i | grep -E 'fiber.stack_size|memory_limit'
```

### ۹-۳) آزمون مستقیم MadelineProto

> ⛔ **اصلاحیه‌ی ۱۱ سپتامبر — نسخه‌ی قبلی این بند خطرناک بود.**
> پیش‌تر اینجا `Tools::testFibers()` توصیه شده بود. **آن را اجرا نکنید.**
> بررسی بدنه‌ی تابع در `madeline81.phar` نشان داد امضایش
> `testFibers(int $fiberCount = 100000)` است، `ini_set('memory_limit', -1)`
> اجرا می‌کند و تا صد هزار Fiber می‌سازد و شروع می‌کند تا به شکست برسد.
> این یک **آزمون فشار** است، نه خواندن اطلاعات، و روی میزبان اشتراکی
> می‌تواند کل حساب را با OOM مواجه کند.

فقط این دو امن‌اند — هر دو صرفاً `/proc` را می‌خوانند و هیچ Fiber
نمی‌سازند:

```php
\danog\MadelineProto\Tools::getMaps();     // شمارش خطوط /proc/self/maps
\danog\MadelineProto\Tools::getMaxMaps();  // خواندن vm.max_map_count
```

اگر به هر دلیل به `testFibers` نیاز شد، **حتماً با آرگومان صریح و کوچک**
و ترجیحاً خارج از ساعات کاری — مثل کاری که خود کتابخانه در
`Magic.php` می‌کند: `testFibers(100)`.

### ۹-۴) نمونه‌ی کامل یک شکست واقعی

اگر توانستید خطا را بازتولید کنید، این اقلام را **جداگانه** ثبت کنید
(نه در یک رشته‌ی درهم):

`session_id` · `state` · `stage` · `action` · کلاس exception ·
پیام exception · stack trace کامل · داده‌ی تشخیصی

پیام اصلی باید **بایت‌به‌بایت** حفظ شود.

### ۹-۵) جدول تصمیم — تفسیر اعداد

این جدول را از قبل ساخته‌ایم تا نتیجه‌گیری یکدست باشد:

| مشاهده | نتیجه |
|---|---|
| `maps_count` ≈ `max_map_count` | ✅ سقف نگاشت کرنل — **اول سقف مؤثر فعلی را بخوانید**، بعد درباره‌ی افزایش تصمیم بگیرید |
| `VmSize`/`VmPeak` ≈ `Max address space` | ✅ سقف `RLIMIT_AS` یا VMEM سطح LVE |
| `VmData` ≈ `Max data size` | ✅ سقف `RLIMIT_DATA` |
| `Committed_AS` ≈ `CommitLimit` | ⚠️ فقط اگر `vm.overcommit_memory=2` باشد — اول سیاست را بخوانید |
| `MemAvailable` و `SwapFree` نزدیک صفر | ✅ فشار واقعی رم |
| `memory.failcnt > 0` یا `memory.events` غیرصفر | ⚠️ شمارنده **تجمعی** است و می‌تواند تاریخی باشد — دلتا بگیرید |
| **همه‌ی موارد بالا فضای آزاد دارند** | ⚠️ آنگاه گزینه‌ی **B** جدی می‌شود: معماری برای این runtime سنگین است |
| هیچ‌کدام قابل خواندن نبود | ❌ `UNKNOWN` + صریحاً بنویسید کدام فیلد در دسترس نبود |

> **«در دسترس نبودن» خطا نیست.** اگر مسیری خوانده نشد، بنویسید
> `unavailable` و ادامه بدهید. ریشه‌یابی نباید به‌خاطرش متوقف شود.

> ⚠️ **هشدار درباره‌ی خودِ این جدول (۱۱ سپتامبر).** هیچ ردیفی از این
> جدول به‌تنهایی اثبات نیست. `maps_count` بالا بدون دانستن سقف مؤثر
> بی‌معناست؛ شمارنده‌های cgroup تجمعی‌اند؛ و «همه‌چیز آزاد است ⇒ پس
> معماری مقصر است» یک استدلال از روی سکوت است، نه شاهد مثبت.
> برای گزینه‌ی **B** به شاهد مستقل نیاز است، نه صرفاً نبود شاهد برای A.

---

## ۱۰) قالب گزارش درخواستی

لطفاً گزارش نهایی را **دقیقاً** با این چهار بخش بدهید:

---

### `Original Error`
متن دقیق خطا، بایت‌به‌بایت، به‌همراه کلاس exception و محل پرتاب.
هیچ بازنویسی، خلاصه‌سازی یا ترجمه‌ای نکنید.

### `Diagnostic Data`
هر عددی که **واقعاً** خوانده‌اید، با ذکر مسیر منبع و زمان خواندن.
اعداد را از حدس جدا کنید.

### `Unavailable Data`
فهرست صریح آنچه نتوانستید بخوانید و **چرا**. این بخش خالی نماند —
اگر همه‌چیز خوانده شد، بنویسید «هیچ».

### `Next Root-Cause Decision`
بین این دو انتخاب کنید و **دلیل عددی** بیاورید:

- **A)** محدودیت محیط/میزبان باعث شکست تخصیص می‌شود.
- **B)** معماری فعلی اجرای Worker/MTProto/Fiber با این runtime ناسازگار
  است.

اگر داده کافی نبود، صریحاً بنویسید `ROOT CAUSE NOT YET PROVEN` و مشخص
کنید **دقیقاً کدام عدد** برای تصمیم کم است.

اگر A است: پارامتر دقیق، مقدار فعلی، مقدار پیشنهادی، و اینکه آیا در
اختیار مالک است یا نیازمند هاستینگ.

اگر B است: کدام بخش معماری، چه شواهدی، و کم‌ریسک‌ترین مسیر تغییر.

---

## ۱۱) فایل‌های کلیدی برای شروع

| اولویت | مسیر | چرا |
|---|---|---|
| ۱ | `includes/class-sti-mtproto.php` خط ۶۸۹ | نقطه‌ی دقیق پرتاب |
| ۱ | `includes/golden-scan/class-gs-env-diag.php` | ابزار تشخیص + `safe_read()` |
| ۲ | `includes/golden-scan/class-gs-auto-worker.php` | زمان‌بند، انتخاب، backoff |
| ۲ | `includes/golden-scan/class-gs-chain-engine.php` خط ۱۳۳۲ | مبدأ `CHAIN_WAITING` |
| ۱ | `includes/golden-scan/class-gs-session.php` خط ۱۵۵/۱۶۷ | ★ منشأ نقص چهارم — بازنویسی `updated_at` |
| ۳ | `includes/golden-scan/class-gs-session.php` خط ۱۸۸ | `release()` |
| ۳ | `includes/golden-scan/class-gs-recovery.php` خط ۴۵۳/۴۹۳ | قربانی دوم همان ستون |
| ۳ | `includes/golden-scan/class-gs-cron-gate.php` | گیت CAS اتمیک |
| ۴ | `GUZARESH-10.12.21.md` | گزارش کامل آخرین دور |
| ۴ | `tests/` | ۹ مجموعه‌ی تست ایستا |

**توضیح درباره‌ی تست‌ها:** در محیط توسعه‌ی ما مفسر PHP نصب نیست. تمام
اعتبارسنجی‌ها = بازرسی کد + تست ایستای متنی. **هیچ‌کدام از تست‌های ما
اثبات زمان اجرا نیستند** و این را پنهان نمی‌کنیم. اگر روی محیطی با PHP
کار می‌کنید، نتیجه‌ی اجرای واقعی بر همه‌ی تست‌های ما ارجحیت دارد.

---

## ۱۲) اگر فقط یک ساعت وقت دارید

۱. سه دستور بخش ۹-۱ را روی یک فرآیند `lsphp` **قدیمی** بگیرید.
۲. `Tools::getMaps()` و `Tools::getMaxMaps()` را اجرا کنید.
۳. نتیجه را با ردیف اول جدول ۹-۵ مقایسه کنید.

اگر `maps_count` نزدیک `max_map_count` بود، مسئله در همان ساعت اول
حل شده و **نیازی به هیچ تغییر کدی نیست**.
