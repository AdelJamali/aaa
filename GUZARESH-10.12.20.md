# گزارش ۱۰.۱۲.۲۰ — مهلت از فاصله‌ی واقعی، و عبور از موارد منتظر

> نسخه **10.12.20** · کد `e15def0` · ZIP `deb00a0`
> `golden-importer-10.12.20.zip` · ۱۳۲ فایل · sha256 `40d1c741b94fbc5e2dc58737d7bd338b84616cb7b717918c25f118b90e34a480`

---

## ۱) خطای اصلی (Original Error)

بدون تغییر:

```
Fiber stack allocate failed: mmap failed: Cannot allocate memory (12)
```

**ROOT_CAUSE_UNKNOWN** — منتظر `/proc/self/maps` · `vm.max_map_count` · `RLIMIT_DATA`.

---

## ۲) داده‌های تشخیصی (Diagnostic Data)

### ✅ اول: پچ ۱۰.۱۲.۱۹ **اجرا شد** — ابزار تشخیص پنهانش کرده بود

در نگاه اول به‌نظر می‌رسید هیچ اتفاقی نیفتاده، چون `AUTO_WORKER_DEFER`
در لاگ دیده نمی‌شد. **علت پیدا شد:**

`class-gs-chain-audit.php:1235-1238` پنل لاگ را با `LIKE` روی **هشت الگوی
ثابت** فیلتر می‌کند:

```
AUTO_WORKER_TICK · AUTO_WORKER_BLOCKED · AUTO_WORKER_PICK · AUTO_WORKER_STAGE
AUTO_WORKER_SELFHEAL · LINE_START_FAILED · LINE_PERSISTENCE · CRON_GATE
```

`AUTO_WORKER_DEFER` در این فهرست **نبود**. لاگ نوشته می‌شد ولی پنل هرگز
نشانش نمی‌داد. این نقص ابزار من بود، نه نبودِ اثر.

**دو شاهد مستقل که ثابت می‌کنند پچ اجرا شده:**

| نشانه | دامپ ۱۹:۱۸ | دامپ ۲۰:۱۳ |
|---|---|---|
| `retry_wait` | ۱ | **۰** |
| فاصله‌ی تیک‌ها | ~۵-۱۵ دقیقه | **~۲۵ دقیقه** |

### ❌ اما دو نقص واقعی هم بود

#### نقص ۱ — مهلت هنوز از فاصله‌ی واقعی کوتاه‌تر بود

مهلت ۱۰.۱۲.۱۹ بر پایه‌ی `worker_interval = 300` حساب می‌شد ⇒ ۳۶۰ ثانیه
برای `CHAIN_WAITING`. اما فاصله‌ی **واقعی** تیک‌ها در همین دامپ:

```
19:16:25  →  19:46:13   = ۱۷۸۸ ثانیه
19:46:13  →  20:10:33   = ۱۴۶۰ ثانیه
```

**تقریباً پنج برابر عدد تنظیمات.** علتش این است که WP-Cron با بازدید سایت
اجرا می‌شود، نه با ساعت سیستم. پس مهلت ۳۶۰ ثانیه‌ای مدت‌ها پیش از تیک بعدی
منقضی می‌شد و `#68` دوباره انتخاب می‌شد:

```
19:46:13  AUTO_WORKER_PICK: ids=[68]
20:10:33  AUTO_WORKER_PICK: ids=[68]
```

#### نقص ۲ — یک Session منتظر، یک دور کامل را می‌سوزاند

با `sessions_per_tick = 1`، وقتی `#68` منتظر است کل آن دور بی‌حاصل می‌ماند.
و چهار مورد اول صف **همگی** منتظرند:

```
#68  CHAIN_WAITING     #70  WAITING_BOT
#69  CHAIN_WAITING     #78  WAITING_BOT
#79  SCANNED   ← اولین موردی که می‌تواند جلو برود
```

با فاصله‌ی واقعی ~۲۵ دقیقه، رسیدن به `#79` **بیش از ۱.۵ ساعت** طول می‌کشید.

---

## ۳) اصلاح انجام‌شده

### الف) مهلت از فاصله‌ی **مشاهده‌شده**، نه تنظیم‌شده

هر تیک فاصله‌ی واقعی از تیک قبلی را ثبت می‌کند:

```php
$avg = $prev_avg > 0 ? (int) round( ( $prev_avg * 2 + $gap ) / 3 ) : $gap;
update_option( self::OBSERVED_GAP_KEY, $avg, false );
```

میانگین متحرک وزن‌دار تا یک نوسان عدد را نپراند، با سقف ۶ ساعت برای کنار
گذاشتن مقدارهای پرت (ری‌استارت، خاموشی طولانی).

و مهلت از عدد بدبینانه‌تر گرفته می‌شود:

```php
protected static function effective_interval() {
    return max( $configured, $observed );
}
```

با فاصله‌ی واقعی ~۱۶۰۰ ثانیه، مهلت `CHAIN_WAITING` می‌شود **~۱۹۲۰ ثانیه**
به‌جای ۳۶۰ — و **خودش را با شرایط میزبان تنظیم می‌کند**.

### ب) عبور از موارد منتظر در همان تیک

```php
$work_budget = self::effective_batch_size();          // = 1
$sessions    = self::pick( $work_budget + self::WAITING_SKIP_BUDGET );  // = 6
```

و در حلقه، فقط **پیشرفت واقعی** از بودجه کم می‌کند:

```php
if ( 'waiting' === $outcome || 'skipped' === $outcome ) {
    self::defer_waiting( ... );      // بودجه دست‌نخورده
} else {
    $work_done++;                    // advanced / completed / failed
}
```

یعنی همان تیک از `#68` تا `#78` رد می‌شود و به `#79` می‌رسد که واقعاً
می‌تواند جلو برود. **ظرفیت کار عوض نشده** — فقط عمق جست‌وجو.

> **قانون `bot_used` دست‌نخورده است:** هنوز فقط یک Session در هر تیک
> اجازه‌ی گفت‌وگو با ربات دارد، پس فشار تلگرام بالا نمی‌رود.

### ج) رفع نقص ابزار تشخیص

`AUTO_WORKER_DEFER` به فیلتر پنل اضافه شد تا دفعه‌ی بعد قابل مشاهده باشد.

---

## ۴) آنچه تغییر نکرد

`sessions_per_tick = 1` و `max_active_sessions = 1` طبق تصمیم شما
دست‌نخورده — هر دو در فرم «اتوماسیون» قابل تنظیم‌اند (بازه ۱ تا ۱۰).

همچنین: سقف تلاش · `backlog_limit` · فهرست‌های `TERMINAL`/`WAITING` ·
گیت `max_active` · Fiber · MTProto · منطق صف · ماشین حالت Session.
هیچ Sessionای دست نخورد.

---

## ۵) اعتبارسنجی

| مجموعه | نتیجه |
|---|---|
| `test-10-12-20-starvation.py` (۵۴ ادعا) | ALL PASS |
| ۸ مجموعه‌ی دیگر | ALL PASS |

بلوک جدید `O1`-`O12` صحت محاسبه‌ی فاصله و منطق عبور را می‌سنجد، و `A1`
تأیید می‌کند پنل تشخیص می‌تواند لاگ جدید را نشان دهد. `O12` مخصوصاً
بررسی می‌کند قانون `bot_used` دست‌نخورده مانده.

ZIP مستقل باز و بررسی شد: نسخه ۱۰.۱۲.۲۰، هر سه ثابت جدید حاضر، فیلتر
پنل اصلاح‌شده.

> ⚠️ در محیط توسعه PHP نصب نیست. اعتبارسنجی = بازرسی کد + تست ایستا.

---

## ۶) نشانه‌ی موفقیت پس از نصب

```
AUTO_WORKER_PICK:  selected=6 ids=[68,69,70,78,79,80]
AUTO_WORKER_DEFER  session=#68 delay=1920s interval=1600s factor=1.2
AUTO_WORKER_DEFER  session=#69 delay=1920s ...
AUTO_WORKER_DEFER  session=#70 delay=3200s ...
AUTO_WORKER_DEFER  session=#78 delay=3200s ...
AUTO_WORKER_STAGE  session=#79 from=SCANNED to=... result=advanced
```

**سه نشانه:**
1. `selected=6` به‌جای `selected=1`
2. خطوط `AUTO_WORKER_DEFER` حالا **دیده می‌شوند**
3. **«مرحله جلو رفت» از صفر بالا می‌رود**

---

## ۷) تصمیم بعدی درباره‌ی Root Cause

| # | مسئله | وضعیت |
|---|---|---|
| ۱ | گرسنگی Worker | **PROVEN** — مکانیزم ۱۸، مقیاس ۱۹، فاصله‌ی واقعی + عبور ۲۰ |
| ۲ | ۸۲۵ رکورد orphan | شناسایی‌شده، **رفع نشده** |
| ۳ | `Fiber stack allocate failed` | **ROOT_CAUSE_UNKNOWN** |

گروه‌بندی خطاها همچنان **۱۶ مورد** `file_get_contents(memory.max)` نشان
می‌دهد — **بدون افزایش** نسبت به دو دامپ قبل. اصلاح ۱۰.۱۲.۱۷ پابرجاست و
این ردیف‌ها تاریخی‌اند.

**«Root Cause مسئله‌ی Fiber هنوز اثبات نشده است.»**

---

## ۸) درس این دور

سه بار پشت‌سرهم عدد را از منبع اشتباه گرفتم:

| نسخه | عدد از کجا آمد | چرا غلط بود |
|---|---|---|
| ۱۰.۱۲.۱۸ | قفل موتور (۴۵/۹۰ ثانیه) | قفل ربطی به زمان‌بندی ندارد |
| ۱۰.۱۲.۱۹ | تنظیمات (۳۰۰ ثانیه) | تنظیمات ≠ رفتار واقعی WP-Cron |
| ۱۰.۱۲.۲۰ | **اندازه‌گیری واقعی** | ✅ |

قاعده‌ای که ثبت می‌شود: **وقتی زمان‌بندی به رفتار محیط وابسته است
(WP-Cron، صف، شبکه)، عدد را باید اندازه گرفت — نه از تنظیمات خواند.**

و درس دوم: **وقتی یک اصلاح بی‌اثر به‌نظر می‌رسد، اول بررسی کن آیا ابزار
اندازه‌گیری می‌تواند آن را ببیند.** نبودِ لاگ در پنل، دلیلِ نبودِ رفتار
نبود — فیلتر پنل آن را حذف می‌کرد.
