# تحلیل دامپ میزبان — ۱۰.۱۲.۲۲

> دامپ: `2026-09-11 23:15:01` · نسخه‌ی فعال: **10.12.22** (تأیید شد)
> پنجره‌ی لاگ: از `2026-09-04 05:20` تا `2026-09-11 23:15`

---

## ۰) نتیجه در یک خط

# 🎉 مرحله جلو رفت = ۳

**بعد از سه روز صفر مطلق، خط لوله حرکت کرد.**

---

## ۱) شمارنده‌های امروز

| سنجه | دیروز (۱۰.۱۲.۲۱) | امروز (۱۰.۱۲.۲۲) |
|---|---|---|
| **مرحله جلو رفت** | **۰** | **۳** ✅ |
| منتظر پاسخ ربات | ۶ | ۲۰ |
| کامل شد | ۰ | ۰ |
| خطا خورد | ۰ | ۱ |
| تعداد دورها | ۶ | ۲۱ |

---

## ۲) شاهد قطعی — صف واقعاً جلو رفت

### ۲-۱) گذار مشاهده‌شده

```
17:27:15  AUTO_WORKER_STAGE session=#81 from=SCANNED to=BUTTON_FOUND result=advanced
```

**این اولین `result=advanced` در تمام دامپ‌های این پرونده است.**

### ۲-۲) وضعیت صف — قبل و بعد

| id | دیروز | امروز |
|---|---|---|
| ۶۸ | `CHAIN_WAITING` | `CHAIN_WAITING` (بدون تغییر) |
| ۶۹ | `CHAIN_WAITING` | `CHAIN_WAITING` (بدون تغییر) |
| ۷۰ | `WAITING_BOT` | **از صف خارج شد** |
| ۷۸ | `WAITING_BOT` | `WAITING_BOT` |
| **۷۹** | `SCANNED` | **`BUTTON_FOUND`** ✅ |
| **۸۰** | `SCANNED` | **`BUTTON_FOUND`** ✅ |
| **۸۱** | `SCANNED` | **`BUTTON_FOUND`** ✅ |
| ۸۲، ۸۶، ۸۷، ۸۹ | `SCANNED` | `SCANNED` (نوبتشان نشده) |

**سه Session از `SCANNED` به `BUTTON_FOUND` رسیدند.**

### ۲-۳) عقب‌گرد نمایی هم درست کار می‌کند

```
19:45:10  DEFER #68 delay=3161s interval=2634s factor=1.2
```

`interval=2634s` یعنی حدود ۴۴ دقیقه — عدد **واقعی** اندازه‌گیری‌شده،
نه ۳۰۰ ثانیه‌ی تنظیمات. اصلاح ۱۰.۱۲.۲۱ پابرجاست و حالا با فاصله‌ی
واقعی میزبان کالیبره شده.

---

## ۳) ★ حلقه‌ی `SCANNED → skipped` — حالت (ج) اثبات شد

نسخه‌ی ۱۰.۱۲.۲۲ ساخته شد تا بین سه احتمال داوری کند. **داوری انجام
شد:**

```
grep "AUTO_WORKER_ROUTE" → صفر نتیجه
```

خط `AUTO_WORKER_ROUTE` **اصلاً ثبت نشده**. یعنی `fallback_to_legacy()`
هرگز صدا زده نشده. و در کنارش:

```
STAGE session=#81 from=SCANNED to=BUTTON_FOUND result=advanced
```

**⇒ نتیجه‌ی قطعی: حالت (ج) درست بود.**

| احتمال | وضعیت |
|---|---|
| الف) بازگشت به Chain Init | ❌ **رد شد** — `fallback` اصلاً اجرا نشد |
| ب) Skip در Button Resolver | ❌ **رد شد** (قبلاً با `no_progress` اثبات شده بود) |
| **ج) گره‌ها به Resolver قدیمی می‌رفتند** | ✅ **تأیید شد** |

### معنایش چیست

`SCANNED → skipped` **یک باگ نبود.** مسیریابی درست کار می‌کرد و
Sessionها به Button Resolver می‌رفتند. آنچه واقعاً جلویشان را گرفته
بود، **گرسنگی صف** بود: تا وقتی `#68` هر تیک نوبت را می‌گرفت، نوبت
هرگز به `#79` و بعدی‌ها نمی‌رسید.

اصلاح `next_retry_at` در ۱۰.۱۲.۲۰/۲۱ صف را باز کرد، و امروز اولین
میوه‌اش را داد.

> **این یعنی من در تحلیل قبلی اشتباه می‌کردم.** در سه سند نوشتم
> «حلقه‌ی `SCANNED → skipped`» و آن را باگ اولویت‌یک نامیدم. کارشناس
> سوم گفت احتمال (ج) «ممکن است ولی به‌تنهایی حلقه را توضیح نمی‌دهد» —
> و همین درست بود: حلقه از گرسنگی می‌آمد، نه از مسیریابی.

---

## ۴) آنچه هنوز حل نشده

### ۴-۱) ‎#68 و ‎#69 — همچنان گیر

```
23:10:39  STAGE #68 from=CHAIN_WAITING to=CHAIN_WAITING result=waiting
```

بیش از **۹۲ ساعت** در `CHAIN_WAITING` با `attempts=0`.

`AUTO_WORKER_WAIT_DEADLINE` هم صفر بار ثبت شده — دقیقاً همان نقصی که
در ارزیابی ۱۰.۱۲.۲۱ پیدا کردیم: مهلت از `updated_at` می‌سنجد و
`claim()` هر تیک آن را تازه می‌کند.

**راه‌حلش آماده است** (`waiting_since` + CAS) و منتظر اجراست.

### ۴-۲) `BUTTON_FOUND` → قدم بعدی

سه Session روی `BUTTON_FOUND` نشسته‌اند. قدم بعدی `Execute Action`
است که با تلگرام تماس می‌گیرد. در پنجره‌ی این دامپ گذاری از آن‌ها
دیده نمی‌شود — ولی این **لزوماً مشکل نیست**، چون:

- پنجره‌ی لاگ فقط ۴۰ ردیف است و ۵۴ رویداد داشته‌ایم
- `#70` که دیروز `WAITING_BOT` بود، امروز کلاً از صف خارج شده — یعنی
  به حالت پایانی رسیده

قضاوت درباره‌ی این مرحله را به دامپ بعدی موکول می‌کنم.

### ۴-۳) «کامل شد» هنوز ۰

طبیعی است — مسیر از `BUTTON_FOUND` تا `PUBLISHED` چند مرحله دارد.
اولین محصول کامل چند دور دیگر زمان می‌برد.

---

## ۵) Fiber — خبر خوب

```
Fiber / mmap    PASS    memory_limit=280M, peak=103954640 bytes (35.4%)
```

- مصرف اوج: **۹۹ مگابایت از ۲۸۰** — ۳۵٪
- **هیچ خطای تازه‌ی Fiber ثبت نشده**
- خطای `file_get_contents(memory.max)` هم اضافه نشده

وضعیت بدون تغییر: `ROOT_CAUSE_UNKNOWN`. ولی این مسئله عملاً **در
مسیر فعلی فعال نیست** و تصمیم درست بود که به اولویت چهار منتقلش
کردیم.

---

## ۶) نکته‌ی فنی — تفکیک دلیل در پنل نیامد

بخش «تفکیک دلیل» که در ۱۰.۱۲.۲۲ اضافه کردم در خروجی پنل دیده نمی‌شود.

دلیلش روشن است و **باگ نیست**: آن جدول فقط وقتی نمایش داده می‌شود که
داده‌ای داشته باشد (`if ( $gs_rows )`). امروز:
- `skipped` رخ نداده (چون Sessionها واقعاً جلو رفتند)
- `throttled` / `capacity_*` رخ نداده (فشاری نبوده)

پس جدول خالی مانده و مخفی شده — **دقیقاً طبق طراحی**.

---

## ۷) جمع‌بندی — پنج نسخه، این یکی جواب داد

| نسخه | اقدام | نتیجه |
|---|---|---|
| 10.12.18 | مهلت از ثابت قفل | ❌ بی‌اثر |
| 10.12.19 | عقب‌گرد نسبت به فاصله | 🔶 نسبی |
| 10.12.20 | `next_retry_at` per-state | ✅ مکانیزم اثبات شد |
| 10.12.21 | منبع درست فاصله + مهلت | ✅ صف باز شد |
| **10.12.22** | **رصدپذیری** | ✅ **حرکت اثبات شد** |

**تصمیم به جدا کردن رصدپذیری از اصلاح رفتاری درست بود.** اگر این دو
را با هم فرستاده بودیم، امروز نمی‌دانستیم کدام‌یک باعث حرکت شد.

---

## ۸) قدم بعدی پیشنهادی

**۱۰.۱۲.۲۳ — رفع ساعت انتظار (`waiting_since`)**

تنها مسئله‌ی رفتاری باقی‌مانده در مسیر اصلی: ‎#68 و ‎#69 که ۹۲ ساعت
است گیر کرده‌اند و هیچ سازوکاری آزادشان نمی‌کند.

طراحی از کارشناس دوم و سوم گرفته شده و مورد تأیید است:
- ستون `waiting_since DATETIME NULL`، بدون backfill
- ثبت فقط در **گذارِ ورود** به حالت انتظار
- مهلت با **CAS** و شرط مالکیت، نه `update()` عمومی
- `ERROR_BOT_TIMEOUT` از مهلت مستثنا (نکته‌ی کارشناس سوم)

ولی پیشنهاد می‌کنم **اول یک دامپ دیگر بگیریم** — با ۲۱ دور در روز،
دامپ بعدی نشان می‌دهد آیا `BUTTON_FOUND`ها هم جلو می‌روند یا آنجا
گیر می‌کنند. اگر گیر کردند، آن مسئله از ‎#68 مهم‌تر است.

---

## ۹) درس

**سه سند نوشتم و در هر سه، «حلقه‌ی `SCANNED → skipped`» را باگ
اولویت‌یک نامیدم. باگ نبود.**

مسیریابی از روز اول درست کار می‌کرد. آنچه می‌دیدم علامتِ گرسنگی صف
بود — همان مسئله‌ای که از ۱۰.۱۲.۱۸ دنبالش بودم — نه یک نقص جدا.

اگر به‌جای ساختن یک نسخه‌ی «فقط اندازه‌گیری»، مستقیم برای این
«باگ» patch نوشته بودم، کدی اضافه می‌کردم که مشکلی را حل نمی‌کرد و
احتمالاً مسیر سالم را خراب می‌کرد.

**اندازه‌گیری قبل از اصلاح، یک دور تأخیر داشت و یک اشتباه را جلوگیری
کرد.**
