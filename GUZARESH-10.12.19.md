# گزارش ۱۰.۱۲.۱۹ — تصحیح مهلت خواب: مضربی از فاصله‌ی تیک

> نسخه **10.12.19** · کد `f8183a2` · ZIP `995a45a`
> `golden-importer-10.12.19.zip` · ۱۳۲ فایل · sha256 `33d746e11359afd562f2ac94c4688d598ce679dfbe09b329f32d0f027084c010`

---

## ۱) خطای اصلی (Original Error)

بدون تغییر:

```
Fiber stack allocate failed: mmap failed: Cannot allocate memory (12)
```

**ROOT_CAUSE_UNKNOWN** — منتظر `/proc/self/maps` · `vm.max_map_count` · `RLIMIT_DATA`.

---

## ۲) داده‌های تشخیصی (Diagnostic Data)

### الف) اصلاح ۱۰.۱۲.۱۸ بی‌اثر بود — و لاگ میزبان آن را ثابت کرد

پس از نصب ۱۰.۱۲.۱۸ روی میزبان:

```
18:52:47  AUTO_WORKER_PICK: selected=1 ids=[68]
19:01:03  AUTO_WORKER_PICK: selected=1 ids=[68]
19:16:25  AUTO_WORKER_PICK: selected=1 ids=[68]
```

آمار امروز: `advanced=0` · `completed=0` · `waiting=34` · `ticks=47` · `eligible_queue=108`
و **هیچ خط `AUTO_WORKER_DEFER` در لاگ نبود.**

### ب) علت — خطای مقیاس در انتخاب اعداد

| مقدار | عدد | منبع |
|---|---|---|
| `worker_interval` (فاصله‌ی تیک) | **۳۰۰ ثانیه** | `class-gs-automation.php:49` |
| مهلت ۱۰.۱۲.۱۸ برای `CHAIN_WAITING` | **۶۰ ثانیه** | `WAITING_BACKOFF` |

اعداد ۶۰/۱۲۰/۳۰۰ از روی قفل‌های موتور گرفته شده بودند
(`POLL_LOCK_SECONDS=45`، `STEP_LOCK_SECONDS=90`). **قفل ربطی به زمان‌بندی ندارد.**
مهلت ۶۰ ثانیه‌ای مدت‌ها پیش از رسیدن تیک بعدی (۳۰۰ ثانیه) منقضی می‌شد،
پس `pick()` دوباره همان `#68` را برمی‌داشت.

> مکانیزم درست بود، **مقیاس غلط**. سازوکار `next_retry_at` کار می‌کرد ولی
> عددش آن‌قدر کوچک بود که تا نوبت بعدی اثری ازش نمانده بود.

### ج) شاهد تأییدی از خودِ دامپ

`eligible_sample` نشان می‌دهد چهار Session اول همگی در حالت انتظارند:

```
#68  CHAIN_WAITING  attempts=0
#69  CHAIN_WAITING  attempts=0
#70  WAITING_BOT    attempts=1
#78  WAITING_BOT    attempts=1
#79  SCANNED        attempts=0   ← اولین موردی که می‌تواند جلو برود
```

این دقیقاً همان سناریویی است که در بازبینی پیش‌بینی شده بود:
«اگر ‎#69 هم WAITING باشد، مشکل دوباره ظاهر می‌شود.»

### د) یک باگ مستقل در گزارش UI

هدر صفحه می‌نوشت «هر ۵ دقیقه، **۳ مورد**» ولی Worker یک مورد برمی‌داشت.

`stats()` مقدار `batch_size()` را گزارش می‌کرد (پیش‌فرض ۳)، در حالی که
آنچه واقعاً اجرا می‌شود این است:

```php
effective_batch_size() = min( batch_size, sessions_per_tick ) × Governor::factor()
                       = min( 3, 1 ) × 1  =  1
```

عدد نمایش‌داده‌شده هرگز با رفتار واقعی نمی‌خواند.

---

## ۳) اصلاح انجام‌شده

### الف) مهلت = ضریبی از فاصله‌ی واقعی تیک

```php
const WAITING_BACKOFF_FACTOR = array(
    'CHAIN_WAITING'     => 1.2,   // دست‌کم یک دور کامل رد می‌شود
    'WAITING_BOT'       => 2.0,   // ربات تلگرام وقت می‌خواهد
    'ERROR_BOT_TIMEOUT' => 4.0,   // یک‌بار مهلتش تمام شده
);
const WAITING_BACKOFF_FACTOR_DEFAULT = 1.5;
const WAITING_BACKOFF_MIN = 60;
```

با `worker_interval = 300`:

| حالت | مهلت مؤثر |
|---|---|
| `CHAIN_WAITING` | ۳۶۰ ثانیه |
| `WAITING_BOT` | ۶۰۰ ثانیه |
| `ERROR_BOT_TIMEOUT` | ۱۲۰۰ ثانیه |

اگر فاصله‌ی تیک را عوض کنید، **مهلت‌ها خودشان مقیاس می‌گیرند.**

### ب) کف سخت‌گیرانه

```php
$floor = max( self::WAITING_BACKOFF_MIN, $interval + 10 );
$delay = max( $floor, min( HOUR_IN_SECONDS, $delay ) );
```

حتی اگر کسی ضریب‌ها را با فیلتر خیلی کوچک کند، باز هم **دست‌کم یک تیک
کامل رد می‌شود**. همان اشتباه ۱۰.۱۲.۱۸ دیگر تکرارشدنی نیست.

### ج) لاگ قابل راستی‌آزمایی

```
AUTO_WORKER_DEFER session=#68 state=CHAIN_WAITING delay=360s
                  interval=300s factor=1.2 next=2026-09-08 19:22:25
                  reason=waiting_backoff
```

حالا در دامپ بعدی می‌توان **حساب را وارسی کرد**، نه اینکه از رفتار حدس زد.

### د) فیلتر با پارامتر جدید

```php
apply_filters( 'sti_gs_waiting_backoff', $base, $state, $session_id, $interval );
```

### ه) تصحیح عدد UI

`'batch' => effective_batch_size()` — عدد واقعی.
`'batch_max' => batch_size()` — عدد پیکربندی‌شده، برای مقایسه.

---

## ۴) آنچه طبق تصمیم شما تغییر نکرد

`sessions_per_tick = 1` و `max_active_sessions = 1` **دست‌نخورده** ماندند.
هر دو از قبل در فرم «اتوماسیون» قابل تنظیم‌اند
(`admin/views/golden-scan/automation-settings.php:13-14`)، بازه‌ی مجاز ۱ تا ۱۰.

اگر بعداً خواستید سرعت را بالا ببرید، همان‌جا قابل تغییر است و پچ فعلی
با هر مقداری کار می‌کند.

---

## ۵) داده‌های در دسترس نبودن (Unavailable Data)

| داده | وضعیت |
|---|---|
| `/proc/self/maps` (`maps_count`) | unavailable |
| `vm.max_map_count` | unavailable |
| `RLIMIT_DATA` | unavailable |

این سه فقط **هنگام وقوع خطای Fiber** ثبت می‌شوند. در دامپ فعلی، گروه‌بندی
خطاها هنوز **۱۶ مورد** `file_get_contents(memory.max)` نشان می‌دهد —
**همان عدد قبلی، بدون افزایش**. یعنی از نصب ۱۰.۱۲.۱۷ هیچ مورد تازه‌ای
اضافه نشده و آن اصلاح واقعاً مؤثر بوده؛ این ردیف‌ها تاریخی‌اند.

---

## ۶) اعتبارسنجی

| مجموعه | نتیجه |
|---|---|
| `test-10-12-19-starvation.py` (۴۲ ادعا) | ALL PASS |
| ۸ مجموعه‌ی دیگر | ALL PASS |

تست جدید علاوه بر اصلاح، **بازگشت به عقب را هم می‌بندد**:
`B5` تأیید می‌کند جدول اعداد ثابت ۱۰.۱۲.۱۸ حذف شده، و `D2a`/`D2b`/`D2c`
تأیید می‌کنند مهلت از `interval_seconds()` مشتق می‌شود.

ZIP مستقل باز و بررسی شد: نسخه ۱۰.۱۲.۱۹، `WAITING_BACKOFF_FACTOR` حاضر،
جدول قدیمی حذف‌شده، `safe_read` تشخیصی (۱۱ مورد) دست‌نخورده.

> ⚠️ در محیط توسعه PHP نصب نیست. اعتبارسنجی = بازرسی کد + تست ایستا.
> تأیید نهایی فقط روی میزبان واقعی.

---

## ۷) نشانه‌ی موفقیت پس از نصب

```
AUTO_WORKER_PICK:  selected=1 ids=[68]
AUTO_WORKER_DEFER  session=#68 delay=360s interval=300s factor=1.2 next=...
   ← تیک بعد (۵ دقیقه بعد)
AUTO_WORKER_PICK:  selected=1 ids=[69]   ← شناسه عوض شد
   ← تیک بعد
AUTO_WORKER_PICK:  selected=1 ids=[70]
```

**شرط موفقیت:** شناسه بین تیک‌ها تغییر کند. با `sessions_per_tick=1`
انتظار می‌رود پس از چهار دور (‎#68، ‎#69، ‎#70، ‎#78) به ‎#79 برسد که در
حالت `SCANNED` است و می‌تواند واقعاً جلو برود ⇒ `advanced` از صفر بالا می‌رود.

---

## ۸) تصمیم بعدی درباره‌ی Root Cause

| # | مسئله | وضعیت |
|---|---|---|
| ۱ | گرسنگی Worker | **PROVEN** — مکانیزم در ۱۰.۱۲.۱۸، مقیاس در ۱۰.۱۲.۱۹ |
| ۲ | ۸۲۵ رکورد orphan | شناسایی‌شده، **رفع نشده** |
| ۳ | `Fiber stack allocate failed` | **ROOT_CAUSE_UNKNOWN** |

برای مسئله‌ی ۳ همچنان بین **الف)** محدودیت محیط/میزبان و
**ب)** ناسازگاری معماری با runtime تصمیم نمی‌گیرم.

**«Root Cause مسئله‌ی Fiber هنوز اثبات نشده است.»**

---

## ۹) درس این دور

در ۱۰.۱۲.۱۸ سازوکار را درست تشخیص دادم ولی عدد را از **قفل** گرفتم
به‌جای **زمان‌بندی**. هر دو «ثانیه» بودند و همین شباهت واحد، خطا را
پنهان کرد.

قاعده‌ای که از این پس رعایت می‌شود: **هر مهلتی که قرار است بین دو اجرای
زمان‌بندی‌شده اثر بگذارد، باید نسبت به خودِ فاصله‌ی زمان‌بندی تعریف شود،
نه نسبت به هیچ عدد دیگری** — حتی اگر آن عدد هم‌واحد باشد.
