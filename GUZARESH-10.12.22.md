# گزارش ۱۰.۱۲.۲۲ — نسخه‌ی «فقط اندازه‌گیری»

> **نسخه:** 10.12.22 · **کد:** `dddb16a`
> `golden-importer-10.12.22.zip` · ۱۳۲ فایل
> sha256 `fe14b68239bac2a63eb7e84f06c5351bb4a871f67168124e8247fb496a597452`

---

## ۰) این نسخه چه کاری نمی‌کند

**هیچ رفتاری تغییر نکرده است.**

این نسخه یک Session را جلو نمی‌برد، یک حلقه را نمی‌شکند و یک باگ
رفتاری را رفع نمی‌کند. تنها کاری که می‌کند این است که **بگوید دقیقاً
کجا کار می‌ایستد**.

دلیلش صریح است: چهار نسخه‌ی متوالی (۱۰.۱۲.۱۸ تا ۲۱) منتشر شد که هر
کدام منطقی بودند، تست‌ها را رد می‌کردند، و روی میزبان کم‌اثر یا
بی‌اثر بودند. اگر این بار هم اصلاح رفتاری و اندازه‌گیری را با هم
بفرستیم و نتیجه نگیریم، **نمی‌دانیم کدام‌یک کار نکرده**.

پس: اول اندازه‌گیری، بعد اصلاح.

---

## ۱) شش تغییر

### ۱-۱) ★ `fallback_to_legacy()` حالا نوشتن خودش را بررسی می‌کند

**مهم‌ترین تغییر این نسخه.**

این تابع (`class-gs-chain-engine.php`) تا امروز `chain_mode` را روی
`legacy` می‌نوشت و **بدون هیچ بررسی‌ای برمی‌گشت**:

```php
STI_GS_Session::update( $session_id, array(
    'chain_mode' => STI_GS_Node::MODE_LEGACY, ...
) );
STI_GS_Event::log( ... );   // ← تمام. هیچ بررسی‌ای نبود.
```

اگر آن نوشتن اثر نمی‌کرد، Session در دور بعد دوباره به Chain Init
می‌رسید و همان `no_progress` را برمی‌گرداند — **یک حلقه‌ی بی‌پایانِ
کاملاً خاموش**.

حالا سه چیز ثبت می‌شود:

| قلم | معنا |
|---|---|
| `before` | مقدار `chain_mode` **پیش از** نوشتن |
| `affected` | تعداد ردیفی که واقعاً تغییر کرد |
| `after` | **read-back** — مقداری که واقعاً در دیتابیس نشسته |

و خروجی لاگ:

```
AUTO_WORKER_ROUTE session=#79 from=NULL to=legacy
                  affected=1 readback=legacy result=PERSISTED reason=...
```

**این تک‌خط، تشخیص را قطعی می‌کند:**

- اگر `result=PERSISTED` باشد ⇒ نوشتن سالم است، مشکل در مسیریابی
  دور بعد است
- اگر `result=LOST` باشد ⇒ نوشتن اثر نمی‌کند و باید دنبال
  بازنویسی‌کننده بگردیم
- اگر این خط **اصلاً نیاید** ⇒ `fallback_to_legacy()` هرگز صدا زده
  نمی‌شود و مسیر دیگری فعال است

### ۱-۲) هر پنج مسیر `no_progress` حالا دلیل دارند

در `Chain_Engine::init()` پنج نقطه `no_progress` برمی‌گرداندند بدون
اینکه بگویند **چرا**. حالا هر کدام `outcome` و `reason` دارند:

| دلیل | معنا |
|---|---|
| `state_not_scanned` | حالت Session دیگر `SCANNED` نیست |
| `global_mode_legacy` | تنظیم سراسری روی legacy است |
| `message_not_found` | پیام مبدأ در `sti_gs_messages` نیست |
| `raw_json_invalid` | محتوای پیام قابل decode نیست |
| `node_not_executable:<نوع>` | گره دکمه‌ی قابل اجرا ندارد |

آخری **نوع گره** را هم می‌گوید — یعنی اگر احتمال (ج) درست باشد
(هر ۱۰۷ ردیف واقعاً دکمه ندارند)، در همین یک دامپ معلوم می‌شود.

### ۱-۳) شمارنده دیگر چیزی را بی‌صدا دور نمی‌ریزد

باگی که در ۱۰.۱۲.۲۱ پیدا کردیم:

```php
$report = array( 'advanced'=>0, 'waiting'=>0, 'failed'=>0, 'completed'=>0 );
//                  ↑ کلید 'skipped' وجود نداشت
...
if ( isset( $report[ $outcome ] ) ) { $report[ $outcome ]++; }
//  ↑ پس 'skipped' ساکت حذف می‌شد
```

شرط برعکس شد: هر نتیجه‌ی ناشناخته حالا **ساخته و شمرده** می‌شود.

### ۱-۴) سه دلیل که با هم قاطی بودند، جدا شدند

`waiting` تا امروز سه چیز کاملاً متفاوت را یکجا می‌شمرد: فشار
Governor، سقف دانلود، سقف ساخت محصول. حالا هر کدام شمارنده‌ی خودش را
دارد.

⚠️ خودِ `waiting` **دست‌نخورده** بالا می‌رود تا عدد فعلی پنل عوض نشود.

### ۱-۵) آمار روزانه همه‌چیز را نگه می‌دارد

`record()` فقط چهار شمارنده‌ی ثابت را ذخیره می‌کرد؛ بقیه در پایان هر
تیک از بین می‌رفتند. حالا هر کلیدی ماندگار می‌شود.

### ۱-۶) پنل ابزار تشخیص خطوط جدید را نشان می‌دهد

فیلتر `LIKE` در `class-gs-chain-audit.php` حالا `AUTO_WORKER_ROUTE` و
`AUTO_WORKER_WAIT_DEADLINE` را هم می‌پذیرد.

> این درسِ ۱۰.۱۲.۱۹ است: آن بار یک اصلاح درست منتشر کردیم و سه روز
> فکر کردیم بی‌اثر بوده — چون پنل خط لاگش را نشان نمی‌داد. **کانال
> شواهد باید همراه خودِ شواهد اضافه شود.**

---

## ۲) پنل چه چیز تازه‌ای نشان می‌دهد

زیر «گزارش امروز» یک بخش تازه اضافه شده:

```
تفکیک دلیل (۱۰.۱۲.۲۲)
  بدون پیشرفت (رد شد)              12
  مسیر به قدیمی تغییر کرد           8
  ⚠️ ثبت تغییر مسیر ناموفق          4
```

اگر داده‌ای نباشد اصلاً نمایش داده نمی‌شود.

---

## ۳) آنچه دست نخورد

`const WAITING` · `const TERMINAL` · سقف تلاش · `backlog_limit` ·
`attempts` · گیت `max_active` · `sessions_per_tick` · Fiber · MTProto ·
منطق صف · ماشین حالت Session.

**هیچ Sessionای حذف یا ویرایش نشد.**

مشخصاً: `SCANNED` به `WAITING` **اضافه نشد** — طبق نظر کارشناس سوم،
`SCANNED` یک وضعیت قابل‌پردازش است، نه انتظار.

---

## ۴) اعتبارسنجی

| مجموعه | نتیجه |
|---|---|
| `test-10-12-22-observability.py` | **۱۱۰ ادعا — ALL PASS** |
| ۸ مجموعه‌ی دیگر | ALL PASS |

بلوک‌های تازه: `R1`-`R10` (read-back و ترتیبش)، `E1`-`E6` (دلیل‌های
نوع‌دار)، `D1`-`D5` (اتصال به Worker)، `C10`-`C18` (شمارنده‌ها)،
`P1`-`P5` (رسیدن به پنل)، `N1`-`N7` (بی‌تغییر ماندن رفتار).

سه تست خودم غلط بودند و اصلاحشان کردم — نه کد را:

- یک تست به همان خطی لنگر داشت که عمداً عوضش کردم
- برچسب‌های `C3`/`C4` تکراری شده بود
- یک پنجره‌ی ۹۰۰۰ کاراکتری از `init()` سرریز می‌کرد و متد بعدی را هم
  می‌بلعید (طول واقعی `init()` = ۵۳۵۴)

و یک تست را حذف کردم چون **از پایه غلط بود**: شمارش خام پرانتز در
`class-gs-chain-engine.php` معنا ندارد، چون داخل کامنت‌های فارسی
پرانتز هست. آن فایل **پیش از دست زدن من هم** ۲۱ تا اختلاف داشت.

> ⚠️ محیط توسعه PHP ندارد. اعتبارسنجی = بازرسی کد + تست ایستا.
> **این‌ها اثبات زمان اجرا نیستند.**

---

## ۵) بعد از نصب — دقیقاً دنبال چه بگردیم

یکی دو ساعت بعد از نصب، ابزار تشخیص را باز کنید و **دنبال خط
`AUTO_WORKER_ROUTE` بگردید**. سه حالت ممکن است:

### حالت الف — خط می‌آید با `result=PERSISTED`
```
AUTO_WORKER_ROUTE session=#79 ... readback=legacy result=PERSISTED
```
⇒ نوشتن سالم است. پس مشکل در **مسیریابی دور بعد** است. یعنی با وجود
`chain_mode=legacy` باز Chain Init صدا زده می‌شود.

### حالت ب — خط می‌آید با `result=LOST`
```
AUTO_WORKER_ROUTE session=#79 ... affected=0 readback=NULL result=LOST
```
⇒ نوشتن اثر نمی‌کند. دنبال بازنویسی‌کننده یا شرط ناموفق می‌گردیم.

### حالت ج — خط اصلاً نمی‌آید
⇒ `fallback_to_legacy()` هرگز صدا زده نمی‌شود. آن‌گاه `reason` در خط
`AUTO_WORKER_STAGE` می‌گوید کدام مسیر فعال است.

**و در هر سه حالت**، خط `STAGE` حالا دلیل را می‌گوید:
```
AUTO_WORKER_STAGE session=#79 from=SCANNED to=SCANNED result=skipped
                  outcome=route_changed reason=raw_json_invalid
                  route=NULL→legacy/affected=1/PERSISTED
```

اگر `reason=node_not_executable:...` برای همه‌ی Sessionها تکرار شود،
یعنی **احتمال (ج) درست است** و مشکل در کیفیت اسکن است، نه Worker.

---

## ۶) چرا این نسخه ارزش یک دور تأخیر را دارد

سه کارشناس مستقل روی این پرونده کار کردند. هر سه به یک نتیجه رسیدند:
**بدون این اعداد، هر اصلاحی حدس است.**

کارشناس سوم پیشنهاد داد رصدپذیری و اصلاح رفتاری هم‌زمان P0 باشند. ما
مخالفت کردیم و جدا کردیم — دقیقاً به‌خاطر تجربه‌ی چهار نسخه‌ی قبل.

یک دور تأخیر دارد. ولی نتیجه‌اش **داده است، نه حدس**.

---

## ۷) وضعیت‌ها

```
SCANNED_LOOP  = ریشه به ۳ مسیر محدود شد — این نسخه بین آن‌ها داوری می‌کند
FIBER_ENOMEM  = ROOT_CAUSE_UNKNOWN (بدون تغییر)
ORPHANS       = ۸۲۵ رکورد، دست‌نخورده، بی‌ضرر (INNER JOIN حذفشان می‌کند)
WAITING_CLOCK = طراحی پذیرفته، اجرا موکول به ۱۰.۱۲.۲۳
```

**قدم بعد:** نصب ۱۰.۱۲.۲۲ ⇒ یکی دو ساعت صبر ⇒ خروجی ابزار تشخیص را
بفرستید ⇒ ۱۰.۱۲.۲۳ اصلاح رفتاری بر پایه‌ی داده‌ی واقعی.

---

## ۸) درس این دور

**یک تابع که نوشتن خودش را بررسی نمی‌کند، می‌تواند سه روز دروغ
بگوید.**

`fallback_to_legacy()` هر بار «موفق» گزارش می‌داد چون اصلاً چیزی را
بررسی نمی‌کرد. اضافه کردن یک `SELECT` بعد از `UPDATE` — سه خط کد —
چیزی است که باید از روز اول می‌بود.

الگوی خانوادگی خطای ما حالا سه عضو دارد:

1. منبع زمانی که چرخه‌ی اندازه‌گیری خودش بازنویسی‌اش می‌کند
2. نوشتن از داخل تابعی که طراحی‌اش صریحاً بدون قفل است
3. **نوشتن بدون خواندنِ نتیجه**

هر سه یک ریشه دارند: **فرض کردن به‌جای تأیید کردن.**
