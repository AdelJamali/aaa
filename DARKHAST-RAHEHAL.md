# درخواست راه‌حل فنی — Golden Importer 10.12.21

> **این سند ادامه‌ی `BRIEF-KARSHENAS.md` است.** آن سند درخواست
> ریشه‌یابی بود؛ این یکی درخواست **راه‌حل قابل اجرا**.
>
> گزارش قبلی شما با `ROOT CAUSE NOT YET PROVEN` بسته شد و ما آن نتیجه
> را می‌پذیریم. اما مالک سامانه نمی‌تواند بی‌نهایت منتظر بماند: خط
> لوله چند هفته است متوقف است.

---

## ۰) قبل از هر چیز — تشکر و تأیید

هر هشت ادعای فنی گزارش شما را روی کد و روی خودِ PHAR بازبینی کردیم.
**هر هشت مورد تأیید شد.** جزئیات در `ARZYABI-GOZARESH-KARSHENAS.md`.

سه موردش برای ما مهم بود:

1. **هشدار `testFibers()`** — بدنه‌ی تابع را از PHAR استخراج کردیم:
   `ini_set('memory_limit', -1)` و حلقه‌ی صد هزار Fiber تا لحظه‌ی
   شکست. توصیه‌ی ما در بند ۹-۳ می‌توانست میزبان را از کار بیندازد.
   **حذف شد.** جایگزین: فقط `getMaps()` و `getMaxMaps()`.
2. **تناقض `memory_limit`** که ماه‌ها «مجهول» علامت خورده بود، با
   راهنمایی شما در کد خودمان پیدا شد: `class-sti-mtproto.php` خطوط
   ۲۴۷ و ۶۵۷ اجرا می‌کنند `@ini_set('memory_limit','512M')`.
3. **`selected=6` را بیش از حقش تفسیر کرده بودیم.** شما درست گفتید:
   شش انتخاب، صفر پیشرفت.

سند بریفینگ اصلاح شد (بندهای ۱-۶، ۲-۴، ۹-۳، ۹-۵).

---

## ۱) آنچه از شما می‌خواهیم

**راه‌حل فنی، در سه سطح اولویت.** برای هر کدام: تشخیص شما از درستی
مسیر، طراحی دقیق، و ریسک‌هایی که باید قبل از انتشار آزموده شود.

نکته‌ی مهم درباره‌ی ترتیب: **مسئله‌ی Fiber دیگر اولویت اول ما نیست.**
شواهد سه دامپ متوالی نشان می‌دهد خط لوله به دلیل دیگری متوقف است و
Fiber ممکن است اصلاً در مسیر فعلی درگیر نشود. پس:

| اولویت | مسئله | چرا این ترتیب |
|---|---|---|
| **۱** | `SCANNED → skipped` (بخش ۲) | تنها چیزی که مستقیماً «مرحله جلو رفت = ۰» را توضیح می‌دهد |
| **۲** | ساعت انتظار / `waiting_since` (بخش ۳) | طراحی‌اش را خودتان دادید، فقط جزئیات اجرا مانده |
| **۳** | ایمن‌سازی ابزار تشخیص (بخش ۴) | پیش‌نیاز جمع‌آوری داده‌ی Fiber |
| **۴** | Fiber/mmap (بخش ۵) | تا نداشتن سه عدد، فقط طرح جمع‌آوری داده |

اگر با این ترتیب موافق نیستید، **صریح بگویید** — دلیلتان برای ما
ارزشمندتر از موافقت است.

---

## ۲) ★ اولویت یک — چرخه‌ی بی‌پایان `SCANNED → skipped`

این همان سؤالی است که در ارزیابی باز گذاشتیم و حالا شواهد بیشتری
داریم.

### ۲-۱) مشاهده روی میزبان

دامپ `2026-09-11 06:11:35`:

```
06:08:08  AUTO_WORKER_PICK: selected=6 ids=[68,69,70,78,79,80]
06:08:27  AUTO_WORKER_STAGE session=#80 from=SCANNED to=SCANNED result=skipped
06:08:27  AUTO_WORKER_STAGE session=#79 from=SCANNED to=SCANNED result=skipped
06:08:27  AUTO_WORKER_STAGE session=#68 from=CHAIN_WAITING to=CHAIN_WAITING result=waiting
```

شمارنده‌ی «مرحله جلو رفت» = **۰**. سه روز پیاپی.

### ۲-۲) مسیر کد — با شماره‌خط

مسیریابی حالت `SCANNED`
(`class-gs-session-ajax.php:392-393`):
```php
'SCANNED' => array( 'Resolve Button', array( 'STI_GS_Button_Resolver', 'resolve' ) ),
```

اما docblock بالای همین متد (خطوط ۳۸۲-۳۸۶) می‌گوید تصمیم به
`chain_mode` خودِ Session بستگی دارد: `auto`/`chain` ⇒ Chain Init،
`legacy` ⇒ Resolver قدیمی.

در `class-gs-chain-engine.php::init()` **پنج مسیر** وجود دارد که
`no_progress => true` برمی‌گردانند:

| خط | شرط | اقدام جانبی |
|---|---|---|
| ۱۲۲ | `state !== 'SCANNED'` | — |
| ۱۲۸ | `mode === MODE_LEGACY` | — |
| ۱۳۴ | پیام مبدأ در `sti_gs_messages` نیست | `fallback_to_legacy()` |
| ۱۴۰ | `raw_json` قابل decode نیست | `fallback_to_legacy()` |
| ۱۵۴ | گره قابل اجرا نیست (`ASSET`/`UNKNOWN`/متن ساده) | `fallback_to_legacy()` |

و `fallback_to_legacy()` (خط ۱۳۷۸-۱۳۸۶) فقط این کار را می‌کند:
```php
STI_GS_Session::update( $session_id, array(
    'chain_mode'   => STI_GS_Node::MODE_LEGACY,
    'stage'        => 'chain_engine',
    'error_reason' => null,
) );
```

**State عوض نمی‌شود. `SCANNED` می‌ماند.**

سپس در `class-gs-auto-worker.php:989-990`:
```php
if ( is_array( $result ) && ! empty( $result['no_progress'] ) ) {
    $outcome = ! empty( $result['waiting'] ) ? 'waiting' : 'skipped';
}
```

`skipped` یعنی: `attempts` بالا نمی‌رود (خط ۹۹۹ که
`handle_failure()` را صدا می‌زند اجرا نمی‌شود)، state ثابت می‌ماند،
و `next_retry_at` هم فقط از `defer_waiting()` می‌آید که یک تأخیر
موقت است.

### ۲-۳) فرضیه‌ی ما (و شک ما به آن)

Session به `chain_mode = legacy` منتقل می‌شود، ولی **هیچ‌کس آن مسیر
legacy را اجرا نمی‌کند** — چون در دور بعد باز `init()` صدا زده
می‌شود و همان‌جا در شرط خط ۱۲۸ (`MODE_LEGACY`) گیر می‌کند و دوباره
`skipped` برمی‌گرداند.

**اما مطمئن نیستیم.** دو احتمال دیگر:

- **الف)** مسیریابی درست است و `Button_Resolver::resolve()` واقعاً
  صدا زده می‌شود، ولی خودش `skipped` برمی‌گرداند
  (`class-gs-button-resolver.php:57-62`، شرط `PAST_STATES`).
- **ب)** هر ۱۰۷ ردیف صف واقعاً «گره غیرقابل اجرا» دارند — یعنی
  محتوایشان اصلاً دکمه ندارد و رفتار فعلی **درست** است و مشکل جای
  دیگری است (کیفیت اسکن، فیلتر پروفایل).

### ۲-۴) سؤال‌های مشخص

1. کدام‌یک از سه احتمال درست است؟ اگر برای پاسخ به شاهد نیاز دارید،
   **دقیقاً بگویید کدام لاگ یا کدام فیلد** — ما از پنل می‌گیریم.
2. اگر فرضیه‌ی اصلی درست است: تعویض `chain_mode` به `legacy` باید
   Session را به کدام مسیر بفرستد؟ و چطور تضمین کنیم دوباره به
   `init()` برنگردد؟
3. **آیا `skipped` باید بی‌نهایت تکرارشدنی بماند؟** حالت انتظار حالا
   مهلت دارد ولی `skipped` ندارد. اگر مهلت لازم است، شمارنده‌اش کجا
   ذخیره شود؟ (`attempts` را عمداً دست نمی‌زنیم — طراحی فعلی
   `skipped` را خطا نمی‌شمارد و دلیلش موجه است.)
4. `SCANNED` در `const WAITING` نیست
   (`class-gs-auto-worker.php:66`). آیا باید اضافه شود، یا سازوکار
   جداگانه بهتر است؟

---

## ۳) اولویت دو — `waiting_since`

طراحی را در گزارش قبلی خودتان دادید و ما پذیرفتیم. جزئیات اجرا مانده.

### ۳-۱) آنچه تأیید شده

- بررسی مهلت از `updated_at` است
  (`class-gs-auto-worker.php:853`).
- `claim()` (`class-gs-session.php:167`) و `update()` (خط ۱۵۵) هر دو
  `updated_at` را تازه می‌کنند.
- `release()` (خط ۱۸۸) آن را دست نمی‌زند — این را شما تصحیح کردید.
- نوشتنِ گذار از snapshot پیش از claim انجام می‌شود
  (خطوط ۸۵۲-۸۶۳) — ایراد شما وارد است.

### ۳-۲) سؤال‌های مشخص

1. **Sessionهای موجود** (‎#68 و ‎#69 بیش از ۸۰ ساعت): شما نوشتید
   «نسبت‌دادن ساختگی `created_at` ممنوع، سیاست صریح لازم است».
   کدام سیاست را پیشنهاد می‌کنید — `NULL` و شروع شمارش از لحظه‌ی
   ارتقا، یا بازیابی از `sti_gs_session_events`؟
2. **حفظ در poll:** دقیقاً کدام گذارها باید ساعت را نگه دارند و
   کدام‌ها ریست کنند؟ مثلاً `CHAIN_WAITING → CHAIN_WAITING` بعد از
   یک poll ناموفق: نگه‌داشتن یا ریست؟
3. **نوشتن اتمیک:** پیشنهاد شما «زیر claim معتبر یا شرط اتمیک
   بازبینی‌شده» بود. کدام را ترجیح می‌دهید؟ اگر شرط اتمیک،
   `UPDATE ... WHERE id=%d AND state=%s AND waiting_since=%s` کافی
   است؟
4. **مبنای زمانی:** میزبان `current_time('mysql')` وردپرس را
   استفاده می‌کند (timezone سایت) ولی `time()` در PHP UTC است. کد
   فعلی هر دو را قاطی می‌کند. پیشنهاد شما برای یکنواخت‌سازی بدون
   شکستن داده‌ی موجود؟
5. **migration:** ستون nullable با `dbDelta`. آیا نیاز به index
   هست؟ جدول حدود ۵۸۰۰ ردیف دارد.

---

## ۴) اولویت سه — ایمن‌سازی ابزار تشخیص

سه ایراد شما، هر سه تأیید شده. برای هر کدام یک سؤال داریم.

### ۴-۱) `@fopen` در `proc_kv()`

`class-gs-env-diag.php:406` — و از خطوط ۹۸، ۱۰۲، ۱۱۰ صدا زده می‌شود،
یعنی `/proc/meminfo`، `/proc/self/status`، `/proc/self/limits`، همگی
**قبل** از اولین `safe_read()`.

قانون این را خودمان در ۱۰.۱۲.۱۷ نوشتیم و در همان فایل نقض کردیم.

**سؤال:** `safe_read()` کل فایل را می‌خواند؛ `proc_kv()` خط‌به‌خط
می‌خواند تا حافظه مصرف نکند. برای `/proc/meminfo` (حدود ۱.۵ کیلوبایت)
خواندن کامل بی‌خطر است، ولی `/proc/self/maps` می‌تواند مگابایتی باشد.
پیشنهاد شما: یک `safe_read_lines()` جداگانه، یا `safe_read()` با سقف
اندازه؟

### ۴-۲) cgroup v2 از مسیر ریشه

خطوط ۱۲۸-۱۳۰ از `/sys/fs/cgroup/memory.max` می‌خوانند، نه cgroup
واقعی فرآیند. برای v1 این کار **انجام شده** (خط ۱۴۲ به بعد) ولی برای
v2 نه.

**سؤال:** ترتیب درست خواندن `/proc/self/cgroup` و
`/proc/self/mountinfo` برای ساخت مسیر v2 چیست؟ و اگر فرآیند در یک
cgroup تودرتو باشد، سقف مؤثر را از کدام سطح باید خواند — خودش یا
کوچک‌ترین سقف در زنجیره‌ی والدها؟

### ۴-۳) برش ۴۰۰ / ۱۵۰۰

`class-sti-mtproto.php` خطوط ۷۷۰، ۷۷۸، ۷۸۰:
```php
$ctx['error_message_raw'] = mb_substr( (string) $last_error, 0, 400 );
$ctx['exception_message'] = mb_substr( (string) $last_error, 0, 400 );
$ctx['exception_trace']   = mb_substr( (string) $last_exception_trace, 0, 1500 );
```

ما در همه‌ی گزارش‌ها نوشتیم «پیام بایت‌به‌بایت حفظ می‌شود» — که برای
این پیام خاص (۶۲ نویسه) درست است ولی تضمین معماری نیست.

**سؤال:** برداشتن کامل سقف ریسک پر شدن جدول لاگ دارد. سقف امن
پیشنهادی شما چقدر است؟ یا بهتر است پیام بدون سقف و trace با سقف
بماند؟

---

## ۵) اولویت چهار — Fiber/mmap

اینجا **راه‌حل نمی‌خواهیم**، چون هنوز داده نداریم. آنچه می‌خواهیم:

### ۵-۱) طرح جمع‌آوری داده‌ی امن

با توجه به اینکه `testFibers()` از دستور خارج شد، و با توجه به اینکه
`OOM_DIAG` فقط **در لحظه‌ی شکست** نوشته می‌شود (پس تا خطا تکرار نشود
چیزی ثبت نمی‌شود):

1. آیا خواندن `getMaps()` در **اجرای عادی** (نه لحظه‌ی خطا) و ثبت
   دوره‌ای آن به‌عنوان baseline ارزش دارد؟ اگر بله، هر چند وقت یک‌بار
   و کجا ذخیره شود؟
2. برای اینکه بتوانیم روند `maps_count` را در طول عمر یک فرآیند
   `lsphp` پایا ببینیم، چه چیزی باید هم‌زمان ثبت شود؟ (PID،
   `getmypid()`، زمان شروع فرآیند؟)
3. آیا راهی هست که **بدون تحریک خطا** بفهمیم سقف مؤثر کدام است؟

### ۵-۲) اگر خطا دیگر تکرار نشود

شما نوشتید baseline مفید است ولی علت تاریخی را قطعی نمی‌کند. سؤال
عملی: **چه مدت بدون تکرار خطا کافی است تا این مسئله را به «معلق»
منتقل کنیم** و تمرکز را روی خط لوله بگذاریم؟

### ۵-۳) اگر تکرار شد

فهرست دقیق چیزهایی که باید در همان لحظه ثبت شود — تا مجبور نشویم
منتظر تکرار بعدی بمانیم. (فهرست فعلی `OOM_DIAG` در بند ۸ سند
بریفینگ است؛ اگر چیزی کم دارد بگویید.)

---

## ۶) محدودیت‌ها — بدون تغییر

همان بند ۶ سند بریفینگ. مهم‌ترین‌ها:

- ❌ حذف Session · پاک کردن backlog · تغییر مخرب DB
- ❌ بازطراحی Worker · تغییر MTProto یا Fiber
- ❌ تغییر سقف تلاش · بالا بردن `backlog_limit`
- ❌ فعال کردن Watcher به‌عنوان درمان اصلی
- ❌ هیچ warning/notice/fatal در UI یا JSON

و یک قید که از تجربه‌ی خودمان اضافه می‌کنیم:

> ❌ **هیچ راه‌حلی که «صف را خالی کن و از نو شروع کن» باشد.** چهار
> بار وسوسه شدیم و هر بار مسئله را پنهان می‌کرد نه حل.

---

## ۷) شکل پاسخ درخواستی

برای هر مسئله‌ای که پاسخ می‌دهید:

### `تشخیص`
آیا تحلیل ما درست است؟ اگر نه، کجا اشتباه کردیم — با شماره‌خط.

### `طراحی`
تغییر دقیق: کدام فایل، کدام تابع، چه رفتاری. شبه‌کد کافی است، کد
کامل لازم نیست.

### `ریسک‌ها`
چه چیزی می‌تواند خراب شود. مخصوصاً: رقابت دو اجرای هم‌زمان،
Sessionهای قدیمی، اختلاف timezone، و رفتار در نصب تازه.

### `آزمون`
قبل از انتشار چه چیزی باید تأیید شود. توجه: **محیط ما PHP ندارد** —
اعتبارسنجی ما فقط بازرسی کد و تست ایستای متنی است. اگر آزمونی نیاز
به اجرای واقعی دارد، بگویید تا از مالک بخواهیم روی میزبان تأیید کند.

### `اولویت`
اگر با ترتیب ما موافق نیستید، ترتیب خودتان را بدهید.

---

## ۸) یک درخواست صادقانه

در چهار نسخه‌ی متوالی (۱۰.۱۲.۱۸ تا ۲۱) ما اصلاحاتی منتشر کردیم که
تست‌ها را رد می‌کردند، منطقی به‌نظر می‌رسیدند، و روی میزبان **کم
اثر یا بی‌اثر** بودند. الگوی تکرارشونده‌ی خطای ما:

> انتخاب یک منبع زمانی که خودِ چرخه‌ی اندازه‌گیری آن را بازنویسی
> می‌کند. سه بار: ثابت قفل، دوره‌ی تنظیم‌شده، و `updated_at`.

اگر در طراحی پیشنهادی شما هم چنین وابستگی‌ای می‌بینید،
**صریح علامت بزنید**. ما ترجیح می‌دهیم یک راه‌حل کندتر ولی
قابل‌اندازه‌گیری داشته باشیم تا یک راه‌حل زیبا که سه روز بعد معلوم
شود کار نکرده.

همچنین: اگر به این نتیجه رسیدید که بخشی از معماری فعلی **قابل
اصلاح تدریجی نیست**، همان را بنویسید. شنیدنش سخت است ولی از چهار
نسخه‌ی بی‌اثر دیگر بهتر است.
