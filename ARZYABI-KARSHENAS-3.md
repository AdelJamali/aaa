# ارزیابی گزارش کارشناس سوم

> تاریخ: ۱۱ سپتامبر ۲۰۲۶ · مبنا: ۱۰.۱۲.۲۱ (`c85d3bb`)
> روش: هر ارجاع خطی روی checkout واقعی بازبینی شد. هیچ کدی تغییر نکرد.

**نتیجه: گزارش دقیق است. یک ارجاع خطی داشت که ما نداشتیم و همان
ارجاع باعث شد فرضیه‌ی (ب) را رد کنیم و مسیر (الف) را به یک نقطه‌ی
مشخص محدود کنیم.**

---

## ۱) ★ ارجاعی که ما ندیده بودیم — و نتیجه‌اش

کارشناس به `class-gs-session-ajax.php:429-457` ارجاع داد. ما در
بریفینگ فقط خطوط ۳۸۲-۳۹۳ را نقل کرده بودیم.

خطوط ۴۴۷-۴۵۵ یک invariant صریح دارند:

```php
/**
 * D6 — invariant قطعی:
 *   SCANNED + chain_mode ∈ {auto, chain} → Chain Init
 *   SCANNED + chain_mode = NULL          → Legacy Resolver
 * Global UI mode هرگز معنای Session قدیمی NULL را تغییر نمی‌دهد.
 */
if ( $is_chain_session ) {
    $map['SCANNED'] = array( 'Chain Init', array( 'STI_GS_Chain_Engine', 'init' ) );
}
```

و خط ۴۴۵:
```php
$is_chain_session = in_array( $session_chain,
    array( STI_GS_Node::MODE_AUTO, STI_GS_Node::MODE_CHAIN ), true );
```

پس مسیریابی **درست طراحی شده**: اگر `chain_mode` روی `legacy` ذخیره
شود، دور بعد `$is_chain_session` غلط می‌شود و `SCANNED` به
Button Resolver می‌رود، نه Chain Init.

---

## ۲) ✅ فرضیه‌ی (ب) قطعاً رد شد — با شاهد جدید

کارشناس گفت «بعید است». ما شاهد قطعی‌تری پیدا کردیم:

```
grep -c "no_progress" class-gs-button-resolver.php  ⇒  0
```

**`Button_Resolver::resolve()` هرگز `no_progress` برنمی‌گرداند.**
تنها `skipped` صریحش در خط ۶۲ است و آن هم بدون `no_progress`:
```php
return array( 'state' => $session['state'], 'skipped' => true,
              'method' => ..., 'confidence' => ... );
```

حالا مسیر تولید `skipped` در Worker را دنبال کنید
(`class-gs-auto-worker.php:989`):
```php
if ( is_array( $result ) && ! empty( $result['no_progress'] ) ) {
    $outcome = ! empty( $result['waiting'] ) ? 'waiting' : 'skipped';
}
```

شرط ورود **`no_progress`** است، نه `skipped`.

و `grep -rn "no_progress" *.php` نشان می‌دهد تنها تولیدکننده‌اش
`class-gs-chain-engine.php` است (۱۰ نقطه: خطوط ۱۲۲، ۱۲۸، ۱۳۴، ۱۴۰،
۱۵۴، ۲۱۹، ۲۲۳، ۲۲۶، ۲۸۹، ۲۹۳).

> **⇒ نتیجه‌ی قطعی: لاگ `result=skipped` برای ‎#79 و ‎#80 فقط و فقط
> می‌تواند از `Chain_Engine` آمده باشد. یعنی آن‌ها همچنان به
> Chain Init مسیریابی می‌شوند — که طبق invariant D6 یعنی
> `chain_mode` آن‌ها هنوز `auto` یا `chain` است، نه `legacy`.**

این شاهد در هیچ‌کدام از سه گزارش قبلی نبود.

---

## ۳) پس فرضیه‌ی (الف) درست است — ولی کدام شاخه؟

`fallback_to_legacy()` یا اجرا نمی‌شود، یا اجرا می‌شود و اثرش
نمی‌ماند. دو شاخه را بررسی کردیم:

### ۳-۱) آیا `chain_mode` در allow-list هست؟

بله. `class-gs-session.php:112-126` — بررسی کردیم، `chain_mode` در
فهرست مجاز `update()` هست. پس **بی‌صدا دور ریخته نمی‌شود.**

(این نکته مهم است چون همان فایل یک کامنت طولانی دارد درباره‌ی
`scheduled_at` که دقیقاً به همین دلیل بی‌صدا از دست می‌رفت.)

### ۳-۲) ⭐ شاخه‌ی محتمل‌تر — گیت خط ۱۲۸ اصلاً به fallback نمی‌رسد

`class-gs-chain-engine.php:124-129`:
```php
$mode = self::mode();          // ← گلوبال، نه Session
if ( STI_GS_Node::MODE_LEGACY === $mode ) {
    return array( 'state' => 'SCANNED', 'skipped' => true,
                  'no_progress' => true, 'mode' => $mode );
}
```

**`self::mode()` تنظیمات سراسری را می‌خواند، نه `chain_mode` این
Session را.** و این `return` **قبل از** هر `fallback_to_legacy()`
است — یعنی اگر این شاخه فعال شود، `chain_mode` هرگز نوشته نمی‌شود.

اما اینجا یک تناقض هست که باید توجه کنید:

- اگر `mode()` سراسری = `legacy` باشد، آن‌گاه در
  `session-ajax.php:430` شرط `MODE_LEGACY !== $global_mode` غلط
  می‌شود و کل بلوک زنجیره اجرا نمی‌شود ⇒ `SCANNED` به
  Button Resolver می‌رود ⇒ `Chain_Engine::init()` **اصلاً صدا زده
  نمی‌شود** ⇒ `no_progress` تولید نمی‌شود.

⇒ **پس خط ۱۲۸ نمی‌تواند منبع لاگ فعلی باشد.** با حذف آن، سه مسیر
باقی می‌ماند: خطوط ۱۳۴ (پیام در `sti_gs_messages` نیست)، ۱۴۰
(`raw_json` قابل decode نیست)، ۱۵۴ (گره غیرقابل اجرا).

هر سه `fallback_to_legacy()` را صدا می‌زنند. پس سؤال دقیق‌تر
می‌شود:

> **چرا `fallback_to_legacy()` اجرا می‌شود ولی `chain_mode` روی
> `legacy` نمی‌ماند؟**

سه احتمال باقی‌مانده:
1. `update()` اجرا می‌شود ولی `affected_rows = 0` (کسی چک نمی‌کند)
2. نوشته می‌شود ولی چیز دیگری بازنویسی‌اش می‌کند
3. `message_pk` یا `raw_json` مشکل دارد و همان مسیر هر بار تکرار
   می‌شود **بدون اینکه fallback مؤثر باشد**

**کارشناس دقیقاً همین را خواسته:** read-back بعد از CAS و ثبت
`affected_rows`. تأیید می‌کنیم.

---

## ۴) ✅ ادعاهای تأییدشده‌ی دیگر

| ادعا | ارجاع | نتیجه |
|---|---|---|
| باگ شمارنده‌ی `skipped` | `auto-worker:564, 672` | ✅ تأیید (خودمان یافته بودیم) |
| `waiting` سه دلیل را جمع می‌کند | `auto-worker:609, 613, 617` | ✅ تأیید |
| `record()` فقط چهار شمارنده | `auto-worker:1245-1258` | ✅ تأیید |
| مهلت از `updated_at` | `auto-worker:852-875` | ✅ تأیید |
| `claim`/`update` ساعت را تازه می‌کنند | `session:167, 155` | ✅ تأیید |
| کامنت «عمداً بدون قفل» | `auto-worker:930` | ✅ تأیید |
| `update()` فقط `WHERE id` | `session:156` | ✅ تأیید |
| `MODE_LEGACY = 'legacy'` | `node:56-58` | ✅ تأیید |

---

## ۵) نکاتی که کارشناس سوم اضافه کرد و ما نداشتیم

### ۵-۱) `SCANNED` را به `WAITING` اضافه نکنید

استدلالش: `SCANNED` یک وضعیت **قابل‌پردازش** است، نه انتظار پاسخ
ربات. ما در سه سند این را به‌عنوان گزینه مطرح کرده بودیم.

می‌پذیریم. اضافه کردنش معنای `WAITING` را خراب می‌کرد.

### ۵-۲) `no_progress_count` و `no_progress_since` جدا از `attempts`

ما پرسیده بودیم «شمارنده کجا ذخیره شود؟» چون `attempts` را نباید
دست بزنیم. پاسخ تمیزی است.

### ۵-۳) دو ستون جدا، نه یکی

`waiting_since` برای حالت‌های `WAITING`، و `no_progress_since` برای
تکرار بی‌پیشرفت. کارشناس دوم فقط اولی را پیشنهاد داده بود — که
`SCANNED` را پوشش نمی‌داد.

### ۵-۴) خروجی نوع‌دار به‌جای Boolean

```
advanced · waiting · route_changed · lock_contention
already_complete · invalid_route · needs_review · failed · completed
```

این ریشه‌ی مشکل را می‌زند: `no_progress` الان شش مفهوم متفاوت را یکی
کرده (قفل، تغییر مسیر، قبلاً کامل شده، نبود مرحله‌ی بعد، fallback،
انتظار).

### ۵-۵) ⭐ مسیر موازی در Test Wizard

`class-gs-test-wizard.php:87-92` مستقیماً
`STI_GS_Channel_Watcher::run()` را صدا می‌زند و گیت‌های `tick()` را
دور می‌زند (`class-gs-channel-watcher.php:149-171`).

کارشناس صریح گفت **این علت backlog فعلی نیست** ولی از نظر معماری
خطرناک است. ما این را ندیده بودیم.

### ۵-۶) تحلیل رکوردهای یتیم

نکته‌ای که هیچ‌کدام نگفته بودیم: `profile_items.message_pk` در schema
`NOT NULL` است (`class-gs-db.php:614-630`) و مسیر تولید فعلی
(`class-gs-profile.php:152-170`) فقط از `m.id` معتبر insert می‌کند.

⇒ **این ۸۲۵ رکورد نمی‌توانند از مسیر تولید فعلی آمده باشند.** یا
پیش از schema فعلی ساخته شده‌اند، یا از مسیر دیگری وارد شده‌اند.

و `candidate_joins()` (`class-gs-db.php:117-120`) از `INNER JOIN`
استفاده می‌کند، پس این رکوردها اصلاً به Candidate معتبر تبدیل
نمی‌شوند — یعنی **بی‌ضررند، فقط رتبه‌بندی را شلوغ می‌کنند**.

---

## ۶) اختلاف نظر — یک مورد

کارشناس سوم ترتیب زیر را پیشنهاد داد:

```
P0: trace routing + تفکیک آمار
P0: اصلاح fallback و route invariant
```

یعنی رصدپذیری و اصلاح رفتاری هر دو P0 و تقریباً هم‌زمان.

**نظر ما: رصدپذیری باید یک نسخه‌ی مستقل و جلوتر باشد.**

دلیل: چهار نسخه‌ی متوالی منتشر کردیم که هر کدام «منطقی» بودند و روی
میزبان کم‌اثر. اگر آمار و اصلاح رفتاری را با هم بفرستیم و باز نتیجه
نگرفتیم، **نمی‌دانیم کدام‌یک کار نکرده**.

پیشنهاد ما:
- **۱۰.۱۲.۲۲** — فقط رصدپذیری (خروجی نوع‌دار + تفکیک آمار +
  read-back در `fallback_to_legacy`). **هیچ تغییر رفتاری.**
- یک دامپ میزبان بگیریم و ببینیم دقیقاً کدام مسیر فعال است
- **۱۰.۱۲.۲۳** — اصلاح رفتاری بر اساس داده‌ی واقعی

این یک دور تأخیر دارد ولی حدس را حذف می‌کند.

---

## ۷) آنچه هنوز `UNOBSERVABLE` است

با وجود پیشرفت بخش ۲، هنوز نمی‌دانیم:

1. مقدار فعلی `chain_mode` برای ‎#79 و ‎#80
2. مقدار `mode()` سراسری
3. آیا `fallback_to_legacy()` اصلاً اجرا می‌شود
4. `affected_rows` آن `update()`
5. کدام‌یک از سه مسیر (۱۳۴/۱۴۰/۱۵۴) فعال است

فهرست شواهد کارشناس دقیقاً همین‌هاست. **بدون آن‌ها اصلاح رفتاری
حدس است.**

---

## ۸) جمع‌بندی

**کیفیت گزارش: بالاترین از میان سه گزارش.** دلایل:

- تنها گزارشی که ارجاع خطی داشت که **ما نداشتیم** و مستقیماً به
  تشخیص کمک کرد (بخش ۱)
- تنها گزارشی که هر هشت مسئله را پوشش داد
- تنها گزارشی که مسیر موازی Test Wizard را دید
- تنها گزارشی که ثابت کرد رکوردهای یتیم از مسیر فعلی نیامده‌اند

**پیشرفت تشخیصی این دور:**

| قبل | بعد |
|---|---|
| سه احتمال باز | (ب) **قطعاً رد شد** · (ج) به‌تنهایی حلقه را توضیح نمی‌دهد |
| «چرا `skipped`؟» | «چرا `fallback_to_legacy` اثرش نمی‌ماند؟» |
| منبع `no_progress` نامعلوم | **فقط `chain-engine`** — ۱۰ نقطه‌ی مشخص |

**وضعیت‌ها:**
```
SCANNED_LOOP  = ریشه محدود شد به ۳ مسیر، هنوز NOT PROVEN
FIBER_ENOMEM  = ROOT_CAUSE_UNKNOWN
ORPHANS       = از مسیر تولید فعلی نیامده‌اند (شاهد schema)
```

**قدم بعدی پیشنهادی ما:** نسخه‌ی ۱۰.۱۲.۲۲ فقط-رصدپذیری، شامل دقیقاً
همان شش قلم شاهدی که کارشناس خواسته. بعد از یک دامپ میزبان، اصلاح
رفتاری.

**منتظر تأیید مالک هستیم.**
