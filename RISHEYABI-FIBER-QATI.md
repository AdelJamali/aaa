# ریشه‌یابی Fiber/ENOMEM — با شواهد واقعی میزبان

> `OOM_DIAG` · `2026-09-11 23:21:40` · pid `3799278` · LiteSpeed · PHP 8.4.23
> **این اولین payload واقعی از لحظه‌ی شکست است. از ۱۰.۱۲.۱۵ منتظرش بودیم.**

---

## ۰) نتیجه در یک خط

# ✅ هر سه فرضیه‌ی قبلی رد شد — مقصر `RLIMIT_AS` است

**سقف فضای آدرس فرآیند = ۱ گیگابایت (نرم) / ۱٫۵ گیگابایت (سخت)**
و MadelineProto در لحظه‌ی شکست **۶۹۳ مگابایت** از آن را گرفته بود.

---

## ۱) ★ آنچه رد شد — با عدد

سه چیزی که چهار نسخه دنبالشان بودم، **همه سالم‌اند**:

| فرضیه | عدد واقعی | حکم |
|---|---|---|
| **سقف تعداد نگاشت (VMA)** | `maps_count=513` از `max_map_count=65530` = **۰٫۸٪** | ❌ **رد** |
| **`RLIMIT_DATA`** | `unlimited` | ❌ **رد** |
| **کمبود RAM میزبان** | `MemAvailable` = **۸۷٫۷ گیگابایت** آزاد | ❌ **رد** |
| **`memory_limit` پی‌اچ‌پی** | `-1` (نامحدود!) در لحظه‌ی شکست | ❌ **رد** |

> **توصیه‌ی خود MadelineProto — «`vm.max_map_count` را به ۲۶۲۱۴۴ برسانید» —
> برای این میزبان بی‌فایده است.** ما در ۰٫۸٪ آن سقف هستیم. اگر بدون این
> داده به هاست پیام داده بودیم، وقت هر دو طرف تلف می‌شد.

---

## ۲) ★ عددی که باقی می‌ماند

```
"rlimit_as": "1073741824 1572864000 bytes"
"status": { "VmSize": "709232 kB", "VmPeak": "719492 kB", "VmRSS": "138480 kB" }
```

| سنجه | مقدار |
|---|---|
| سقف نرم فضای آدرس | **۱۰۲۴ مگابایت** |
| سقف سخت | ۱۵۰۰ مگابایت |
| `VmSize` در لحظه‌ی شکست | **۶۹۳ مگابایت** (۶۷٫۶٪ سقف) |
| `VmPeak` | ۷۰۳ مگابایت (۶۸٫۶٪) |
| **حافظه‌ی فیزیکی واقعی** | **فقط ۱۳۵ مگابایت** |

**اختلاف ۶۹۳ در برابر ۱۳۵ مگابایت، قلب ماجراست.**

`RLIMIT_AS` فضای **مجازی** را می‌شمارد، نه فیزیکی. MadelineProto حدود
۵۵۰ مگابایت نگاشت مجازی می‌گیرد که عملاً استفاده نمی‌شود — ولی از سهم
یک‌گیگابایتی خرج می‌شود.

---

## ۳) چرا با ۳۳۱ مگابایت باقی‌مانده، ۲ مگابایت شکست خورد؟

اینجا باید صادق باشم: **۳۳۱ مگابایت جا در برابر یک درخواست ۲ مگابایتی،
به‌تنهایی شکست را توضیح نمی‌دهد.** پس دنبال شاهد بیشتری گشتم و پیدا شد:

### شاهد ۱ — دو فرآیند هم‌زمان

```
23:21:39   pid=3799278  ← Fiber failed
23:21:47   pid=3795335  ← Fiber failed
23:21:48   pid=3795335  ← Fiber failed
```

**دو PID متفاوت، در ۹ ثانیه.** `Max processes = 310` و همه‌ی lsphpها
زیر یک LVE (`/lve2571`) هستند. سقف `RLIMIT_AS` **برای هر فرآیند** است،
ولی حافظه‌ی فیزیکی LVE **مشترک** است.

### شاهد ۲ — `memory_limit` در لحظه‌ی شکست `-1` بود

```
ENV_DIAG (23:21:38): "memory_limit":"512M"
OOM_DIAG (23:21:40): "memory_limit":"-1"
```

دو ثانیه فاصله، دو مقدار متفاوت. کد ما فقط `512M` می‌گذارد
(`class-sti-mtproto.php:247` و `:657`) — پس **`-1` را خود MadelineProto
گذاشته**. یعنی سقف محافظ PHP در آن لحظه برداشته شده بود و تنها ترمز
باقی‌مانده، همان `RLIMIT_AS` کرنل بود.

### شاهد ۳ — worker یتیم انباشته می‌شود

```
23:21:38  ترمیم IPC لغو شد — exec در دسترس نیست
          فایل‌های موجود: [ipcState.php, lock]
23:21:47  همان پیام، pid دیگر
23:21:47  MTProto: getDialogs — The endpoint does not exist!
```

`exec` غیرفعال است ⇒ نمی‌توانیم worker مرده را پاک کنیم ⇒ هر تلاش
جدید یک فرآیند lsphp دیگر اضافه می‌کند که همان ۶۹۳ مگابایت مجازی را
می‌گیرد.

**⇒ نتیجه: `RLIMIT_AS` سقف است، و انباشت worker یتیم آن را پر می‌کند.**

---

## ۴) وضعیت رسمی دو فرضیه‌ی باز

از ۱۰.۱۲.۱۵ گفتم بین این دو انتخاب نمی‌کنم تا داده برسد. داده رسید:

| فرضیه | حکم |
|---|---|
| **الف) محدودیت محیط/میزبان** | ✅ **تأیید شد** — `RLIMIT_AS=1GiB` روی LVE |
| **ب) ناسازگاری ذاتی معماری** | 🔶 **جزئی** — MadelineProto ۶۹۳ مگابایت مجازی برای ۱۳۵ مگابایت واقعی می‌گیرد؛ این با سقف ۱ گیگ سازگار نیست |

**هر دو درست‌اند و به یک راه‌حل می‌رسند.**

---

## ۵) 🎯 راه‌حل — به ترتیب اولویت

### راه‌حل ۱ — درخواست از هاست ⭐ (ساده‌ترین، مؤثرترین)

متن دقیق برای ارسال به پشتیبانی:

> سلام. روی اکانت من (LVE `lve2571`) مقدار `RLIMIT_AS`
> (Max address space) برای فرآیندهای `lsphp` برابر ۱ گیگابایت نرم /
> ۱٫۵ گیگابایت سخت است.
>
> برنامه‌ای که استفاده می‌کنم (کتابخانه‌ی MadelineProto با PHP Fiber)
> حدود ۷۰۰ مگابایت فضای **مجازی** می‌گیرد در حالی که حافظه‌ی
> **فیزیکی** واقعی‌اش فقط ۱۳۵ مگابایت است، و به همین دلیل
> `mmap` با خطای `Cannot allocate memory` شکست می‌خورد.
>
> لطفاً یکی از این دو کار را انجام دهید:
> ۱. `RLIMIT_AS` را به ۲ گیگابایت افزایش دهید، یا
> ۲. اگر روی CloudLinux از VMEM استفاده می‌کنید، آن را روی `0`
>    (نامحدود) بگذارید — توصیه‌ی رسمی خود CloudLinux است و
>    `PMEM` کنترل واقعی حافظه را حفظ می‌کند.
>
> مصرف فیزیکی من تغییری نمی‌کند؛ فقط شمارش مجازی مشکل‌ساز است.

**نکته:** روی CloudLinux، VMEM از سال‌ها پیش **منسوخ** اعلام شده و
مقدار پیشنهادی رسمی `0` است. این درخواست کاملاً متعارف است.

### راه‌حل ۲ — مسیر Bot API به‌جای MTProto

دکمه‌ی ‎#164 یک `deep_link` است:
```
https://t.me/FileechFreeBot?start=download-317326
```
این را **Bot API هم می‌تواند بزند** — بدون MTProto، بدون Fiber، بدون
phar ۱۹ مگابایتی. مصرف مجازی نزدیک صفر.

اگر هاست همکاری نکرد، این راه‌حل واقعی و پایدار است. ولی تغییر
معماری است و طبق قرارمان بدون تأیید شما شروع نمی‌کنم.

### راه‌حل ۳ — کاهش `fiber.stack_size`

از ۲ مگابایت به ۵۱۲ کیلوبایت. **توصیه نمی‌کنم**: مصرف مجازی
MadelineProto را کم نمی‌کند (مشکل اصلی) و ریسک stack overflow دارد.

---

## ۶) آنچه قطعاً کار نمی‌کند

| اقدام | چرا بی‌فایده است |
|---|---|
| افزایش `memory_limit` | در لحظه‌ی شکست `-1` بود — یعنی نامحدود |
| افزایش `vm.max_map_count` | در ۰٫۸٪ سقف هستیم |
| افزایش RAM سرور | ۸۷ گیگابایت آزاد است |
| ری‌استارت | تا وقتی سقف ۱ گیگ است، تکرار می‌شود |

---

## ۷) 🐛 دو باگ جانبی که همین حالا دیدم

### باگ A — `sys_getloadavg()` (تکراری، هنوز زنده)

```
Governor: sys_getloadavg() expects exactly 0 arguments, 1 given
```

در ۳ب۴۵۱۰۵ ادعا شده بود رفع شده. **نشده** — یا برگشته. بی‌خطر
(fallback به `/proc/loadavg` دارد) ولی هر تیک دو warning می‌سازد و
لاگ را کثیف می‌کند. **در ۱۰.۱۲.۲۳ رفع می‌کنم.**

### باگ B — تشخیص cgroup v1 روی CloudLinux ناقص است

```
"cgroup": { "raw": "12:memory:/lve2571 ...", "v2": "unavailable", "v1": "unavailable" }
```

مسیر memory را **درست خواند** (`/lve2571`) ولی `v1` هم `unavailable`
شد. یعنی `/sys/fs/cgroup/memory/lve2571/...` قابل خواندن نبود. روی
CloudLinux داده‌ی واقعی در `/proc/bc/lve2571/meminfo` است که اصلاً
بررسی نمی‌کنیم. **در ۱۰.۱۲.۲۳ اضافه می‌کنم** (فقط‌خواندنی).

---

## ۸) حکم نهایی

```
FIBER_ENOMEM = ROOT CAUSE PROVEN
```

**علت ریشه‌ای:** `RLIMIT_AS` برابر ۱ گیگابایت روی LVE، در برابر
`VmSize` معادل ۶۹۳ مگابایت که MadelineProto اشغال می‌کند — با تشدید از
سوی workerهای یتیمی که به‌خاطر غیرفعال بودن `exec` پاک نمی‌شوند.

**فایل/خط:** `AbstractDriver.php:503` → `Fiber->start()` →
`zend_fiber_stack_allocate()` → `mmap(2 MiB + guard)` → `ENOMEM`
از `RLIMIT_AS`، نه از حافظه‌ی واقعی.

**اطمینان:** بالا برای «سقف = `RLIMIT_AS`» (چهار جایگزین دیگر با عدد
رد شدند). متوسط برای سهم دقیق انباشت worker (فقط دو PID دیدیم؛ برای
اثبات کامل به شمارش فرآیندها نیاز است که `exec` غیرفعال اجازه نمی‌دهد
⇒ **UNOBSERVABLE**).

---

## ۹) قدم بعدی

**۱. پیام بخش ۵-۱ را به هاست بفرستید** — این تنها کاری است که الان
واقعاً گره را باز می‌کند و از دست من خارج است.

**۲. پاسخشان را بگویید.** اگر مثبت بود، همان دامپ بعدی تأییدش می‌کند.
اگر منفی بود، سراغ راه‌حل ۲ (Bot API) می‌رویم و پیش از هر کد، طرحش
را می‌نویسم.

**۳. ‎#68 همچنان اولویت دوم است.** آزاد کردنش قبل از باز شدن این گره،
فقط سریع‌تر به همین دیوار می‌رساندش.
