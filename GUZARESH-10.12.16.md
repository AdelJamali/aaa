# گزارش پچ تشخیصی ۱۰.۱۲.۱۶ — طبق RULE 0 تا RULE 11

> **هیچ علت ریشه‌ای اعلام نشده است.** این پچ فقط شاهد جمع می‌کند.
> `PRIMARY_ROOT_CAUSE = ROOT_CAUSE_UNKNOWN` — و تا رسیدن داده‌ی هاست همین می‌ماند.

نسخه: **10.12.16** · کامیت کد: `8b8b7c3` · کامیت ZIP: `fa93904`
ZIP: `golden-importer-10.12.16.zip` (۱۳۲ فایل)
sha256: `9c6c56b5900876596fdaadad4db59a682564fc0d8e0658a025005d88b1154c00`
blob گیت‌هاب (تأییدشده روی ریموت): `6db52bc08acf7003073b402f8407a7dde44c4013`

---

## ۱) فایل‌های تغییریافته

| # | فایل | خطوط | نقش تغییر |
|---|------|------|-----------|
| ۱ | `includes/golden-scan/class-gs-env-diag.php` | +۱۲۶ | فیلدهای RULE 6/7/8 + `oom_context_safe()` + `oom_gate_probe()` |
| ۲ | `includes/class-sti-mtproto.php` | +۸۳ | try/catch دور بلوک تشخیصی، ثبت نتیجه‌ی گیت، پرچم نمایشی، `auth_state_report()` |
| ۳ | `includes/golden-scan/class-gs-flags.php` | +۵ | کلید قابلیت `oom_diag` (پیش‌فرض روشن) |
| ۴ | `includes/golden-scan/class-gs-system-check.php` | +۱۸ | نمایش `CLIENT_CONSTRUCTION_FAILED` به‌جای «وارد نشده‌اید» |
| ۵ | `admin/views/telegram.php` | +۳ | همان تفکیک در نوار وضعیت |
| ۶ | `includes/golden-scan/class-gs-test-wizard.php` | +۵/−۴ | استفاده از نسخه‌ی safe |
| ۷ | `sanil-telegram-importer.php` | +۲/−۲ | نسخه ۱۰.۱۲.۱۵ ← ۱۰.۱۲.۱۶ |
| ۸ | `tests/test-10-12-16-oomdiag2.py` | +۲۰۳ | سوئیت جدید: هر RULE یک تست |
| ۹-۱۱ | سه سوئیت موجود | +۱۹/−۱۰ | هم‌ترازی انتظارِ نسخه و رشته‌های تغییرکرده |

**هیچ فایلی حذف یا جابه‌جا نشد. هیچ فایل جدیدی در مسیر اجرایی افزوده نشد** (تنها فایل جدید یک تست است که در ZIP نیست).

## ۲) دیف کامل

`docs-10.12.16-patch.diff` (۶۵۶ خط) — و در گیت: `git diff d9ea32a..8b8b7c3`.

## ۳) توضیح هر تغییر

**۳٫۱ `oom_context()` — سه عدد گمشده (RULE 6).**
`maps_count` با `substr_count(/proc/self/maps, "\n")`، `max_map_count` از `/proc/sys/vm/max_map_count`، و `rlimit_data` از «Max data size». دلیل فنی: `mmap` می‌تواند با ENOMEM شکست بخورد در حالی که RAM آزاد است — وقتی تعداد VMAها به سقف کرنل برسد. طبق منبع php-src، هر Fiber دقیقاً **۲ VMA** می‌سازد (ناحیه + یک guard page) و اندازه‌اش `۲MiB + ۴KiB` است. بدون این دو عدد، «سقف شمارشی» از «کمبود حجمی» قابل تفکیک نیست. `rlimit_data` هم لازم است چون از کرنل ۴٫۷ (کامیت `84638335900f`) سقف RLIMIT_DATA شامل نگاشت‌های private anonymous — یعنی دقیقاً stack یک Fiber — هم می‌شود.

**۳٫۲ فیلدهای حافظه (RULE 7).** `VmSize/VmPeak/VmRSS/VmHWM` از `/proc/self/status` و `MemAvailable/SwapFree/CommitLimit/Committed_AS` از `/proc/meminfo`. هر مقدار خوانده‌نشده صریحاً `unreadable` می‌شود — هرگز حدس زده نمی‌شود — و در `diagnostic_read_errors` هم فهرست می‌شود تا «نبودِ داده» با «صفر بودنِ داده» اشتباه نشود.

**۳٫۳ گیت حذف نشد؛ اندازه‌گیری شد (RULE 5).** ادعای ممیزی بیرونی («گیت هرگز شلیک نمی‌کند») **اثبات‌نشده** است، پس رفتار گیت را دست نزدم. به‌جایش `oom_gate_probe()` همان الگوها را روی متن خطا اجرا می‌کند و `matched|skipped` را با دلیل دقیق (`contains_cannot_allocate_memory`، `no_pattern_match`، …) ثبت می‌کند. مهم‌تر: حالا ثبتِ لاگ **دیگر به این گیت مشروط نیست** — همیشه ثبت می‌شود و نتیجه‌ی گیت فقط یک فیلد داده است. یعنی اگر گیت واقعاً هم اشتباه بوده باشد، دیگر شاهدی را پنهان نمی‌کند. `error_message_raw` (۴۰۰ کاراکتر) هم ذخیره می‌شود تا متن واقعی — نه تفسیر ما — قابل بازرسی باشد.

**۳٫۴ محافظت کامل (RULE 3/4).** کل بلوک در `try { … } catch (\Throwable $diag_error)` است، و حتی خودِ logger هم در یک try/catch تودرتو قرار دارد. `oom_context_safe()` هر Throwable را به داده تبدیل می‌کند (`diagnostic_failed => true`). ترتیب اجرا هم تضمین‌شده است: `client_error` **قبل** از بلوک تشخیصی مقداردهی می‌شود و `return new WP_Error(... $last_error)` **بعد** از آن — و در فاصله‌ی این دو هیچ انتساب دیگری به `$last_error` نیست (تست R3d).

**۳٫۵ ipc_heal فقط گزارش می‌شود (RULE 8).** `ipc_heal_possible` با `function_exists('exec') && is_callable('exec')` تعیین می‌شود و `ipc_heal_reason` مقدار `exec_disabled` می‌گیرد. روی هاست شما که exec غیرفعال است، این فیلد **`false / exec_disabled`** خواهد بود — یعنی مسیر «آزادسازی حافظه» در این محیط عملاً بی‌اثر است. طبق قاعده‌ی خودتان این را «محدودیت محیطی» ثبت کردم، نه «نبودِ worker».

**۳٫۶ UI دیگر دروغ نمی‌گوید (RULE 9).** `auth_state()` **دست‌نخورده** است. یک متد نمایشی `auth_state_report()` اضافه شد که وقتی ساخت client شکست خورده باشد `CLIENT_CONSTRUCTION_FAILED` و متن خطای واقعی را برمی‌گرداند. در AJAX، کلید `state` با همان معنای قبلی باقی مانده و فقط `display_state` و `construction_failed` **اضافه** شده‌اند (سازگاری کامل با هر مصرف‌کننده‌ی موجود).

**۳٫۷ کلید خاموشی (RULE 10).** `oom_diag` با پیش‌فرض روشن. چک‌باکسش خودکار در «کلیدهای قابلیت» صفحه‌ی Worker ظاهر می‌شود (حلقه روی `definitions()`)، پس هیچ کار UI لازم نبود.

## ۴) اثبات اینکه رفتار عملیاتی تغییر نکرده

| ادعا | شاهد |
|---|---|
| خطای اصلی حفظ شده | `client_error` قبل از تشخیص ست می‌شود، `WP_Error` بعد از آن با همان `$last_error` برمی‌گردد، و بین‌شان هیچ `$last_error =` نیست — تست‌های R3a/R3b/R3c/R3d |
| هیچ مسیر اجرایی جدیدی نیست | ناحیه‌ی تشخیصی از نظر ایستا اسکن شد: هیچ‌کدام از `proc_open/exec/shell_exec/popen/system/passthru/unlink/rmdir/file_put_contents/fwrite/update_option/delete_option/$wpdb/ini_set` در آن نیست — تست R2a |
| فقط خواندن | تنها `@file_get_contents` و `fopen(...,'rb')`؛ هیچ mode نوشتنی — تست R2b |
| exec صدا زده نمی‌شود | فقط `is_callable('exec')` به‌عنوان کاوش؛ هیچ فراخوانی‌ای — تست R2c |
| سیاست تلاش مجدد دست‌نخورده | `$attempts = 2;` و `MAX_IPC_RECYCLES = 2` — تست R1b/R1c |
| memory_limit دست‌نخورده | همان منطق ۵۱۲M قبلی — تست R1a |
| Fiber دستکاری نشده | هیچ `fiber.stack_size` در کد نیست — تست R1d |
| گارد ipc_heal نسخه‌ی ۱۰٫۱۲٫۱۴ سالم | رشته‌ی گارد عیناً موجود؛ `ipc_heal` در بلوک تشخیصی صدا زده نمی‌شود — تست R8c/R8d |
| مقادیر بازگشتی auth_state تغییر نکرده | هر سه مقدار قبلی موجود؛ کلید `state` در AJAX دست‌نخورده — تست R9c/R9d |
| تغییرات UI فقط نمایشی | `STI_GS_System_Check::run()` تنها دو مصرف‌کننده‌ی نمایشی دارد و هیچ کدی روی وضعیت یک سطر شاخه نمی‌زند (بررسی‌شده با grep) |
| بار اضافه ندارد | `auth_state_report($state)` وضعیتِ از پیش محاسبه‌شده را می‌گیرد؛ هیچ فراخوانی دوم و هیچ بارگذاری اضافه‌ی موتور نیست |
| فاجعه‌ی ۱۰٫۱۲٫۱۲ تکرار نشده | آشکارساز ternary تودرتوی PHP 8.4 روی هر پنج فایل PHP — تست S1؛ و خنثی‌بودن آکولادها نسبت به کامیت قبلی — تست S2 |
| رگرسیون کلی | **هر ۸ سوئیت ALL PASS** |

دو ایراد در کار خودم پیدا و اصلاح شد پیش از کامیت: (۱) متد جدید اشتباهاً بین docblock و خودِ `auth_state()` نشسته بود و آن را یتیم کرده بود؛ (۲) `auth_state()` دوبار ارزیابی می‌شد. هر دو رفع شدند. همچنین دو «شکست» اولیه‌ی تست، باگ **خودِ تست** بود نه پچ (شمارنده‌ی ساده‌لوح آکولاد روی `{1,10}` داخل regex) — با مقایسه‌ی دلتا نسبت به کامیت قبلی اثبات شد که پچ از نظر آکولاد خنثی است.

## ۵) ریسک هر تغییر

| تغییر | ریسک | چرا پایین است |
|---|---|---|
| فیلدهای جدید `/proc` | **خیلی کم** | فقط خواندن؛ خطا ⇒ `unreadable` |
| `oom_context_safe()` | **خیلی کم** | فقط پوشش try/catch |
| `oom_gate_probe()` | **کم** | تابع خالص روی یک رشته |
| برداشتن شرطِ گیت از مسیر ثبت لاگ | **کم** | حداکثر یک ردیف لاگ بیشتر در هر request (با فیوز `$oom_diag_logged`) |
| try/catch در `client()` | **خیلی کم** | فقط خطای تشخیصی را می‌بلعد، نه خطای اصلی را |
| `auth_state_report()` | **کم** | متد جدید؛ هیچ مصرف‌کننده‌ی قدیمی آن را صدا نمی‌زند |
| دو تغییر UI | **کم** | فقط متن؛ در بدترین حالت پیام نادرست، نه رفتار نادرست |
| کلید `oom_diag` | **خیلی کم** | یک ردیف در آرایه‌ی تعاریف |
| افزایش حجم لاگ | **کم** | یک ردیف JSON در هر request شکست‌خورده |

بزرگ‌ترین ریسک باقی‌مانده: **نبودِ parser واقعی PHP در محیط من.** جبران: آشکارساز ternary + بررسی خنثی‌بودن آکولاد + بازخوانی چشمی دیف. با این حال، تأیید نهایی فقط با نصب روی هاست ممکن است.

## ۶) نقشه‌ی بازگشت (rollback)

سه سطح، از سبک به سنگین:

1. **خاموش‌کردن بدون هیچ تغییر کد** — Worker → «کلیدهای قابلیت» → تیک «تشخیص ENOMEM/Fiber» را بردارید. تمام خروجی‌های جدید متوقف می‌شود.
2. **بازگشت نسخه** — نصب مجدد ZIP نسخه‌ی ۱۰٫۱۲٫۱۵ (sha256 `cb5d409b0f59d6df75fe0d99e58f87e3fc8a22d37cc436f3133b88939dcb8bea`). چون هیچ تغییری در دیتابیس، schema، option یا session نداده‌ام، بازگشت بی‌عارضه است.
3. **بازگشت گیت** — `git revert 8b8b7c3` (کد) و در صورت نیاز `fa93904` (ZIP).

هیچ مهاجرت داده‌ای، هیچ `ALTER TABLE` و هیچ تغییر option‌ای در کار نیست، پس هیچ سطحی از بازگشت نیازمند ترمیم داده نخواهد بود.

## ۷) فهرست فرض‌های اثبات‌نشده

۱. **نصب ۱۰٫۱۲٫۱۵ روی هاست تأیید نشده** — و اکنون ۱۰٫۱۲٫۱۶ هم همین‌طور.
۲. **نشتی Fiber اثبات نشده.** قوی‌ترین فرضیه است (LiteSpeed فرآیند lsphp را بین requestها زنده نگه می‌دارد و `Magic::registerFiber()` یک `WeakMap` سراسری دارد)، اما **تا نیامدن `maps_count` واقعی، فرضیه است، نه یافته**.
۳. **`vm.max_map_count` علت ریشه‌ای نیست تا وقتی عددش را ندیده‌ایم.** مقدار پیش‌فرض ۶۵۵۳۰ فقط یک انتظار است.
۴. **ادعای «گیت هرگز شلیک نمی‌کند» هنوز اثبات‌نشده** — دقیقاً به همین دلیل حذفش نکردم؛ حالا خودش را گزارش می‌کند.
۵. **تناقض حل‌نشده در memory_limit:** شما ۲۸۰MB با ۱۰۳MB مصرف نوشتید، ولی runtime تأییدشده‌ی قبلی در context خودِ MTProto مقدار ۵۱۲M را نشان می‌داد. این دو **متعلق به دو SAPI/دامنه‌ی متفاوت‌اند**؛ هیچ‌کدام را به‌عنوان مقدار فرآیندِ شکست‌خورده انتخاب نکردم. ضمناً چون Fiber از `mmap` خام استفاده می‌کند نه `emalloc`، `memory_limit` نه می‌تواند این خطا را بسازد و نه درمان کند.
۶. **روی CloudLinux، `/proc/meminfo` مربوط به هاست است نه LVE شما** — پس اعداد meminfo ممکن است سخاوتمندانه‌تر از سقف واقعی شما باشند.
۷. **`fiber.stack_size` مؤثر خوانده نشده** (در این پچ اضافه نکردم چون RULE 1 تنظیمات Fiber را ممنوع کرده بود؛ خواندنش بی‌خطر است و اگر بخواهید در گام بعد اضافه می‌کنم).
۸. **همبستگی زمانی هنوز UNPROVEN** — LiteSpeed از PID دوباره استفاده می‌کند.
۹. **پایداری در ۱۰ اجرای متوالی سنجیده نشده** (نیازمند هاست).

---

## قدم بعدی شما — و دقیقاً همین کافی است

۱. ZIP نسخه ۱۰٫۱۲٫۱۶ را نصب کنید.
۲. کاری کنید که خطا دوباره رخ دهد (همان کاری که همیشه شکست می‌خورد).
۳. **گزارش‌ها → فیلتر `error`** → دنبال ردیفی بگردید که با `OOM_DIAG {` شروع می‌شود و کل JSON آن را برایم بفرستید.
۴. اگر ردیفی نبود: **Golden Scan → محیط → دکمه‌ی تشخیص** را بزنید و خروجی JSON را بفرستید.

مهم‌ترین چیزی که در آن JSON دنبالش می‌گردم: `maps_count` و `max_map_count`. اگر این دو به هم نزدیک بودند ⇒ **B (محدودیت هاست)** و راه‌حل یک تغییر sysctl است بدون هیچ تغییر کد. اگر فاصله‌ی زیادی داشتند ⇒ به سمت **A (باگ در کد ما)** می‌رویم. تا آن لحظه:

**`ROOT_CAUSE_UNKNOWN`.**

### جمع‌بندی اجباری (عیناً)

> escapeshellarg() blocker: FIXED/WORKED THROUGH.
> Current blocker: Fiber stack mmap ENOMEM.
> Physical RAM exhaustion: NOT YET PROVEN.
> PHP memory_limit exhaustion: NOT THE EVIDENCE SHOWN.
> Worker automatic operation: currently OFF, therefore not a valid test of automatic recovery yet.
