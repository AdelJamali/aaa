# درخواست دوم — تکمیل راه‌حل

> پیرو `DARKHAST-RAHEHAL.md` و پاسخ شما درباره‌ی `waiting_since`.
> این سند کوتاه است و فقط شکاف‌ها را می‌پرسد.

---

## ۰) تأیید دریافت

پاسخ اولویت ۲ را کامل بازبینی کردیم. **هر هفت ادعای فنی تأیید شد**
(جزئیات در `ARZYABI-RAHEHAL-1-WAITING.md`).

دو موردش را ما اصلاً ندیده بودیم:

**الف) نوشتن deadline بدون مالکیت.** شما گفتید بلوک مهلت روی snapshot
و پیش از claim می‌نویسد. بررسی کردیم — و **کامنت خودِ کد تأییدتان
می‌کند**. هشتاد خط پایین‌تر از بلوک مهلت،
`class-gs-auto-worker.php:930`:

> «عمداً اینجا قفل گرفته نمی‌شود. هر هفت موتور … خودشان
> `STI_GS_Session::claim()` را صدا می‌زنند.»

یعنی `advance_one()` عامدانه بدون قفل طراحی شده و ما یک نوشتنِ
تغییردهنده‌ی state داخلش گذاشته‌ایم.

**ب) `ERROR_BOT_TIMEOUT`.** نکته‌ی شما درست است: این حالت خطاست، مسیر
requeue دستی خودش را دارد (`class-gs-session-ajax.php:284`)، ولی
داخل `const WAITING` است (`class-gs-auto-worker.php:66`) — پس مهلت
۱۲ ساعته شاملش می‌شود و می‌تواند Sessionای را که کاربر قصد requeue
داشته به `NEEDS_REVIEW` ببرد.

یک نکته‌ی جانبی که شما نمی‌توانستید بدانید: نام `waiting_since` از
قبل به‌صورت جزئی در کد هست —
`LOCK_WAIT_SINCE_KEY = 'sti_gs_migration_lock_wait_since'`
(`class-gs-db.php:70`). بررسی کردیم: **تعارض ندارد**، فقط option قفل
migration است. ولی هنگام grep گمراه‌کننده است.

---

## ۱) متن کامل گزارش

خلاصه‌ی نتایج را داریم، **متن کامل را نه**. مسیر
`/tasklet/threads/a_t2q77bgevsjnv9hsper9/work/waiting-audit.md`
مربوط به محیط شماست و برای ما قابل دسترسی نیست.

سه بخش را حتماً لازم داریم:

1. **جدول کامل episodeها** — هسته‌ی طراحی است (بخش ۲)
2. **SQLهای فقط‌خواندنی** — برای بررسی وضعیت فعلی
3. **ماتریس آزمون** race / TTL / migration / timezone

اگر می‌توانید متن را مستقیم در پاسخ بگذارید، بهترین حالت است.

---

## ۲) جدول episodeها — مهم‌ترین شکاف

نوشته‌اید episodeها «به‌طور دقیق تعریف شده‌اند» ولی تعاریف در خلاصه
نیامده. بدون آن پیاده‌سازی حدس خواهد بود.

لطفاً برای هر گذار مشخص کنید ساعت **حفظ** شود یا **ریست**:

| # | گذار | حفظ / ریست / پاک |
|---|---|---|
| ۱ | `CHAIN_WAITING → CHAIN_WAITING` (poll ناموفق) | ؟ |
| ۲ | `CHAIN_WAITING → WAITING_BOT` | ؟ |
| ۳ | `WAITING_BOT → WAITING_BOT` (legacy poll) | ؟ |
| ۴ | `WAITING_BOT → BOT_RESPONSE → WAITING_BOT` | ؟ |
| ۵ | `CHAIN_WAITING → CHAIN_STEP` (hop جدید) | ؟ |
| ۶ | `CHAIN_WAITING → CHAIN_FAILED` | ؟ |
| ۷ | `WAITING_BOT → ERROR_BOT_TIMEOUT` | ؟ |
| ۸ | requeue دستی کاربر | ؟ |
| ۹ | callback جدید / deep-link جدید | ؟ |
| ۱۰ | خروج به هر حالت غیرانتظار | ؟ |

---

## ۳) چهار سؤال کوتاه

### ۳-۱) `ERROR_BOT_TIMEOUT`
از `const WAITING` خارج شود، یا داخل بماند با مهلت جداگانه‌ی
طولانی‌تر؟ ترجیح شما؟

### ۳-۲) تقسیم دو مرحله‌ای
جمله‌ی پایانی شما بود:

> «باید timeout به CAS/claim معتبر منتقل و state writeهای waiting در
> call-siteهای Chain، Collector، Matcher، Action و Recovery
> مالکیت‌محور شوند.»

این پنج فایل و چند نقطه‌ی نوشتن است. قید «بازطراحی Worker ممنوع»
داریم. پیشنهاد ما:

- **۱۰.۱۲.۲۲** — ستون + ثبت ساعت + انتقال deadline به CAS
- **۱۰.۱۲.۲۳** — مالکیت‌محور کردن call-siteها، با آزمون مستقل

موافقید، یا نصفه‌کاره بودنش خطرناک‌تر از یک‌جا انجام دادن است؟

### ۳-۳) ایندکس
`(state, waiting_since, id)` را «در صورت نیاز» گفتید. جدول ~۵۸۰۰
ردیف دارد. نظر ما: لازم نیست. تأیید یا رد؟

### ۳-۴) مبنای UTC
`UTC_TIMESTAMP()` در SQL یا `gmdate()` در PHP؟ هر دو را نام برده‌اید؛
برای اجرا باید یکی انتخاب شود.

---

## ۴) ★ اولویت ۱ — هنوز بی‌پاسخ

این مهم‌ترین بخش این سند است.

`waiting_since` مسئله‌ی ‎#68 و ‎#69 را حل می‌کند. ولی خط لوله را
**راه نمی‌اندازد**، چون:

```
06:08:08  PICK: selected=6 ids=[68,69,70,78,79,80]
06:08:27  STAGE #80 from=SCANNED to=SCANNED result=skipped
06:08:27  STAGE #79 from=SCANNED to=SCANNED result=skipped
06:08:27  STAGE #68 from=CHAIN_WAITING to=CHAIN_WAITING result=waiting
```

**«مرحله جلو رفت» = ۰.** سه روز پیاپی.

و `SCANNED` اصلاً در `const WAITING` نیست
(`class-gs-auto-worker.php:66`) — پس **هیچ‌کدام از کارهای اولویت ۲
شاملش نمی‌شود**. حتی با `waiting_since` کامل، ‎#79 و ‎#80 در همان
حلقه می‌مانند.

بخش ۲ سند `DARKHAST-RAHEHAL.md` تحلیل کامل با شماره‌خط دارد. خلاصه:
پنج مسیر در `chain-engine::init()` مقدار `no_progress => true`
برمی‌گردانند؛ سه‌تایشان `fallback_to_legacy()` صدا می‌زنند که فقط
`chain_mode` را عوض می‌کند و **state را `SCANNED` نگه می‌دارد**.

سه احتمال مطرح کردیم و نمی‌دانیم کدام درست است:

- **الف)** Session به legacy می‌رود ولی دور بعد باز `init()` صدا زده
  می‌شود و در شرط `MODE_LEGACY` (خط ۱۲۸) گیر می‌کند
- **ب)** `Button_Resolver::resolve()` واقعاً صدا زده می‌شود ولی خودش
  `skipped` برمی‌گرداند (شرط `PAST_STATES`، خط ۵۷-۶۲)
- **ج)** هر ۱۰۷ ردیف واقعاً «گره غیرقابل اجرا» دارند و رفتار فعلی
  **درست** است — یعنی مشکل در کیفیت اسکن است، نه Worker

اگر برای تشخیص به شاهد نیاز دارید، **دقیقاً بگویید کدام لاگ یا کدام
فیلد** — از پنل می‌گیریم. (مالک برنامه‌نویس نیست؛ SQL دستی یا SSH
ممکن نیست، ولی خروجی پنل و لاگ در دسترس است.)

---

## ۵) اولویت‌های ۳ و ۴

عجله نداریم، ولی باز هستند:

- **۳** — ایمن‌سازی ابزار تشخیص: `@fopen` در
  `class-gs-env-diag.php:406`، مسیر cgroup v2 (خطوط ۱۲۸-۱۳۰)، برش
  ۴۰۰/۱۵۰۰ در `class-sti-mtproto.php:770,778,780`
- **۴** — طرح جمع‌آوری داده‌ی Fiber بدون تحریک خطا

جزئیات در بخش‌های ۴ و ۵ سند اول.

---

## ۶) یک الگو که ثبت کردیم

در سند اول نوشتیم الگوی تکراری خطای ما این است:

> انتخاب یک منبع زمانی که خودِ چرخه‌ی اندازه‌گیری آن را بازنویسی
> می‌کند.

یافته‌ی شما نشان داد یک الگوی خواهر هم دارد:

> **نوشتن در یک ردیف، از داخل تابعی که طراحی‌اش صریحاً بدون قفل است —
> و کامنت خودش همین را می‌گوید.**

هر دو یک ریشه دارند: **فرض کردن به‌جای خواندن قرارداد تابع میزبان.**

اگر در طراحی نهایی جایی می‌بینید که ما دوباره در همین دام می‌افتیم،
لطفاً صریح علامت بزنید.
