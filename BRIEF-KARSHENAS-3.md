# بریفینگ کارشناس سوم — Golden Importer

**نسخه:** 10.12.21 · **تاریخ:** ۱۱ سپتامبر ۲۰۲۶
**بسته:** `golden-importer-10.12.21.zip` · ۱۳۲ فایل
sha256 `1b9c89dbf10ec03ad1309a4b3a4deb8b74692626afc2c1a3f7235d64886dcdfd`

> شما سومین کارشناسی هستید که این پرونده را می‌بیند. دو نفر قبلی
> نتایج ارزشمندی دادند و **دامنه‌شان محدود بود**. این سند تمام آنچه
> می‌دانیم را یکجا می‌دهد، از جمله اشتباهات خودمان.
>
> **آنچه از شما می‌خواهیم در بخش ۸ است.** اگر وقت محدودی دارید،
> بخش‌های ۲ و ۸ کافی است.

---

## ۱) افزونه در یک نگاه

کانال‌های تلگرام را اسکن می‌کند، فایل‌ها را (که معمولاً پشت یک ربات
واسط و زنجیره‌ای از دکمه‌ها هستند) پیدا و دانلود می‌کند، و محصول
ووکامرس می‌سازد.

| لایه | جزئیات |
|---|---|
| بستر | وردپرس + ووکامرس |
| PHP | ۸.۴.۲۳ روی LiteSpeed (lsphp پایا) |
| تلگرام کاربر | MadelineProto به‌صورت phar (۱۹.۵ MB) |
| پایگاه داده | جداول `sti_gs_*` |
| زمان‌بندی | **WP-Cron — بازدید‌محور، نه cron سیستمی** |
| حجم | ۱۱۴ فایل PHP |

**ماشین حالت:**
```
SCANNED → FILE_MATCHED → DOWNLOADING → STORED
        → MEDIA_BUILDING → PRODUCT_BUILDING → REVIEW_READY → PUBLISHED
```

```php
// class-gs-auto-worker.php:63
const TERMINAL = array( 'REVIEW_READY', 'PUBLISHED', 'SKIPPED',
                        'NEEDS_REVIEW', 'ERROR_FILE_NOT_FOUND', 'DEAD_LETTER' );
// class-gs-auto-worker.php:66
const WAITING  = array( 'WAITING_BOT', 'ERROR_BOT_TIMEOUT', 'CHAIN_WAITING' );
```

**میزبان:** cPanel · `memory_limit` ۲۸۰M (افزونه خودش به ۵۱۲M
می‌بردش) · `proc_open` فعال · **`exec`/`shell_exec` غیرفعال** ·
مالک SSH ندارد و **برنامه‌نویس نیست**.

---

## ۲) ★ مسئله‌ی اصلی — خط لوله متوقف است

### ۲-۱) علامت

سه روز پیاپی، شمارنده‌ی «مرحله جلو رفت» = **۰**.

آخرین دامپ میزبان (`2026-09-11 06:11:35`):
```
06:08:08  AUTO_WORKER_PICK: selected=6 ids=[68,69,70,78,79,80]
06:08:27  STAGE #80 from=SCANNED       to=SCANNED       result=skipped
06:08:27  STAGE #79 from=SCANNED       to=SCANNED       result=skipped
06:08:27  STAGE #68 from=CHAIN_WAITING to=CHAIN_WAITING result=waiting
```

شش Session انتخاب شدند، **هیچ‌کدام جلو نرفتند**.

صف: `eligible_queue = 107`. نمونه‌ی صف از همان دامپ:

| id | state | attempts |
|---|---|---|
| 68, 69 | `CHAIN_WAITING` | 0 |
| 70, 78 | `WAITING_BOT` | 1 |
| 81, 82, 86, 87, 89, 90 | `SCANNED` | 0 |

### ۲-۲) دو حلقه‌ی جداگانه

**حلقه‌ی الف — `CHAIN_WAITING` (‎#68، ‎#69):** بیش از ۸۰ ساعت
منتظر با `attempts=0`. مکانیزم اثبات شده (بخش ۳).

**حلقه‌ی ب — `SCANNED → skipped` (‎#79، ‎#80، …):** ریشه‌یابی نشده.

در `class-gs-chain-engine.php::init()` پنج مسیر `no_progress => true`
برمی‌گردانند:

| خط | شرط | اقدام جانبی |
|---|---|---|
| ۱۲۲ | `state !== 'SCANNED'` | — |
| ۱۲۸ | `mode === MODE_LEGACY` | — |
| ۱۳۴ | پیام مبدأ در `sti_gs_messages` نیست | `fallback_to_legacy()` |
| ۱۴۰ | `raw_json` قابل decode نیست | `fallback_to_legacy()` |
| ۱۵۴ | گره قابل اجرا نیست (`ASSET`/`UNKNOWN`) | `fallback_to_legacy()` |

و `fallback_to_legacy()` (خطوط ۱۳۷۸-۱۳۸۶) فقط `chain_mode` را عوض
می‌کند — **state دست‌نخورده `SCANNED` می‌ماند**:
```php
STI_GS_Session::update( $session_id, array(
    'chain_mode'   => STI_GS_Node::MODE_LEGACY,
    'stage'        => 'chain_engine',
    'error_reason' => null,
) );
```

سپس `class-gs-auto-worker.php:989`:
```php
if ( is_array( $result ) && ! empty( $result['no_progress'] ) ) {
    $outcome = ! empty( $result['waiting'] ) ? 'waiting' : 'skipped';
}
```

`skipped` یعنی: `attempts` بالا نمی‌رود، state ثابت، و **برخلاف حالت
انتظار هیچ مهلتی ندارد**.

### ۲-۳) ⭐ یافته‌ی تازه — شمارنده‌ها دروغ می‌گویند

**این را همین امروز، حین آماده‌سازی همین سند پیدا کردیم.**

`class-gs-auto-worker.php:564`:
```php
$report = array( 'advanced' => 0, 'waiting' => 0, 'failed' => 0, 'completed' => 0 );
```

**کلید `skipped` وجود ندارد.** و خط ۶۷۲:
```php
if ( isset( $report[ $outcome ] ) ) {
    $report[ $outcome ]++;
}
```

نتیجه: وقتی `$outcome === 'skipped'` است، شرط `isset` رد می‌شود و
**هیچ شمارنده‌ای بالا نمی‌رود**. رویداد در هیچ آماری ثبت نمی‌شود.

از طرف دیگر پنل «منتظر پاسخ ربات = ۶» نشان می‌دهد که از
`$today['waiting']` می‌آید (`worker.php:598`) — و آن شمارنده در
خطوط ۶۰۹/۶۱۳/۶۱۷ برای دلایل کاملاً متفاوتی (فشار Governor، سقف
دانلود، سقف محصول) هم بالا می‌رود.

**پیامد:** پنل شش Session را «منتظر ربات» نشان می‌دهد در حالی که
دست‌کم دوتایشان `SCANNED` بودند و اصلاً منتظر ربات نبودند — بلکه
`skipped` شده بودند.

⚠️ **این یعنی هر تحلیلی که ما یا دو کارشناس قبلی بر پایه‌ی این
شمارنده انجام داده‌ایم مشکوک است.** لطفاً به این اعداد اعتماد نکنید.

### ۲-۴) سه احتمال — نمی‌دانیم کدام

- **الف)** Session به `chain_mode=legacy` می‌رود ولی دور بعد باز
  `init()` صدا زده می‌شود و در شرط خط ۱۲۸ گیر می‌کند ⇒ حلقه‌ی ابدی
- **ب)** `Button_Resolver::resolve()` واقعاً صدا زده می‌شود ولی خودش
  `skipped` برمی‌گرداند (`class-gs-button-resolver.php:57-62`)
- **ج)** هر ۱۰۷ ردیف واقعاً «گره غیرقابل اجرا» دارند ⇒ **رفتار فعلی
  درست است** و مشکل در کیفیت اسکن یا فیلتر پروفایل است

مسیریابی حالت (`class-gs-session-ajax.php:392`):
```php
'SCANNED' => array( 'Resolve Button', array( 'STI_GS_Button_Resolver', 'resolve' ) ),
```
ولی docblock خطوط ۳۸۲-۳۸۶ می‌گوید تصمیم به `chain_mode` خودِ Session
بستگی دارد: `auto`/`chain` ⇒ Chain Init، `legacy` ⇒ Resolver قدیمی.

---

## ۳) مسئله‌ی دوم — گرسنگی Worker (اثبات‌شده)

### ۳-۱) مکانیزم

پرس‌وجوی انتخاب (`class-gs-auto-worker.php:375-403`):
```sql
WHERE state NOT IN (<۶ حالت پایانی>)
  AND (locked_until  IS NULL OR locked_until  < NOW())
  AND (next_retry_at IS NULL OR next_retry_at <= NOW())
ORDER BY (attempts >= 5) ASC, priority DESC, id ASC
```

`class-gs-chain-engine.php:1332` برمی‌گرداند
`array('state'=>'CHAIN_WAITING','waiting'=>true)` بدون نوشتن
`next_retry_at`؛ بلافاصله `finally` خط ۱۳۳۳ تابع `release()` را صدا
می‌زند که `locked_until` را `NULL` می‌کند
(`class-gs-session.php:188`).

⇒ هر دو شرط `NULL`، `id ASC` همان Session را برمی‌دارد.
**حدود ۱۹۰ دور پیاپی `ids=[68]`.**

### ۳-۲) زنجیره‌ی علّی پذیرفته‌شده

```
گرسنگی Worker → Session جلو نمی‌رود → صف انتشار رشد می‌کند
              → backlog=109 → Watcher ورودی را می‌بندد
```
**Watcher معلول است، نه علت.** «Watcher علت است» و «صف انتشار علت
است» هر دو **رد شده‌اند**.

### ۳-۳) چهار نسخه، چهار نتیجه

| نسخه | اقدام | نتیجه روی میزبان |
|---|---|---|
| 10.12.18 | مهلت از روی ثابت قفل | ❌ بی‌اثر |
| 10.12.19 | عقب‌گرد حالت | 🔶 نسبی |
| 10.12.20 | `next_retry_at` با backoff per-state | ✅ مکانیزم اثبات شد |
| 10.12.21 | اصلاح دو نقص | ✅ نصب شد — بخش ۳-۴ |

### ۳-۴) وضعیت فعلی ۱۰.۱۲.۲۱ روی میزبان

| سنجه | قبل | بعد |
|---|---|---|
| `interval` | ۳۰۰ ثانیه (عدد تنظیمات) | **۸۹۶ ثانیه (واقعی)** |
| `selected` | ۱ | **۶** |
| صف در دسترس | فقط `[68]` | `[68,69,70,78,79,80]` |
| **مرحله جلو رفت** | ۰ | **۰** ← تغییری نکرد |

اندازه‌گیری درست شد، صف باز شد، **ولی هیچ Sessionای جلو نمی‌رود.**

---

## ۴) مسئله‌ی سوم — ساعت انتظار

`WAITING_DEADLINE = 12 ساعت` در ۱۰.۱۲.۲۱ اضافه شد ولی **هرگز اجرا
نشد**. علت: مهلت را از `updated_at` می‌سنجد
(`class-gs-auto-worker.php:853`) در حالی که:

- `claim()` هر تصاحب `updated_at` را تازه می‌کند
  (`class-gs-session.php:167`)
- `update()` هم همین‌طور (خط ۱۵۵)
- `chain-engine` در چهار نقطه هر تیک `claim()` می‌زند
  (خطوط ۱۱۲، ۳۰۱، ۵۱۰، ۷۸۲)

کارشناس دوم دو ایراد **بزرگ‌تر** پیدا کرد:

**الف) نوشتن بدون مالکیت.** بلوک مهلت در خطوط ۸۵۲-۸۷۵ است. هشتاد خط
پایین‌تر، خط ۹۳۰، کامنت خودِ کد می‌گوید:

> «عمداً اینجا قفل گرفته نمی‌شود. هر هفت موتور … خودشان
> `STI_GS_Session::claim()` را صدا می‌زنند.»

یعنی `advance_one()` عامدانه بدون قفل طراحی شده و ما یک نوشتنِ
تغییردهنده‌ی state داخلش گذاشته‌ایم.

**ب) `update()` هیچ شرط مالکیت ندارد** — فقط `WHERE id`:
```php
// class-gs-session.php:156
return $wpdb->update( self::table(), $row, array( 'id' => (int) $id ) );
```

و TTLهای قفل از **۴۵ تا ۶۰۰ ثانیه** متغیرند، بدون هیچ سازوکار
renew (`grep` برای `renew|extend_lock` ⇒ صفر نتیجه).

**راه‌حل پذیرفته‌شده (اجرا نشده):** ستون `waiting_since DATETIME NULL`،
بدون backfill، UTC، با CAS. جزئیات episodeها هنوز از کارشناس دوم
دریافت نشده.

---

## ۵) مسئله‌ی چهارم — Fiber/mmap (اولویت پایین)

```
Fiber stack allocate failed: mmap failed: Cannot allocate memory (12)
```

از `class-sti-mtproto.php:689` هنگام ساخت client.

**آنچه قطعی است** (از `Zend/zend_fibers.c`): یک `mmap` خام است، نه
`emalloc` ⇒ `memory_limit` نه علت است نه درمان. هر Fiber دو VMA
می‌سازد. تخصیص تنبل است.

**چهار فرضیه‌ی باز:** سقف `vm.max_map_count` · `RLIMIT_DATA` ·
`RLIMIT_AS`/LVE · overcommit یا cgroup.

**وضعیت:** `ROOT_CAUSE_UNKNOWN`. از ۱۰.۱۲.۱۷ تاکنون **هیچ خطای تازه‌ای
رخ نداده** — بار تشخیصی فقط در لحظه‌ی شکست نوشته می‌شود، پس اعداد
میزبان هرگز ثبت نشده‌اند.

⛔ **`Tools::testFibers()` را اجرا نکنید.** بدنه‌اش را از phar
استخراج کردیم: `ini_set('memory_limit',-1)` و حلقه‌ی صد هزار Fiber تا
لحظه‌ی شکست. آزمون فشار است، نه تشخیص. فقط `getMaps()` و
`getMaxMaps()` امن‌اند.

---

## ۶) مسئله‌ی پنجم — ۸۲۵ رکورد یتیم

| مورد | مقدار |
|---|---|
| کل واجد شرایط | ۵۸۱۰ |
| **یتیم** | **۸۲۵** |
| ۵۰۰ کاندیدای اول | ۱۰۰٪ `NO_PK` |
| اولین شناسه‌ی معتبر | `23339` — رتبه‌ی ۸۲۶ |

همه: `profile_id=1`، کلیدواژه‌ی `Mockup`، تاریخ یکسان
`2026-08-15 12:45:48`. دست‌نخورده.

---

## ۷) اشتباهات ما — لطفاً بخوانید

این بخش را عمداً نوشته‌ایم. **الگوی تکرارشونده‌ی خطای ما** در چهار
نسخه‌ی متوالی:

> **انتخاب یک منبع زمانی که خودِ چرخه‌ی اندازه‌گیری آن را بازنویسی
> می‌کند.**

سه بار: ثابت قفل (۱۰.۱۲.۱۸) ← دوره‌ی تنظیم‌شده (۱۰.۱۲.۱۹) ←
`updated_at` (۱۰.۱۲.۲۱).

و یک الگوی خواهر که کارشناس دوم نشانمان داد:

> **نوشتن در یک ردیف، از داخل تابعی که طراحی‌اش صریحاً بدون قفل است —
> و کامنت خودش همین را می‌گوید.**

هر دو یک ریشه دارند: **فرض کردن به‌جای خواندن قرارداد تابع میزبان.**

سه درس دیگر:

- **کامنت، اثبات وجود کد نیست.** یک پاراگراف کامل درباره‌ی «مهلت
  انتظار» در کد بود که **هیچ کدی نداشت**؛ چند دور فرض کردیم موجود
  است.
- **شمارنده‌های UI می‌توانند دروغ بگویند** — بخش ۲-۳ را ببینید.
- **تست سبز اثبات درستی نیست.** چهار نسخه با تست‌های کامل منتشر شد و
  سه‌تایشان روی میزبان کم‌اثر بودند.

### آنچه قبلاً رد شده — تکرار نکنید

| مسیر | چرا |
|---|---|
| بالا بردن `memory_limit` | خطا از `mmap` خام است |
| رشته‌گردی روی متن exception | اثبات‌شده غیرقابل‌اتکا |
| `@file_get_contents` در runtime | `Magic::start()` warning را به exception تبدیل می‌کند |
| بالا بردن `sessions_per_tick` | `id ASC` + قفل تک‌ربات خنثی‌اش می‌کند |
| Watcher به‌عنوان درمان | معلول است |
| `Tools::testFibers()` | آزمون فشار — خطرناک |
| «صف را خالی کن و از نو شروع کن» | مسئله را پنهان می‌کند |

دو ادعای بسته‌شده که **دوباره طرح نکنید**:
- «سیستم قفل بی‌اثر است» — **غلط**، `claim()` با CAS اتمیک، ۱۶ محل
- «`selected=1` یعنی منطق دسته‌ای خراب است» — **غلط**، فیلتر
  `next_retry_at` درست عمل می‌کند

---

## ۸) ★ آنچه از شما می‌خواهیم

### ۸-۱) اولویت یک — چرخه‌ی `SCANNED → skipped`

**مهم‌ترین سؤال کل پرونده.**

1. کدام‌یک از سه احتمال بخش ۲-۴ درست است؟
2. اگر «الف»: تعویض `chain_mode` به `legacy` باید Session را به کدام
   مسیر بفرستد، و چطور تضمین کنیم دوباره به `init()` برنگردد؟
3. **آیا `skipped` باید بی‌نهایت تکرارشدنی بماند؟** حالت انتظار حالا
   مهلت دارد، `skipped` ندارد. اگر مهلت لازم است، شمارنده‌اش کجا
   ذخیره شود؟ (`attempts` را عمداً دست نمی‌زنیم.)
4. `SCANNED` در `const WAITING` نیست. اضافه شود یا سازوکار جدا؟

اگر برای پاسخ شاهد لازم دارید، **دقیقاً بگویید کدام لاگ یا فیلد** —
از پنل می‌گیریم.

### ۸-۲) باگ رصدپذیری (بخش ۲-۳)

`skipped` در `$report` نیست پس شمرده نمی‌شود، و `waiting` سه دلیل
متفاوت را با هم جمع می‌کند.

- آیا صرفاً افزودن کلید `skipped` کافی است؟
- آیا `waiting` باید به دلایلش تفکیک شود؟
- **این را قبل از هر اصلاح دیگری انجام دهیم یا بعدش؟** (استدلال ما:
  قبل — وگرنه اثر اصلاح بعدی را نمی‌توانیم بسنجیم.)

### ۸-۳) نظر مستقل درباره‌ی اولویت‌بندی

ترتیب فعلی ما:
```
۱) SCANNED/skipped   ۲) waiting_since   ۳) ابزار تشخیص   ۴) Fiber
```
موافقید؟ اگر نه، دلیلتان برای ما ارزشمندتر از موافقت است.

### ۸-۴) نگاه تازه

دو کارشناس قبلی دامنه‌ی محدود داشتند. **شما محدودیت دامنه ندارید.**

اگر چیزی می‌بینید که ما و آن‌ها ندیده‌ایم — حتی اگر خارج از پنج
مسئله‌ی بالا باشد — بگویید. مخصوصاً:

- آیا معماری فعلی (Chain Engine + Legacy Resolver کنار هم) قابل
  نگهداری است؟
- آیا جایی هست که دو سازوکار موازی یک کار را انجام می‌دهند؟
- آیا `no_progress` به‌عنوان مفهوم، طراحی درستی است؟

---

## ۹) محدودیت‌ها

**داده:** ❌ حذف Session · پاک کردن backlog · تغییر مخرب DB · ریست
حساب یا حذف فایل session تلگرام

**معماری:** ❌ بازطراحی Worker · تغییر MTProto/Fiber · تغییر سقف
تلاش · بالا بردن `backlog_limit` · فعال کردن Watcher به‌عنوان درمان

**روش:** ❌ ویرایش `wp-config.php` · هیچ warning/notice/fatal در UI
یا JSON · حلقه‌ی retry بی‌پایان · polling سنگین AJAX

**آزمون‌های شما باید فقط‌خواندنی باشند.** اگر برای اثبات فرضیه‌ای
نیاز به نوشتن است، اول بگویید.

---

## ۱۰) روش کاری مورد انتظار

1. **علامت ≠ ریشه**
2. **هر ادعا با ارجاع** — فایل + متد + شماره‌ی خط
3. **حداقل سه فرضیه‌ی جایگزین** قبل از انتخاب یکی
4. **درجه‌ی اطمینان** — «قطعی / محتمل / ممکن»، هرگز «۱۰۰٪»
5. **آنچه قابل مشاهده نیست ⇒ `UNOBSERVABLE`**، حدس نزنید
6. **پایان با** `ROOT CAUSE PROVEN` یا `ROOT CAUSE NOT YET PROVEN`

و یک قاعده از تجربه‌ی تلخ خودمان: **کامنت داخل کد، اثبات وجود آن
رفتار نیست.** با `grep` تأیید کنید.

---

## ۱۱) شکل پاسخ

برای هر مسئله:

### `تشخیص`
آیا تحلیل ما درست است؟ اگر نه، کجا اشتباه کردیم — با شماره‌خط.

### `طراحی`
کدام فایل، کدام تابع، چه رفتاری. شبه‌کد کافی است.

### `ریسک‌ها`
رقابت هم‌زمانی، Sessionهای قدیمی، timezone، نصب تازه.

### `آزمون`
⚠️ **محیط ما PHP ندارد** — اعتبارسنجی ما فقط بازرسی کد و تست ایستای
متنی است. اگر آزمونی نیاز به اجرای واقعی دارد، بگویید تا از مالک
بخواهیم روی میزبان تأیید کند.

---

## ۱۲) فایل‌های کلیدی

| اولویت | مسیر | چرا |
|---|---|---|
| **۱** | `includes/golden-scan/class-gs-chain-engine.php` L108-160, 1378 | پنج مسیر `no_progress` + `fallback_to_legacy` |
| **۱** | `includes/golden-scan/class-gs-auto-worker.php` L564, 672, 989 | باگ شمارنده + نگاشت `outcome` |
| **۱** | `includes/golden-scan/class-gs-session-ajax.php` L382-393 | مسیریابی `SCANNED` |
| ۲ | `includes/golden-scan/class-gs-session.php` L155, 167, 188 | `update`/`claim`/`release` |
| ۲ | `includes/golden-scan/class-gs-auto-worker.php` L852-875, 930 | مهلت انتظار + کامنت «بدون قفل» |
| ۳ | `includes/golden-scan/class-gs-env-diag.php` L98-140, 406 | `@fopen` + مسیر cgroup |
| ۴ | `includes/class-sti-mtproto.php` L689, 770-780 | نقطه‌ی پرتاب + برش شواهد |

**اسناد همراه:** `BRIEF-KARSHENAS.md` (بریفینگ اول، مفصل‌تر درباره‌ی
Fiber) · `DARKHAST-RAHEHAL.md` + `-2.md` (درخواست راه‌حل) ·
`ARZYABI-*.md` (ارزیابی ما از دو گزارش قبلی) ·
`GUZARESH-10.12.21.md` (گزارش آخرین نسخه)

---

## ۱۳) اگر فقط یک ساعت وقت دارید

۱. بخش ۲ را بخوانید (حلقه‌ی `SCANNED → skipped`)
۲. سه فایل اولویت ۱ جدول بالا را ببینید
۳. به سؤال ۸-۱ پاسخ دهید — کدام‌یک از سه احتمال؟

بقیه می‌تواند منتظر بماند. **بدون حل این، خط لوله راه نمی‌افتد.**
