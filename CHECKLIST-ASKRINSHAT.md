# چک‌لیست اسکرین‌شات — فقط با پنل فعلی، بدون هیچ تغییر کد

> شما فقط عکس می‌فرستید. تحلیل با من.
> ترتیب مهم است: هر مرحله ممکن است بقیه را بی‌نیاز کند.
> اگر در هر مرحله‌ای علت قطعی شد، همان‌جا می‌ایستیم.

---

## 🔴 خبر خوب: یک راه غیرمستقیم برای دیدن حالت A پیدا کردم

در پیام قبلی گفتم حالت `REVIEW_READY + active` در پنل دیده نمی‌شود. آن حرف درباره‌ی **شمارش مستقیم** درست بود، ولی بعد از بررسی دقیق‌تر یک راه غیرمستقیم پیدا کردم:

جدول **«۱۰ مورد بعدی زمان‌بندی‌شده»** در صفحه‌ی صف انتشار، کوئری‌اش این است (`publish-queue.php:63-71`):

```
WHERE scheduled_at IS NOT NULL AND state NOT IN ('PUBLISHED','REVIEW','CANCELLED')
```

**این جدول روی `queue_status` فیلتر نمی‌کند و ستون `state` را هم چاپ می‌کند.** یعنی مقایسه‌ی این جدول با عدد «در صف» یک تست تشخیصی واقعی به ما می‌دهد. جزئیاتش در مرحله‌ی ۲.

پس بله — می‌توانیم دامنه را خیلی بیشتر از چیزی که فکر می‌کردم کوچک کنیم.

---

# مرحله ۱ — صفحه‌ی «📦 صف انتشار»

**مسیر:** گلدن اسکن → صف انتشار

### چه چیزی در عکس باشد
۱. **بالای صفحه**: کلید روشن/خاموش صف انتشار (و هر متنی کنارش)
۲. **بخش «۳) برنامه‌ی انتشار»**: هر پنج عدد — در صف / منتشر شده / خطا / نوبت بعدی / فاصله (دقیقه)
۳. **جدول «۱۰ مورد بعدی زمان‌بندی‌شده»** — کامل، با ستون وضعیت. اگر نوشته «هنوز مورد زمان‌بندی‌شده‌ای نیست»، از همان جمله عکس بگیرید.

> اگر همه در یک صفحه جا نشد، دو عکس بفرستید. جدول را حتماً کامل بگیرید — همان تشخیصی‌ترین بخش است.

### چه چیزی رد یا تأیید می‌شود

| آنچه می‌بینیم | نتیجه |
|---|---|
| کلید صف **خاموش** | ✅ **علت پیدا شد و همین‌جا تمام می‌شویم.** Worker روشن بودن بی‌ربط است — دو کلید کاملاً مستقل‌اند (`sti_gs_worker_enabled` در برابر `sti_gs_queue_running`) |
| «در صف» = ۰ **و** جدول ۱۰تایی هم خالی | فرضیه‌ی enqueue-نشدن **قوی** می‌شود: هیچ Sessionی نه `queued` است نه `scheduled_at` دارد |
| «در صف» = ۰ **ولی** جدول ۱۰تایی پر است و وضعیت‌ها `REVIEW_READY` هستند | 🎯 **اثبات مستقیم حالت A.** یعنی ردیف‌هایی با `scheduled_at` وجود دارند که `queued` نیستند — دقیقاً همان رهاشده‌ها |
| «در صف» > ۰ **و** «نوبت بعدی» گذشته است ولی چیزی منتشر نمی‌شود | صف کار می‌کند ولی `tick()` اجرا نمی‌شود ⇒ می‌رویم سراغ کران (مرحله ۴) |
| «در صف» > ۰ و «نوبت بعدی» در آینده | صف **سالم** است، فقط نوبتش نرسیده. اگر «فاصله» عدد بزرگی باشد، همان کندی را توضیح می‌دهد |
| «خطا» > ۰ | این‌ها هرگز خودشان دوباره تلاش نمی‌کنند (Retry خودکار وجود ندارد) |
| «منتشر شده» > ۰ | ⚠️ **مهم:** یعنی این مسیر قبلاً کار کرده. پس مشکل «هرگز کار نکرده» نیست، «متوقف شده» است |

---

# مرحله ۲ — صفحه‌ی «اتوماسیون»

**مسیر:** گلدن اسکن → اتوماسیون

### چه چیزی در عکس باشد
۱. **کارت هفت‌تایی** با هر هفت عدد: ⏳ منتظر پردازش · ⬇️ در حال دانلود · 🏗️ در حال ساخت محصول · 📦 **در صف انتشار** · ✅ منتشر شده · 🟡 نیازمند بازبینی · 🔴 خطا
۲. **بالای صفحه**: وضعیت خط (Line) — همان نقطه‌ی رنگی و برچسبش

### چه چیزی رد یا تأیید می‌شود

این کارت گروه‌های D تا H را پوشش می‌دهد:

| سطل | شامل کدام گروه شما |
|---|---|
| ⏳ منتظر پردازش | **D (WAITING_BOT)** · **E (CHAIN_WAITING)** · **F (DOWNLOADED)** و چند تای دیگر |
| 📦 در صف انتشار | **A + B + C** (چون `PRODUCT_READY` و `REVIEW_READY` را با هم جمع می‌زند) |
| 🔴 خطا | **G (PRODUCT_FAILED)** و بقیه‌ی خطاها |
| 🟡 نیازمند بازبینی | NEEDS_REVIEW و فایل‌یافت‌نشده |

**تست تعیین‌کننده — همین‌جا کل ماجرا روشن می‌شود:**

عدد «📦 در صف انتشار» این صفحه را با عدد «در صف» مرحله‌ی ۱ مقایسه کنید:

- **📦 بزرگ (مثلاً ۴۰) ولی «در صف» = ۰** ⇒ 🎯 **اثبات حالت A.** یعنی ده‌ها Session به REVIEW_READY رسیده‌اند ولی هیچ‌کدام وارد صف نشده‌اند. این دقیقاً همان حفره‌ای است که در کد ثابت کردم.
- **📦 و «در صف» تقریباً برابر** ⇒ enqueue درست کار می‌کند؛ مشکل جای دیگری است (کران یا فاصله یا سقف روزانه).
- **📦 = ۰** ⇒ اصلاً چیزی به REVIEW_READY نمی‌رسد ⇒ **مشکل قبل از Publish Queue است** و باید در ⏳ یا 🔴 دنبالش بگردیم.

> این مقایسه‌ی دو عدد ساده، همان کاری را می‌کند که فکر می‌کردم فقط با SQL ممکن است.

---

# مرحله ۳ — صفحه‌ی «⚙️ Worker / پردازش»

**مسیر:** گلدن اسکن → Worker (یا از دکمه‌ی «⚙️ جزئیات پردازش» پایین صفحه‌ی صف انتشار)

### چه چیزی در عکس باشد
۱. کلید روشن/خاموش **Auto Worker**
۲. دو عدد بزرگ: **«در انتظار پردازش»** و **«نیازمند بازبینی»**
۳. بخش **«کلیدهای قابلیت»** (چک‌باکس‌ها) — کامل
۴. اگر جدول «آخرین خطاها» دارد، آن هم

### چه چیزی رد یا تأیید می‌شود

| آنچه می‌بینیم | نتیجه |
|---|---|
| «در انتظار پردازش» = ۰ | Worker کارش را **تمام کرده**. یعنی توپ در زمین Publish Queue است ⇒ فرضیه‌ی مرز A/B/C تقویت می‌شود |
| «در انتظار پردازش» > ۰ و ثابت می‌ماند | Worker **گیر کرده** ⇒ مشکل قبل از REVIEW_READY است، نه در صف |
| «نیازمند بازبینی» عدد بزرگ | بخشی از کار به بن‌بست انسانی خورده |
| کلیدهای قابلیت | می‌خواهم ببینم کدام‌ها خاموش‌اند — یکی‌شان ممکن است مسیر را بسته باشد |

**نکته‌ی مهم:** «در انتظار پردازش» عمداً REVIEW_READY را نمی‌شمارد (چون Worker آن را Terminal می‌داند). پس **صفر بودنش به معنی سلامت کل سیستم نیست** — فقط یعنی Worker دیگر کاری برای انجام دادن ندارد.

---

# مرحله ۴ — صفحه‌ی «بررسی سیستم»

**مسیر:** گلدن اسکن → بررسی سیستم

### چه چیزی در عکس باشد
کل صفحه، همه‌ی ردیف‌های 🟢🟡🔴. اگر بلند است، دو عکس.

### چه چیزی رد یا تأیید می‌شود

| ردیف | چه می‌گوید |
|---|---|
| **WP-Cron** | اگر 🟡 «غیرفعال است» ⇒ بدون کران سیستمی، **هیچ‌کدام** از دو بخش خودکار اجرا نمی‌شوند. این به‌تنهایی «اتوماسیون کار نمی‌کند» را توضیح می‌دهد |
| **کارهای زمان‌بندی‌شده گلدن اسکن** | عددش را می‌خواهم. اگر ۰ یا خیلی کم باشد، هوک‌ها ثبت نشده‌اند |
| **ورود اکانت (تلگرام)** | اگر `CLIENT_CONSTRUCTION_FAILED` دیدید، مشکل MTProto دوباره فعال است و باید جدا بررسی شود |
| **GET_LOCK دیتابیس** | اگر 🔴 باشد، قفل‌ها کار نمی‌کنند و `tick()` هیچ‌وقت آیتم را claim نمی‌کند |

---

# مرحله ۵ — صفحه‌ی «گزارش‌ها» (فقط اگر مراحل ۱ تا ۴ قطعی نشد)

**مسیر:** گلدن اسکن → گزارش‌ها

### چه چیزی در عکس باشد
۱. فیلتر سطح روی **error** — صفحه‌ی اول
۲. اگر جعبه‌ی جست‌وجو هست، این سه عبارت را جداگانه جست‌وجو کنید:
   - `ورود به صف ناموفق`
   - `ستون ناشناخته`
   - `PUBLISH_FAILED`

### چه چیزی رد یا تأیید می‌شود

| آنچه می‌بینیم | نتیجه |
|---|---|
| `ورود به صف ناموفق` پیدا شد | enqueue **اجرا شده ولی شکست خورده** — متن خطا علت دقیق را می‌گوید (معمولاً «محصول ندارد») |
| `ستون ناشناخته` پیدا شد | باگ قدیمیِ ستون گمشده برگشته |
| `PUBLISH_FAILED` پیدا شد | انتشار شروع شده ولی در ووکامرس شکست خورده |
| **هیچ‌کدام پیدا نشد** | 🎯 خودش شاهد است: enqueue **اصلاً اجرا نشده** — چون اگر اجرا و رد می‌شد، لاگ داشت. این حالت A را تأیید می‌کند |

---

# خلاصه: با این ۵ مرحله چه چیزی حل می‌شود

| احتمال | با کدام مرحله حذف/تأیید می‌شود |
|---|---|
| صف انتشار خاموش است | مرحله ۱ |
| Worker خاموش یا گیرکرده | مرحله ۳ |
| کران کار نمی‌کند | مرحله ۴ |
| قفل دیتابیس خراب | مرحله ۴ |
| MTProto دوباره شکسته | مرحله ۴ |
| سقف روزانه پر شده | مرحله ۱ |
| فاصله‌ی انتشار خیلی زیاد | مرحله ۱ |
| enqueue اجرا شده ولی شکست خورده | مرحله ۵ |
| **حالت A: رهاشده در REVIEW_READY + active** | **مقایسه‌ی مرحله ۱ و ۲** |
| مشکل قبل از REVIEW_READY | مرحله ۲ و ۳ |

**ده احتمال، پنج اسکرین‌شات.** انتظار دارم در پایان یک یا دو احتمال باقی بماند، نه بیشتر.

---

## اگر می‌خواهید فقط دو عکس بفرستید

اگر وقت کم دارید، **مرحله ۱ و مرحله ۲** را بفرستید. آن مقایسه‌ی دو عدد (📦 در برابر «در صف») به‌تنهایی بزرگ‌ترین سؤال را جواب می‌دهد.

---

## تعهد من

- **هیچ کدی تغییر نمی‌دهم**، هیچ صفحه‌ای اضافه نمی‌کنم، هیچ پچی نمی‌نویسم — تا وقتی خودتان بگویید.
- تا وقتی عدد واقعی نبینم: **Root Cause هنوز اثبات نشده است.**
- اگر داده‌ها به نتیجه‌ی قطعی نرسیدند، صریح می‌گویم «به بن‌بست رسیدیم» و آن‌وقت درباره‌ی قدم بعدی تصمیم می‌گیریم — نه زودتر.
