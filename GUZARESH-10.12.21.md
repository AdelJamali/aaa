# گزارش ۱۰.۱۲.۲۱ — منبع درست فاصله، و مهلتی که فقط کامنتش نوشته شده بود

> نسخه **10.12.21** · کد `c8d4b64` · ZIP `4c48cdb`
> `golden-importer-10.12.21.zip` · ۱۳۲ فایل · sha256 `1b9c89dbf10ec03ad1309a4b3a4deb8b74692626afc2c1a3f7235d64886dcdfd`
> منبع داده: دامپ میزبان `2026-09-11 05:55:26` (نسخه ۱۰.۱۲.۲۰ فعال، سه روز داده)

---

## ۱) خطای اصلی (Original Error)

بدون تغییر:

```
Fiber stack allocate failed: mmap failed: Cannot allocate memory (12)
```

**ROOT_CAUSE_UNKNOWN** — منتظر `/proc/self/maps` · `vm.max_map_count` · `RLIMIT_DATA`.

---

## ۲) ✅ آنچه کار کرد — مکانیزم اثبات شد

برای اولین بار بعد از حدود ۱۹۰ دور تکرار، **شناسه عوض شد**:

```
01:58:14  AUTO_WORKER_DEFER  session=#68  next=02:04:14
02:02:47  AUTO_WORKER_PICK   ids=[69]        ← عوض شد
02:03:34  AUTO_WORKER_DEFER  session=#69  next=02:09:34
```

و خطوط `AUTO_WORKER_DEFER` حالا در پنل **دیده می‌شوند** — اصلاح فیلتر
ابزار تشخیص هم جواب داد.

سازوکار خواباندن Session از سر تا ته درست کار می‌کند.

---

## ۳) ❌ نقص ۱ — فاصله‌ی واقعی هرگز ثبت نشد

لاگ بعد از سه روز هنوز می‌گوید:

```
DEFER ... delay=360s interval=300s factor=1.2
```

`interval=300` یعنی همان عدد تنظیمات. ولی فاصله‌های **واقعی** در همین دامپ:

| از | تا | فاصله |
|---|---|---|
| 03:01:12 | 04:33:06 | **۹۱ دقیقه** |
| 04:33:06 | 04:58:54 | ۲۶ دقیقه |
| 05:14:32 | 05:45:38 | ۳۱ دقیقه |

### ریشه

کد ۱۰.۱۲.۲۰ زمان تیک قبلی را از `STATS_KEY . '_last'` می‌خواند:

```php
$prev_tick = (int) get_option( self::STATS_KEY . '_last', 0 );   // ← خواندن
...
update_option( self::STATS_KEY . '_last', time(), false );        // ← نوشتن، چند خط پایین‌تر
```

اما آن option **در همان تابع و چند خط پایین‌تر** نوشته می‌شود. یعنی هرگز
مقدار تیکِ *قبلی* را نداشت. `OBSERVED_GAP_KEY` صفر ماند و
`effective_interval()` همیشه به عدد تنظیمات برمی‌گشت.

### اصلاح

منبع درست از قبل موجود بود: ردیف `sti_gs_gate_auto_worker` که
`Cron_Gate::pass()` با یک CAS اتمیک نگه می‌دارد و **دقیقاً** timestamp
تیک قبلی است.

```php
/* قبل از pass()، چون pass() مقدار را به «الان» تغییر می‌دهد */
$gate_prev = (int) get_option( 'sti_gs_gate_auto_worker', 0 );

if ( ! STI_GS_Cron_Gate::pass( 'auto_worker', self::interval_seconds() ) ) {
    return;
}
```

ترتیب حیاتی است: اگر بعد از `pass()` بخوانیم، همیشه فاصله‌ی صفر می‌بینیم.
تست `G2` همین ترتیب را قفل می‌کند.

---

## ۴) ❌ نقص ۲ — کامنتی که وعده داد ولی کدش نوشته نشده بود

در `advance_one()` این کامنت بود:

> «انتظارِ بی‌پایان هم یک بن‌بست است… بعد از پایان مهلت، به کلیک دوباره
> برمی‌گردیم.»

**هیچ کدی برای آن وجود نداشت.** فقط یک توضیح یتیم، بلافاصله بعدش کامنت
بعدی شروع می‌شد.

### پیامد روی میزبان

```
id=68  state=CHAIN_WAITING  attempts=0
```

Session ‎#68 از **۸ سپتامبر تا ۱۱ سپتامبر** — بیش از ۷۲ ساعت — در حالت
انتظار ماند. چون انتظار «خطا» شمرده نمی‌شود، `attempts` روی صفر ماند و
**هیچ سازوکاری نمی‌توانست آزادش کند**. حتی سقف تلاش هم بی‌اثر بود.

### اصلاح

```php
const WAITING_DEADLINE = 12 * HOUR_IN_SECONDS;
```

اگر Session بیش از ۱۲ ساعت در `CHAIN_WAITING` / `WAITING_BOT` /
`ERROR_BOT_TIMEOUT` بماند و `updated_at` تکان نخورده باشد:

- به `NEEDS_REVIEW` منتقل می‌شود با دلیل صریح
- خط `AUTO_WORKER_WAIT_DEADLINE` در لاگ ثبت می‌شود
- `NEEDS_REVIEW` از قبل در `TERMINAL` است ⇒ دیگر انتخاب نمی‌شود

**هیچ Sessionای حذف نمی‌شود** — تصمیم با شماست، فقط از سر راه صف کنار
می‌رود.

---

## ۵) درباره‌ی `selected=1` — این باگ نیست

انتظار داشتم `selected=6` ببینم ولی `selected=1` است. بررسی کردم:
**رفتار درست است.**

`pick()` واقعاً شش ردیف درخواست می‌کند، ولی بقیه‌ی Sessionها
(`#69`، `#70`، `#78`) از قبل `next_retry_at` آینده دارند و شرط
`next_retry_at <= NOW` آن‌ها را حذف می‌کند. صف واقعاً در آن لحظه فقط یک
کاندید واجد شرایط داشت.

این دقیقاً همان چیزی است که باید اتفاق بیفتد.

---

## ۶) آنچه تغییر نکرد

سقف تلاش · `backlog_limit` · فهرست‌های `TERMINAL`/`WAITING` · گیت
`max_active` · `sessions_per_tick`/`max_active_sessions` (طبق تصمیم شما
روی ۱) · Fiber · MTProto · منطق صف · ماشین حالت Session.
**هیچ Sessionای حذف یا دستی ویرایش نشد.**

---

## ۷) اعتبارسنجی

| مجموعه | نتیجه |
|---|---|
| `test-10-12-21-starvation.py` (۶۵ ادعا) | ALL PASS |
| ۸ مجموعه‌ی دیگر | ALL PASS |

بلوک `G1`-`G4` منبع فاصله و **ترتیب خواندن نسبت به `pass()`** را می‌سنجد.
بلوک `W1`-`W7` مهلت انتظار را: اینکه در `advance_one` اجرا شود، از
`updated_at` بخواند، به `NEEDS_REVIEW` برود (نه حذف)، و **پیش از**
ماشین مرحله‌ها اجرا شود.

ZIP مستقل باز و بررسی شد: نسخه ۱۰.۱۲.۲۱، هر دو اصلاح داخلش.

> ⚠️ در محیط توسعه PHP نصب نیست. اعتبارسنجی = بازرسی کد + تست ایستا.

---

## ۸) نشانه‌ی موفقیت پس از نصب

**مرحله‌ی اول (چند دقیقه):**
```
AUTO_WORKER_WAIT_DEADLINE session=#68 state=CHAIN_WAITING waited=80h
                          limit=12h action=NEEDS_REVIEW
```
‎#68 و ‎#69 که سه روز گیر بودند، آزاد می‌شوند.

**مرحله‌ی دوم (دور بعد):**
```
AUTO_WORKER_PICK: ids=[70]  →  [78]  →  [79]
AUTO_WORKER_STAGE session=#79 from=SCANNED to=... result=advanced
```

**مرحله‌ی سوم (بعد از دو تیک):**
```
DEFER ... interval=2400s   ← عدد واقعی، نه ۳۰۰
```

**شرط قطعی موفقیت: «مرحله جلو رفت» از صفر بالا برود.**
همچنین «نیازمند بازبینی» چند واحد زیاد می‌شود — این **مطلوب** است، یعنی
Sessionهای مرده از صف خارج شده‌اند.

---

## ۹) تصمیم بعدی درباره‌ی Root Cause

| # | مسئله | وضعیت |
|---|---|---|
| ۱ | گرسنگی Worker | مکانیزم **PROVEN و کارکرد تأییدشده**؛ دو نقص جانبی در ۲۱ رفع شد |
| ۲ | انتظار بی‌پایان (بدون سقف) | **PROVEN** — کد وجود نداشت، حالا اضافه شد |
| ۳ | ۸۲۵ رکورد orphan | شناسایی‌شده، **رفع نشده** |
| ۴ | `Fiber stack allocate failed` | **ROOT_CAUSE_UNKNOWN** |

خطای `file_get_contents(memory.max)` همچنان **۱۶ مورد بدون افزایش** در
چهار دامپ متوالی طی سه روز. اصلاح ۱۰.۱۲.۱۷ پابرجاست.

نکته: «خطا خورد» امروز **۰** است (قبلاً ۱۳) — چون تعداد دورها هم فقط ۵
بوده. این عدد را تا دامپ بعد قضاوت نمی‌کنم.

**«Root Cause مسئله‌ی Fiber هنوز اثبات نشده است.»**

---

## ۱۰) درس این دور

**درس اول — نوشتن و خواندن از یک option در یک تابع.** کد من مقدار را
چند خط پایین‌تر از جایی که می‌خواندش می‌نوشت. تست ایستا این را نمی‌گیرد
چون هر دو خط «درست» هستند؛ فقط ترتیبشان غلط است. حالا تست `G2` صریحاً
ترتیب را می‌سنجد.

**درس دوم — کامنت، اثباتِ وجود کد نیست.** یک توضیح دقیق و قانع‌کننده
درباره‌ی مهلت انتظار وجود داشت که باعث شد چند دور آن را «موجود» فرض کنم.
از این پس: هر رفتاری که کامنت وعده می‌دهد باید با `grep` روی خودِ کد
تأیید شود، نه با خواندن توضیح.
