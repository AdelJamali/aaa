# ارزیابی گزارش کارشناس مستقل — ۱۱ سپتامبر ۲۰۲۶

> روش: هر ادعای کارشناس روی کد نسخه‌ی ۱۰.۱۲.۲۱ و روی خودِ
> `madeline81.phar` بازبینی شد. هیچ ادعایی بدون بررسی پذیرفته نشد.
> استخراج PHAR با خواندن باینری انجام شد (سندباکس مفسر PHP ندارد).

**نتیجه‌ی کلی: گزارش دقیق است. از ۸ ادعای فنی قابل‌بررسی، ۸ مورد تأیید
شد. دو مورد از آن‌ها خطای مستقیم من بودند و یک مورد می‌توانست به سایت
آسیب بزند.**

---

## ۱) ⚠️ خطرناک‌ترین یافته — پیشنهاد من می‌توانست سایت را از کار بیندازد

در بند ۹-۳ سند بریفینگ نوشته بودم «سریع‌ترین مسیر» اجرای این است:

```php
\danog\MadelineProto\Tools::testFibers();
```

کارشناس هشدار داد این یک **آزمون فشار** است، نه خواندن اطلاعات.
بررسی کردم — **درست می‌گوید.**

بدنه‌ی واقعی تابع، استخراج‌شده از `madeline81.phar` در آفست
`11406225`:

```php
public static function testFibers(int $fiberCount = 100000): array
{
    ini_set('memory_limit', -1);

    $f = [];
    for ($x = 0; $x < $fiberCount; $x++) {
        try {
            $f []= $cur = new Fiber(static function (): void {
                Fiber::suspend();
            });
            $cur->start();
        } catch (\Throwable $e) {
            break;
        }
    }
    return [
        'maxFibers'    => $x,
        'realMemoryMb' => (int) (memory_get_usage(true)/1024/1024),
        'maps'         => self::getMaps(),
        'maxMaps'      => self::getMaxMaps(),
    ];
}
```

سه نکته:

1. `memory_limit` را به **`-1`** (نامحدود) تغییر می‌دهد.
2. پیش‌فرض **صد هزار** Fiber است و تا لحظه‌ی `Throwable` جلو می‌رود —
   یعنی **عمداً تا مرز فروپاشی می‌رود**.
3. هر Fiber دو VMA می‌سازد ⇒ تا ۲۰۰٬۰۰۰ نگاشت روی یک فرآیند lsphp پایا.

روی یک میزبان اشتراکی این می‌تواند کل حساب را با OOM مواجه کند.

**اشتباه من چه بود:** دیده بودم MadelineProto خودش در `Magic.php` تابع
`testFibers` را صدا می‌زند و نتیجه گرفتم «پس ابزار تشخیص بی‌خطر است».
اما فراخوانی داخلی کتابخانه `testFibers(100)` است — با آرگومان صریح.
من امضای پیش‌فرض را نخوانده بودم.

**جایگزین امن — فقط این دو:**
```php
\danog\MadelineProto\Tools::getMaps();     // فقط شمارش خطوط /proc/self/maps
\danog\MadelineProto\Tools::getMaxMaps();  // فقط خواندن sysctl
```
هر دو صرفاً `file_get_contents` روی `/proc` هستند. هیچ Fiber نمی‌سازند.

> **بند ۹-۳ سند بریفینگ باید اصلاح شود. تا آن زمان `testFibers()` را
> روی سایت اجرا نکنید.**

---

## ۲) ✅ ادعاهای تأییدشده درباره‌ی کد من

### ۲-۱) `@fopen` بدون محافظ در ابزار تشخیص — تأیید شد

کارشناس: `proc_kv()` هنوز `@fopen` دارد.

`class-gs-env-diag.php` خط ۴۰۶:
```php
$fp = @fopen( $path, 'rb' );
```

و از سه نقطه‌ی **ابتدای** `oom_context()` صدا زده می‌شود (خطوط ۹۸، ۱۰۲،
۱۱۰) — یعنی `/proc/meminfo`، `/proc/self/status`، `/proc/self/limits`،
همگی **قبل** از اولین `safe_read()`.

این دقیقاً همان الگویی است که در ۱۰.۱۲.۱۷ به‌عنوان علت اثبات‌شده‌ی
بازنویسی خطای اصلی شناسایی کردم و برایش قانون گذاشتم:

> «`@file_get_contents()` داخل runtime مدلاین امن نیست — چون
> `Magic::start()` یک `exceptionErrorHandler` نصب می‌کند که warning را
> به exception تبدیل می‌کند، و در PHP 8.0+ عملگر `@` دیگر
> `error_reporting()` را صفر نمی‌کند.»

بعد `file_get_contents` را با `safe_read()` جایگزین کردم و **`fopen` را
جا انداختم**. قانون را خودم نوشتم و در همان فایل نقضش کردم.

اثر عملی: اگر `@fopen` زیر handler کتابخانه throw کند،
`oom_context_safe()` (خط ۲۵۷) جلوی انتشارش را می‌گیرد — پس خطای اصلی
بازنویسی **نمی‌شود** — ولی کل خروجی تشخیص به `diagnostic_failed` تبدیل
می‌شود و **دقیقاً همان سه فایلی که Stage 2 لازم دارد از دست می‌رود**.

### ۲-۲) خواندن cgroup v2 از مسیر ثابت ریشه — تأیید شد

`class-gs-env-diag.php` خطوط ۱۲۸-۱۳۰:
```php
$v2max = self::safe_read( '/sys/fs/cgroup/memory.max', $read_errors );
$v2cur = self::safe_read( '/sys/fs/cgroup/memory.current', $read_errors );
$v2evt = self::safe_read( '/sys/fs/cgroup/memory.events', $read_errors );
```

اینها ریشه‌ی سلسله‌مراتب‌اند، نه cgroup واقعی همین فرآیند. مسیر درست
باید از `/proc/self/cgroup` و `/proc/self/mountinfo` ساخته شود. جالب
اینکه برای cgroup **v1** این کار را کرده‌ام (خط ۱۴۲ به بعد) ولی برای v2
نه.

### ۲-۳) برش پیام و trace — تأیید شد

`class-sti-mtproto.php` خطوط ۷۷۰، ۷۷۸، ۷۸۰:
```php
$ctx['error_message_raw'] = mb_substr( (string) $last_error, 0, 400 );
$ctx['exception_message'] = mb_substr( (string) $last_error, 0, 400 );
$ctx['exception_trace']   = mb_substr( (string) $last_exception_trace, 0, 1500 );
```

در تمام گزارش‌ها نوشته‌ام «پیام اصلی بایت‌به‌بایت حفظ می‌شود». برای این
پیام خاص (۶۲ نویسه) عملاً درست است، ولی **تضمین معماری نیست** — و trace
در ۱۵۰۰ نویسه تقریباً همیشه بریده می‌شود، یعنی دقیقاً همان چیزی که
Stage 3 خواسته بود ناقص ثبت می‌شود.

### ۲-۴) `registerFiber` در `GarbageCollector` است نه `Magic` — تأیید شد

آفست `3824820` در PHAR، کلاس دربرگیرنده `GarbageCollector`:
```php
/** @var \WeakMap<\Fiber, true> */
public static WeakMap $map;
public static function registerFiber(\Fiber $fiber): \Fiber
{
    self::$map ??= new WeakMap;
    self::$map[$fiber] = true;
    return $fiber;
}
```

ارجاع من به `Magic::registerFiber()` غلط بود. نکته‌ی مهم‌تر کارشناس:
وجود `WeakMap` **شاهد نشت نیست** — `WeakMap` دقیقاً برای این ساخته شده
که مانع آزادسازی نشود. فرضیه‌ی ۲-۴ سند بریفینگ باید به «بدون شاهد»
تنزل کند.

### ۲-۵) تغییر `memory_limit` به `512M` توسط خود افزونه — تأیید شد

`class-sti-mtproto.php` خطوط ۲۴۷ و ۶۵۷:
```php
@ini_set( 'memory_limit', '512M' );
```

این **تناقض ۲۸۰/۵۱۲ را توضیح می‌دهد** — تناقضی که در همه‌ی گزارش‌ها
به‌عنوان «حل‌نشده» علامت زده بودم. عدد ۲۸۰ مقدار پیکربندی میزبان است و
۵۱۲ مقداری که افزونه‌ی خودم قبل از ساخت client اعمال می‌کند.

جواب داخل کد خودم بود و پیدایش نکرده بودم. البته این سقف heap است و
سقف `mmap` نیست — پس علت خطا نیست، ولی دیگر یک مجهول نیست.

---

## ۳) ✅ تصحیح‌های کارشناس روی تحلیل «نقص چهارم» — می‌پذیرم

### ۳-۱) «شرط هرگز اجرا نمی‌شود» — بیش‌ازحد مطلق بود

نوشته بودم `updated_at` هرگز کهنه نمی‌شود پس شرط هرگز درست نمی‌شود.

تصحیح درست کارشناس: اگر یک وقفه‌ی طولانی‌تر از ۱۲ ساعت رخ دهد (سایت
بی‌بازدید بماند، WP-Cron نخوابد، Worker خاموش شود) شرط **می‌تواند**
فعال شود. جمله‌ی درست:

> در شرایط عادیِ این میزبان که فاصله‌ی تیک حدود ۹۰۰ تا ۵۴۰۰ ثانیه است،
> `updated_at` مرتب تازه می‌شود و مهلت عملاً هرگز نمی‌رسد — ولی این یک
> قطعیت ساختاری نیست.

### ۳-۲) «Recovery هیچ Session یتیمی پیدا نمی‌کند» — بیش‌ازحد کلی بود

پرس‌وجوهای `class-gs-recovery.php` خطوط ۴۵۳ و ۴۹۳ شرط‌های دیگری هم
دارند (`worker_id IS NOT NULL`, `locked_until < now`). اگر فرآیند
بمیرد و قفل منقضی شود، همچنان می‌توانند کار کنند. آنچه از کار افتاده
**فقط سناریوی Session منتظرِ بدون قفل** است.

### ۳-۳) `selected=6` را بیش از حقش تفسیر کردم

در گزارش نوشتم «صف دیگر روی یک Session قفل نیست». کارشناس درست
می‌گوید: `selected=6` یعنی شش ردیف **انتخاب** شد، نه شش پیشرفت.

شاهد نقض در همان دامپ:
```
06:08:27  STAGE #80 from=SCANNED to=SCANNED result=skipped
06:08:27  STAGE #79 from=SCANNED to=SCANNED result=skipped
06:08:27  STAGE #68 from=CHAIN_WAITING to=CHAIN_WAITING result=waiting
```
**هیچ‌کدام جلو نرفتند.** «مرحله جلو رفت» همچنان صفر است.

### ۳-۴) `SCANNED` در فهرست `WAITING` نیست

`const WAITING = array( 'WAITING_BOT', 'ERROR_BOT_TIMEOUT', 'CHAIN_WAITING' )`

پس ‎#79 و ‎#80 که `SCANNED` هستند و `skipped` می‌شوند، **اصلاً مشمول
مهلت انتظار نمی‌شوند**. یک حالت گیرکردن دیگر که پوشش ندارد.

### ۳-۵) نوشتن از snapshot پیش از claim

بلوک مهلت در خط ۸۵۲-۸۶۳ از ردیفی می‌نویسد که در `pick()` (خط ۸۱۰،
`SELECT *`) خوانده شده — **بدون claim معتبر**. اگر اجرای دیگری هم‌زمان
همان Session را جلو ببرد، این نوشتن می‌تواند رویش بنویسد. باید زیر
claim یا با شرط اتمیک باشد.

### ۳-۶) `clicked_at` / `last_polled_at` جایگزین مطمئنی نیستند

پیشنهاد قبلی من بود. کارشناس رد کرد و ستون اختصاصی `waiting_since`
خواست، با migration سازگار، ثبت فقط در **گذارِ ورود** به انتظار، حفظ در
poll/claim، پاک‌سازی در خروج. و برای Sessionهای موجود: سیاست صریح، نه
نسبت‌دادن ساختگی `created_at`.

می‌پذیرم — طراحی او تمیزتر است.

---

## ۴) ✅ تصحیح‌های کارشناس روی جدول تصمیم ۹-۵

سه ایراد وارد است:

| ردیف من | ایراد |
|---|---|
| `memory.failcnt > 0` ⇒ سقف cgroup | شمارنده **تجمعی** است؛ می‌تواند تاریخی باشد. باید دلتا سنجید. |
| `Committed_AS ≈ CommitLimit` ⇒ سقف overcommit | فقط وقتی معنا دارد که `vm.overcommit_memory=2` باشد. باید اول سیاست خوانده شود. |
| فرآیند قدیمی در برابر جدید ⇒ نشت | بدون کنترل نوع کار، اختلاف چیزی را اثبات نمی‌کند. |

و پیشنهاد `vm.max_map_count=262144` زودهنگام بود — اول باید سقف مؤثر
فعلی معلوم شود.

---

## ۵) نکته‌ای که کارشناس محتاطانه‌تر از من بود — و حق داشت

من در سند نوشتم `interval=896s` و `selected=6` را «تأیید روی میزبان»
دیده‌ام. کارشناس این‌ها را **نقل سند** طبقه‌بندی کرد، چون خودش دامپ خام
را ندیده بود.

این طبقه‌بندی درست است. من دامپ را مستقیم از برنچ خواندم
(`git show` روی commit `0b6f60e`) و اعداد از خروجی واقعی پنل می‌آیند —
ولی کارشناس آن دسترسی را نداشت و درست عمل کرد که ادعای دست‌دوم را
به‌عنوان مشاهده ثبت نکرد.

---

## ۶) جمع‌بندی — چه چیزی عوض می‌شود

### تغییر وضعیت

| مورد | قبل | بعد |
|---|---|---|
| تناقض `memory_limit` ۲۸۰/۵۱۲ | «حل‌نشده» | ✅ **حل شد** — کد خودم خط ۲۴۷/۶۵۷ |
| `testFibers()` به‌عنوان ابزار تشخیص | «سریع‌ترین مسیر» | ❌ **خطرناک — حذف شد** |
| نشت Fiber از طریق WeakMap | «پشتیبانی‌شده» | ⬇️ **بدون شاهد** |
| `getMaps()` / `getMaxMaps()` | پیشنهاد | ✅ **همچنان امن و توصیه‌شده** |
| نقص چهارم (`updated_at`) | «شرط هرگز اجرا نمی‌شود» | 🔶 **عملاً درست، ساختاری نه** |
| `selected=6` | «صف آزاد شد» | 🔶 **شش انتخاب، صفر پیشرفت** |

### بدون تغییر

```
FIBER_ENOMEM = ROOT_CAUSE_UNKNOWN
ROOT CAUSE NOT YET PROVEN
```

هر دو مستقل به یک نتیجه رسیدیم. چهار فرضیه‌ی A تا D باز می‌مانند و
انتخاب بین «محدودیت محیط» و «ناسازگاری معماری» همچنان بی‌شاهد است.

### کارهای پیش رو

**فوری (اصلاح سند):**
- بند ۹-۳: `testFibers()` حذف، فقط `getMaps()`/`getMaxMaps()`
- بند ۹-۵: سه تصحیح جدول تصمیم
- بند ۲-۴: تنزل فرضیه‌ی WeakMap
- ثبت پاسخ تناقض `memory_limit`

**نسخه‌ی ۱۰.۱۲.۲۲ (منتظر تأیید):**
1. `@fopen` در `proc_kv()` ⇒ `is_readable()` + handler اختصاصی
2. cgroup v2 از مسیر واقعی فرآیند
3. برداشتن برش ۴۰۰/۱۵۰۰ برای پیام و trace
4. `waiting_since` طبق طراحی کارشناس
5. پوشش حالت `SCANNED` که `skipped` می‌ماند

**سؤال باز که هیچ‌کدام جواب نداده‌ایم:** چرا ‎#79 و ‎#80 در حالت
`SCANNED` نتیجه‌ی `skipped` می‌دهند؟ این نه انتظار است نه خطا — و
«مرحله جلو رفت» را صفر نگه داشته. به‌نظرم این باید اولویت بعدی باشد،
نه Fiber.
