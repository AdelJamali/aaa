# تشخیص Publish Pipeline — Auto Worker یا Publish Queue؟

> روش: فقط بازرسی کد نسخه‌ی ۱۰.۱۲.۱۶ در همین مخزن. هر ادعا با فایل و خط.
> هیچ داده‌ی runtime از هاست در دست نیست، پس هرجا لازم باشد صریح می‌گویم «اثبات‌نشده».

---

## راستی‌آزمایی ادعاهای Audit

| ادعا | نتیجه | شاهد |
|---|---|---|
| Worker عمداً REVIEW_READY را Terminal می‌داند | ✅ **تأیید** | `class-gs-auto-worker.php:63` — `const TERMINAL = array( 'REVIEW_READY', … )`؛ کوئری‌های انتخاب در خطوط ۲۹۴ و ۵۱۲ با `state NOT IN (…)` |
| Worker دیگر مسئول انتشار نیست | ✅ **تأیید** | در کل `class-gs-auto-worker.php` هیچ ارجاعی به `Publish_Queue` نیست (grep خالی) |
| انتشار توسط `STI_GS_Publish_Queue` انجام می‌شود | ✅ **تأیید** | `class-gs-publish-queue.php` → `tick()` → `publish()` خط ۴۱۶ |
| Publish Queue فقط `queued` مصرف می‌کند | ✅ **تأیید، و سخت‌گیرانه‌تر از تصور شما** | `tick()` خط ۳۷۱-۳۷۹: `queue_status='queued'` **و** `product_id>0` **و** `scheduled_at IS NOT NULL` **و** `scheduled_at <= now` |
| روی `failed` هیچ Retry خودکاری نیست | ✅ **تأیید** | `failed` فقط در دو جای نوشتن (۱۳۸، ۳۹۸) و یک‌جای شمردن (۴۹۱) ظاهر می‌شود. هیچ کوئری‌ای آن را نمی‌خواند تا دوباره اجرا کند |
| دو Toggle مستقل | ✅ **تأیید** | Worker: `sti_gs_worker_enabled` (auto-worker خط ۳۴) · Queue: `sti_gs_queue_running` (publish-queue خط ۱۶۵). دو option جدا، بدون هیچ پیوندی |

تا اینجا Audit در **هر شش مورد درست** است.

---

## الف) آیا REVIEW_READY نشانه‌ی موفقیت Worker است؟

**بله — ولی فقط تا مرز خودش، و این «بله» مشروط است.**

REVIEW_READY یعنی Validator تمام بررسی‌های سخت را پاس کرده است: وجود محصول، عنوان، توضیحات، تصویر شاخص، و یک HEAD روی لینک دانلود (`class-gs-product-validator.php:44-56`). هیچ‌کدام از این‌ها ارزان نیست. پس رسیدن به REVIEW_READY یعنی **زنجیره‌ی اسکن → دانلود → ساخت محصول → اعتبارسنجی کامل کار کرده است**.

اما یک نکته‌ی مهم که Audit به آن اشاره نکرده و از نظر معماری تعیین‌کننده است:

**`STI_GS_Stage` اصلاً REVIEW_READY را Terminal نمی‌داند.** در `class-gs-stage.php:95-98` صراحتاً نوشته شده:

> «REVIEW_READY نهایی نیست — منتظر صف انتشار است (PUBLISH/WAITING).»

یعنی در سیستم شما **دو تعریف متناقض از «تمام‌شده» وجود دارد**: Auto Worker آن را Terminal می‌داند، Stage آن را «در انتظار» می‌داند. این تناقض خودش منشأ حس «اتوماسیون کار نمی‌کند» است.

پس پاسخ دقیق: REVIEW_READY نشانه‌ی موفقیت Worker است، **ولی نشانه‌ی موفقیت Pipeline نیست**. و اگر Session در REVIEW_READY **با `queue_status='active'`** مانده باشد، آن یک موفقیت نیست — یک **Session رهاشده** است. دلیلش را در بخش بعد اثبات می‌کنم.

---

## ⚠️ یافته‌ی اصلی: تنها درِ ورود به صف، یک‌بارمصرف است

این مهم‌ترین چیزی است که پیدا کردم و در گزارش Audit شما نیست.

**در کل افزونه، `STI_GS_Publish_Queue::enqueue()` فقط از یک نقطه صدا زده می‌شود** (grep در کل `includes/`):

```
class-gs-product-validator.php:85 → STI_GS_Publish_Queue::enqueue( $session_id );
```

و این تنها فراخوانی، دقیقاً **۱۴ خط بعد از** جایی است که state روی REVIEW_READY ست می‌شود (خط ۷۱). حالا به گارد ابتدای همان تابع نگاه کنید (خط ۲۳-۲۹):

```php
if ( 'REVIEW_READY' === $session['state'] ) {
    …
    return array( 'state' => 'REVIEW_READY', 'skipped' => true );   // ← خروج زودهنگام
}
```

**نتیجه‌ی منطقی — و این یک باگ ساختاری است:**

اگر به هر دلیلی state روی REVIEW_READY ست شود ولی `enqueue()` در همان اجرا موفق نشود، آن Session **برای همیشه** خارج از صف می‌ماند. چون:

- هر بار بعد که `validate()` صدا زده شود، گارد خط ۲۳ زودتر `return` می‌کند و **هرگز به خط ۸۵ نمی‌رسد**.
- Worker هم آن را نمی‌بیند (Terminal است).
- `repair_missing_schedule()` هم نجاتش نمی‌دهد، چون کوئری‌اش (خط ۲۸۲-۲۸۷) فقط ردیف‌هایی را برمی‌دارد که **از قبل `queue_status='queued'`** هستند و صرفاً `scheduled_at` ندارند. یک ردیف `active` اصلاً در دامنه‌اش نیست.

پس **سه مسیر بازیابی وجود دارد و هر سه این حالت را رد می‌کنند**. این تعریف دقیق «رهاشدگی دائمی» است.

سه سناریوی واقعی که این را می‌سازند — هر سه بدون نیاز به فرض کردن مشکل MTProto/Memory/Cron:

1. **`gs_auto_enqueue` خاموش باشد** (خط ۸۴). آن‌وقت state ست می‌شود ولی enqueue هرگز اجرا نمی‌شود. اگر بعداً روشنش کنید، **Sessionهای قبلی هرگز وارد صف نمی‌شوند** — چون گارد خط ۲۳ راه را بسته.
2. **`enqueue()` خطا برگرداند** — مثلاً `product_id` خالی باشد (خط ۶۷ همان کلاس: `sti_gs_no_product`). خطا فقط در Event لاگ می‌شود (خط ۸۷-۸۸) و Session در `active` رها می‌ماند.
3. **قطع اجرا بین خط ۷۱ و خط ۸۵** — timeout، fatal، یا kill شدن فرآیند. state نوشته شده، enqueue نشده.

ضمناً یک شاهد تاریخی مهم: در `class-gs-session.php:132-140` توضیح داده شده که قبلاً دقیقاً همین دسته اتفاق افتاده بود — `scheduled_at` در whitelist نبود، `enqueue()` آن را می‌فرستاد و **بی‌صدا دور ریخته می‌شد**، و نتیجه‌اش این بود که «از بیرون شبیه صف کار نمی‌کند دیده می‌شد، در حالی که صف درست بود». آن مورد خاص رفع شده (الان `scheduled_at` در whitelist هست)، ولی الگوی شکست تکرارشونده است.

---

## ب) برای اثبات اینکه مشکل در Publish Queue است، چه چیزی را ببینیم

### گام ۱ — بدون SQL (اول این را امتحان کنید)

**گلدن اسکن → صف انتشار.** خروجی `stats()` (خط ۴۸۸-۵۱۶) دقیقاً همان اعدادی است که لازم داریم: `running`, `queued`, `published`, `failed`, `unscheduled`, `next_at`, `published_today`, `daily_cap`.

جدول تصمیم — فقط با همین صفحه:

| نشانه | تشخیص |
|---|---|
| `running = false` | **صف خاموش است.** Worker روشن بودن ربطی ندارد. دلیل کافی برای «اتوماسیون کار نمی‌کند» |
| `unscheduled > 0` | باگ زمان‌بندی دوباره رخ داده (خودِ کد این را «همان باگ» می‌نامد) |
| `queued = 0` ولی Sessionهای REVIEW_READY دارید | **دقیقاً همان یافته‌ی بالا: enqueue انجام نشده. مشکل در Validator است، نه در Queue** |
| `failed > 0` | انتشار شکست خورده و **هیچ‌وقت خودش دوباره تلاش نمی‌کند** |
| `published_today >= daily_cap` | سقف روزانه پر شده؛ `tick()` خط ۱۰۶ بی‌صدا برمی‌گردد |
| `next_at` در آینده‌ی دور | فاصله‌ی انتشار زیاد است یا `LAST_PUB_KEY` جلو پریده |

### گام ۲ — لاگ‌ها

در **گزارش‌ها**، این دو رشته را جست‌وجو کنید:

- `ورود به صف ناموفق` → سناریوی ۲ بالا (خط ۸۷).
- `ستون ناشناخته نادیده گرفته شد` → همان باگ whitelist در حال تکرار (`class-gs-session.php:146`).

و در Eventهای هر Session (جدول `wp_sti_gs_session_events`)، مرحله‌ی `publish_queue`: نبودِ هیچ رویدادی برای یک Session در REVIEW_READY، **اثبات مستقیم** است که enqueue اصلاً اجرا نشده.

### گام ۳ — SQL فقط خواندنی (اگر گام ۱ کافی نبود)

نام جدول `wp_sti_gs_pipeline_items` است — ولی توجه: اسم جدول در نصب شما ممکن است `wp_sti_gs_sessions` باشد (مهاجرت `PIPELINE_SLUG_OLD` در `class-gs-db.php:75-76`). اگر کوئری خطا داد، نام دوم را امتحان کنید.

**کوئری تعیین‌کننده — این یک عدد کل ماجرا را روشن می‌کند:**

```sql
SELECT state, queue_status, COUNT(*) AS n,
       SUM(product_id IS NULL OR product_id = 0) AS bi_mahsul,
       SUM(scheduled_at IS NULL)                 AS bi_zaman
FROM wp_sti_gs_pipeline_items
GROUP BY state, queue_status
ORDER BY n DESC;
```

خواندن نتیجه:

- ردیف `REVIEW_READY | active` با n بزرگ ⇒ **رهاشده‌های اثبات‌شده**. نه Worker می‌بیندشان نه Queue.
- ردیف `REVIEW_READY | queued` با `bi_zaman > 0` ⇒ منتظرِ `repair_missing_schedule()`.
- ردیف `REVIEW_READY | queued` با `bi_zaman = 0` ⇒ صف سالم است، فقط نوبتشان نرسیده یا صف خاموش است.
- ردیف با `queue_status='failed'` ⇒ نیازمند دخالت دستی (Retry خودکار وجود ندارد).

**دو کوئری کمکی:**

```sql
-- آیا زمانِ نوبت رسیده ولی منتشر نشده؟ (اگر عدد > 0 و چیزی منتشر نمی‌شود ⇒ tick اجرا نمی‌شود)
SELECT COUNT(*) FROM wp_sti_gs_pipeline_items
WHERE queue_status='queued' AND product_id > 0
  AND scheduled_at IS NOT NULL AND scheduled_at <= NOW();

-- وضعیت دو کلید مستقل + کران
SELECT option_name, option_value FROM wp_options
WHERE option_name IN ('sti_gs_worker_enabled','sti_gs_queue_running',
                      'sti_gs_queue_interval','sti_gs_queue_daily',
                      'sti_gs_queue_last_published_at','cron');
```

اگر کوئری اول عددی مثبت داد و هیچ انتشاری رخ نمی‌دهد، آن‌وقت — و فقط آن‌وقت — سراغ کران می‌رویم. (خودِ کد هوک را روی `sti_gs_5min` ثبت می‌کند، خط ۸۶-۸۹.)

---

## ج) مسئولِ ادامه‌ی پردازش در هر حالت

| وضعیت | مسئول | واقعیت کد |
|---|---|---|
| `REVIEW_READY` + `active` | **هیچ‌کس** ❌ | Worker: Terminal (خط ۶۳). Queue: `queue_status='queued'` لازم است (خط ۳۷۳). Validator: گارد خط ۲۳ زودتر خارج می‌شود. `repair_missing_schedule()`: فقط روی `queued` کار می‌کند. **رهاشده‌ی دائمی — فقط با کلیک دستی خارج می‌شود** |
| `REVIEW_READY` + `queued` | `STI_GS_Publish_Queue::tick()` ✅ | مشروط به هر پنج شرط: صف روشن، `product_id>0`، `scheduled_at` پرشده، `scheduled_at <= now`، قفل آزاد، و سقف روزانه پر نشده |
| `REVIEW_READY` + `failed` | **هیچ‌کس** ❌ | نوشته می‌شود ولی هرگز خوانده نمی‌شود. تنها راه: دکمه‌ی دستی در صفحه‌ی صف انتشار |

پس از سه حالتی که پرسیدید، **دو تا مسیر خودکار ندارند**.

---

## د) REVIEW_READY به Worker برگردد یا Queue مستقل بماند؟

**Queue مستقل بماند.** جداسازی درست است و برگرداندنش سه چیز را خراب می‌کند: Worker دوباره مسئول دو کار متفاوت می‌شود (پردازش + انتشار)، فاصله‌ی انتشار و سقف روزانه که الان در Queue متمرکزند به Worker نشت می‌کنند، و کنترل اپراتور روی «چه چیزی منتشر شود» از بین می‌رود.

مشکل شما **جداسازی نیست، نبودِ پل بین دو بخش است**. الان تنها پل، یک فراخوانی تک‌نقطه‌ای و یک‌بارمصرف است که اگر از دستش بدهید دیگر تکرار نمی‌شود.

راه‌حل درست معماری، **یک reconciler است، نه ادغام**: یک بررسی دوره‌ای که هر Session با `state='REVIEW_READY'` و `queue_status='active'` و `product_id>0` را دوباره enqueue کند. این کار idempotent است (خودِ `enqueue()` در خط ۶۹-۷۱ گارد idempotency دارد)، رفتار هیچ بخش موجودی را تغییر نمی‌دهد، و دقیقاً همان حفره‌ای را می‌بندد که در بخش «یافته‌ی اصلی» اثبات کردم.

ساده‌ترین جای درستش: گسترش دامنه‌ی `repair_missing_schedule()` یا یک متد خواهر کنار آن — همان‌جا که زیرساخت ترمیم از قبل هست.

---

## ه) آیا طراحی فعلی از نظر Recovery ناقص است؟

**بله. برای هدف «اتوماسیون کامل بدون هیچ کلیک دستی»، سه حفره‌ی مشخص وجود دارد:**

1. **بدون reconcile برای `REVIEW_READY + active`** — تنها درِ ورود به صف یک‌بارمصرف است و پشت یک گارد early-return قفل می‌شود. (اثبات در بخش «یافته‌ی اصلی».)
2. **بدون Retry روی `failed`** — ستون‌های `attempts` و `next_retry_at` در همان جدول وجود دارند و بقیه‌ی سیستم از آن‌ها استفاده می‌کند، ولی Publish Queue اصلاً به آن‌ها دست نمی‌زند. یک خطای گذرا (مثلاً یک قطعی لحظه‌ای) Session را برای همیشه از دور خارج می‌کند.
3. **دو Toggle مستقل، بدون هیچ هشداری** — می‌شود Worker روشن و Queue خاموش باشد و هیچ‌جا به کاربر گفته نشود. از بیرون عیناً شبیه «اتوماسیون خراب است» دیده می‌شود، در حالی که هر دو بخش سالم‌اند. این مسئله‌ی UX نیست؛ مسئله‌ی قابلیت مشاهده است.

هر سه با تغییرات کوچک و کم‌ریسک قابل رفع‌اند و هیچ‌کدام نیازمند دست‌زدن به Worker یا معماری فعلی نیست.

---

## پاسخ نهایی به پرسش اصلی

**«آیا Auto Worker مشکل دارد یا Publish Queue؟» — هیچ‌کدام خراب نیستند. مرزِ بینشان درز دارد.**

Worker دقیقاً همان کاری را می‌کند که برایش تعریف شده و REVIEW_READY را به‌درستی Terminal می‌داند. Publish Queue هم منطق انتشارش سالم است. آنچه وجود ندارد **تضمینِ انتقال از اولی به دومی** است: یک فراخوانی تک‌نقطه‌ای که اگر یک‌بار از دست برود، هیچ سازوکاری آن را جبران نمی‌کند.

`PIPELINE_ROOT_CAUSE = MISSING_ENQUEUE_RECONCILIATION` — **در سطح کد اثبات‌شده**؛ اینکه روی سایت شما هم همین رخ داده باشد، با یک کوئری گروه‌بندی بخش (ب) در چند ثانیه قطعی می‌شود.

**فرض‌های اثبات‌نشده (صادقانه):** تعداد واقعی Sessionهای `REVIEW_READY + active` روی سایت شما را ندیده‌ام؛ مقدار `gs_auto_enqueue` و `sti_gs_queue_running` روی نصب شما را ندیده‌ام؛ و طبق خواسته‌ی خودتان MTProto/Memory/Cron را بررسی نکردم — هیچ شاهد جدیدی هم پیدا نشد که آن‌ها را وارد ماجرا کند.

**هیچ تغییری در کد ندادم.** این گزارش فقط تشخیص است. اگر بگویید، reconciler بخش (د) را به‌صورت یک پچ کوچک، idempotent و پشت کلید خاموشی می‌نویسم.
