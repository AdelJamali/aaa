# ارزیابی راه‌حل کارشناس — اولویت ۲ (`waiting_since`)

> تاریخ: ۱۱ سپتامبر ۲۰۲۶ · نسخه‌ی مبنا: ۱۰.۱۲.۲۱ (`2403ab8`)
> روش: هر ادعا روی checkout واقعی بازبینی شد. هیچ کدی تغییر نکرد.

---

## ۰) وضعیت پوشش — مهم

کارشناس **فقط اولویت ۲** را پاسخ داده. سه مورد دیگر باز است:

| اولویت | مسئله | وضعیت پاسخ |
|---|---|---|
| ۱ | چرخه‌ی `SCANNED → skipped` | ❌ **پاسخ داده نشده** |
| ۲ | ساعت انتظار / `waiting_since` | ✅ پاسخ کامل |
| ۳ | ایمن‌سازی ابزار تشخیص | ❌ پاسخ داده نشده |
| ۴ | Fiber — طرح جمع‌آوری داده | ❌ پاسخ داده نشده |

خودِ کارشناس در بخش Feedback نوشته دامنه را عمداً محدود نگه داشته
(«دامنه فقط `waiting_since`، Schema و concurrency نگه داشته شد»).

**نکته‌ی مهم:** اولویت ۱ همان چیزی است که مستقیماً «مرحله جلو رفت = ۰»
را توضیح می‌دهد. رفع `waiting_since` به‌تنهایی خط لوله را راه
نمی‌اندازد — فقط ‎#68 و ‎#69 را از صف خارج می‌کند.

همچنین **متن کامل گزارش در دسترس ما نیست** — فقط خلاصه‌ی نتایج.
مسیر `/tasklet/threads/.../waiting-audit.md` مربوط به محیط کارشناس
است. برای اجرا به متن کامل نیاز داریم (بخش ۵).

---

## ۱) بازبینی ادعاها — همه تأیید شد

### ۱-۱) ✅ `updated_at` ساعت معتبری نیست

`class-gs-session.php:155` و `:167` — تأیید شد. این را قبلاً خودمان
یافته بودیم و کارشناس در گزارش قبلی تصحیحش کرد.

### ۱-۲) ✅ deadline پیش از claim و روی snapshot اجرا می‌شود

این ادعا از قبلی مهم‌تر است و **کامنت خودِ کد تأییدش می‌کند**.

بلوک deadline در `class-gs-auto-worker.php:852-875` است. هشتاد خط
پایین‌تر، خط ۹۳۰، کامنتی هست که خودمان نوشته‌ایم:

```
عمداً اینجا قفل گرفته نمی‌شود.

هر هفت موتور (Button Resolver، Action Executor، File Matcher،
Download، Media، Product Builder، Validator) **خودشان**
STI_GS_Session::claim() را صدا می‌زنند.
```

یعنی `advance_one()` عامدانه بدون قفل کار می‌کند، چون قرار بوده فقط
مسیریابی کند. من یک **نوشتنِ تغییردهنده‌ی state** را داخل تابعی
گذاشته‌ام که طراحی‌اش صریحاً بدون قفل است.

### ۱-۳) ✅ `update()` هیچ شرط مالکیت ندارد

`class-gs-session.php:156`:
```php
return $wpdb->update( self::table(), $row, array( 'id' => (int) $id ) );
```

فقط `WHERE id`. نه `worker_id`، نه `state`، نه `locked_until`.

### ۱-۴) ✅ `defer_waiting()` پس از release و بدون claim

`class-gs-auto-worker.php:658-664` — `defer_waiting()` بعد از بازگشت
از `advance_one()` صدا زده می‌شود، یعنی بعد از اینکه موتور در بلوک
`finally` خودش `release()` کرده. نوشتن فقط با `id`.

### ۱-۵) ✅ TTL از ۴۵ تا ۶۰۰ ثانیه، بدون renew

| TTL | محل |
|---|---|
| ۴۵ | `bot-candidate-collector:196` · `file-matcher:33` · `POLL_LOCK_SECONDS` |
| ۶۰ | `button-resolver:47` · `chain-engine:112,301` · `product-validator` |
| ۹۰ | `action-executor:26` · `STEP_LOCK_SECONDS` |
| ۱۸۰ | `product-builder` |
| ۲۴۰ | `auto-worker:36` |
| ۳۰۰ | `media-engine` · `publish-queue` |
| **۶۰۰** | `download-engine:19` |

`grep` برای `renew|extend_lock` ⇒ **صفر نتیجه**. تأیید شد.

اهمیتش: یک دانلود ۶۰۰ ثانیه‌ای می‌تواند از TTL عبور کند، قفل منقضی
شود، Worker دیگری Session را بردارد، و **هر دو** بنویسند — چون
`update()` شرط مالکیت ندارد.

### ۱-۶) ✅ Schema — `waiting_since` وجود ندارد، `DB_VER = '2.4'`

`class-gs-db.php:53`. هیچ ستونی به این نام نیست.

⚠️ **یک نکته که کارشناس نمی‌توانست بداند:** نام `waiting_since` از قبل
به‌صورت جزئی در کد هست — `LOCK_WAIT_SINCE_KEY = 'sti_gs_migration_lock_wait_since'`
(خط ۷۰) و متغیر محلی `$waiting_since` در خطوط ۱۰۷۸-۱۰۸۳.

بررسی کردم: **تعارضی نیست** — آن یک option برای قفل migration است، نه
ستون. ولی هنگام `grep` زدن باعث سردرگمی می‌شود. پیشنهاد می‌کنم در
پیاده‌سازی، نام ستون `waiting_since` بماند ولی در کامنت به این
هم‌نامی اشاره شود.

### ۱-۷) ✅ قفل migration با transient اتمیک نیست

`class-gs-db.php:1099-1107`:
```php
protected static function acquire_transient_lock() {
    if ( get_transient( self::LOCK_TRANSIENT ) ) {
        return false;
    }
    set_transient( self::LOCK_TRANSIENT, 1, 5 * MINUTE_IN_SECONDS );
```

فاصله‌ی بین `get` و `set` یک پنجره‌ی رقابت است. دو درخواست هم‌زمان
می‌توانند هر دو قفل بگیرند.

این ریسک **مستقل از کار ما** است و کارشناس درست کرد که جدا ثبتش کرد.
برای migration ستون جدید مهم است چون `dbDelta` می‌تواند دو بار
هم‌زمان اجرا شود.

---

## ۲) آنچه از خلاصه پذیرفتیم

### ۲-۱) بدون backfill

هیچ مقداری از `created_at`، `updated_at`، `clicked_at` یا جدول
رویدادها استنتاج نشود. در اولین مشاهده‌ی معتبر، ساعت **همان لحظه**
ثبت شود و **همان دور مشمول deadline نشود**.

این دقیقاً همان چیزی است که در سند درخواست پرسیده بودیم و پاسخ
درستی است: یک Session که ۸۰ ساعت منتظر بوده، بعد از ارتقا از صفر
شمرده می‌شود. کندتر، ولی بدون داده‌ی ساختگی.

### ۲-۲) قرارداد UTC مستقل

`waiting_since` در UTC و مستقل از ستون‌های محلی موجود. این پاسخ سؤال
۳-۴ ماست (تداخل `current_time('mysql')` با `time()`).

**سؤال باقی‌مانده:** خواندن با `UTC_TIMESTAMP()` در SQL یا با
`gmdate()` در PHP؟ کارشناس در خلاصه هر دو را نام برده. برای اجرا باید
یکی انتخاب شود.

### ۲-۳) شرط ساده کافی نیست

ما در سند پرسیده بودیم آیا
`WHERE id AND state AND waiting_since` کافی است. پاسخ: **نه** — باید
مالکیت قفل یا CAS کامل شامل مقدار قبلی ساعت و وضعیت قفل باشد.

با توجه به یافته‌ی ۱-۲ (نوشتن بدون قفل) این پاسخ منطقی است.

### ۲-۴) ⭐ ایراد جدی که ما ندیده بودیم — `ERROR_BOT_TIMEOUT`

کارشناس اشاره کرده مهلت ۱۲ ساعته‌ی سراسری نباید مسیر retry را پنهان
کند، و `ERROR_BOT_TIMEOUT` **یک حالت خطاست، نه انتظار**.

بررسی کردم — `class-gs-session-ajax.php:284`:
```php
if ( ! in_array( (string) $session['state'],
     array( 'ERROR_BOT_TIMEOUT', 'ERROR_MATCH' ), true ) ) {
```
یعنی این حالت مسیر requeue دستی خودش را دارد.

ولی `class-gs-auto-worker.php:66` آن را داخل `const WAITING` گذاشته:
```php
const WAITING = array( 'WAITING_BOT', 'ERROR_BOT_TIMEOUT', 'CHAIN_WAITING' );
```

**پس مهلت ۱۲ ساعته شامل یک حالت خطا هم می‌شود** و می‌تواند Sessionای
را که کاربر قصد داشته دستی requeue کند، به `NEEDS_REVIEW` ببرد.

این را ما ندیده بودیم. ✅ می‌پذیریم.

---

## ۳) آنچه نیاز به شفاف‌سازی دارد

### ۳-۱) تعریف «episode» — کلید کل طراحی

خلاصه می‌گوید episodeها «به‌طور دقیق برای callback، deep-link،
chain poll، legacy poll، `BOT_RESPONSE`، timeout، requeue و hopهای
chain تعریف شده‌اند» — ولی **خودِ تعاریف در خلاصه نیستند.**

این هسته‌ی طراحی است. بدون آن نمی‌دانیم مثلاً:

| گذار | ساعت حفظ شود یا ریست؟ |
|---|---|
| `CHAIN_WAITING → CHAIN_WAITING` (poll ناموفق) | ؟ |
| `CHAIN_WAITING → WAITING_BOT` | ؟ |
| `WAITING_BOT → BOT_RESPONSE → WAITING_BOT` | ؟ |
| `CHAIN_WAITING → CHAIN_STEP` | ؟ |
| requeue دستی کاربر | ؟ |

بدون جدول کامل، پیاده‌سازی حدس خواهد بود.

### ۳-۲) ایندکس `(state, waiting_since, id)`

خلاصه می‌گوید «در صورت نیاز». جدول حدود ۵۸۰۰ ردیف دارد.

نظر ما: با این حجم ایندکس لازم نیست و هزینه‌ی نوشتن را بالا می‌برد.
**تأیید یا رد کارشناس را می‌خواهیم.**

### ۳-۳) `DB_VER` از ۲.۴ به ۲.۵

منطقی است. ولی `class-gs-db.php:371`:
```php
if ( get_option( self::DB_VER_KEY ) === self::DB_VER && self::schema_healthy() ) {
```

باید مطمئن شویم `missing_pieces()` ستون جدید را بشناسد، وگرنه
`schema_healthy()` روی نصب‌های قدیمی دائم `false` می‌دهد. کارشناس هم
همین را گفته — فقط تأکید می‌کنیم که این نکته حیاتی است.

### ۳-۴) دامنه‌ی «مالکیت‌محور کردن»

جمله‌ی پایانی کارشناس:

> «باید timeout به CAS/claim معتبر منتقل و state writeهای waiting در
> call-siteهای Chain، Collector، Matcher، Action و Recovery
> مالکیت‌محور شوند.»

این **بسیار بزرگ‌تر از افزودن یک ستون** است. پنج فایل، هر کدام چند
نقطه‌ی نوشتن.

نگرانی ما: قید صریح «بازطراحی Worker ممنوع» داریم. باید روشن شود این
تغییر داخل آن قید می‌گنجد یا نه. پیشنهاد ما تقسیم به دو مرحله:

- **۱۰.۱۲.۲۲** — فقط ستون + ثبت ساعت + انتقال deadline به CAS
- **۱۰.۱۲.۲۳** — مالکیت‌محور کردن call-siteها، جداگانه و با آزمون مستقل

**نظر کارشناس را می‌خواهیم.**

---

## ۴) آنچه هنوز راه‌حل ندارد

**اولویت ۱ — چرخه‌ی `SCANNED → skipped`.**

یادآوری شواهد: در دامپ `06:11:35` هر شش Session انتخاب شدند و
**هیچ‌کدام جلو نرفتند**. ‎#79 و ‎#80 با `result=skipped`، ‎#68 با
`result=waiting`.

`SCANNED` اصلاً در `const WAITING` نیست، پس **هیچ‌کدام از کارهای
اولویت ۲ شاملش نمی‌شود**. حتی با `waiting_since` کامل، ‎#79 و ‎#80 در
همان حلقه می‌مانند.

سه احتمالی که در سند درخواست مطرح کردیم هنوز بی‌پاسخ است.

---

## ۵) درخواست از کارشناس

۱. **متن کامل `waiting-audit.md`** — مسیر tasklet برای ما قابل
   دسترسی نیست. مخصوصاً جدول episodeها و SQLهای فقط‌خواندنی.
۲. **پاسخ اولویت ۱** — مهم‌تر از اولویت ۲ برای راه‌اندازی خط لوله.
۳. **تصمیم درباره‌ی `ERROR_BOT_TIMEOUT`** — از `const WAITING` خارج
   شود یا مهلت جداگانه بگیرد؟
۴. **تأیید تقسیم دو مرحله‌ای** (بند ۳-۴).
۵. **ایندکس: لازم است یا نه** (بند ۳-۲).

---

## ۶) جمع‌بندی

**کیفیت پاسخ: بالا.** هفت ادعای فنی، هر هفت تأیید شد. دو موردش را ما
اصلاً ندیده بودیم:

- نوشتن deadline داخل تابعی که کامنت خودش می‌گوید «عمداً بدون قفل»
- `ERROR_BOT_TIMEOUT` که حالت خطاست ولی داخل `const WAITING` است

**الگوی خطای ما، بار چهارم.** در سند درخواست نوشتیم:

> انتخاب یک منبع زمانی که خودِ چرخه‌ی اندازه‌گیری آن را بازنویسی
> می‌کند.

حالا معلوم شد یک الگوی خواهر هم دارد:

> **نوشتن در یک ردیف، از داخل تابعی که طراحی‌اش صریحاً بدون قفل
> است — و کامنتش همین را می‌گوید.**

هر دو یک ریشه دارند: **فرض کردن به‌جای خواندن قرارداد تابع میزبان.**

**وضعیت‌ها بدون تغییر:**
```
FIBER_ENOMEM       = ROOT_CAUSE_UNKNOWN
PIPELINE_STALL     = اولویت ۱ هنوز بدون راه‌حل
WAITING_CLOCK      = طراحی پذیرفته، جزئیات episode ناقص
```
