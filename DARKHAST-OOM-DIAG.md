# ‎#164 — خطای واقعی Fiber رخ داد · درخواست یک داده

> `2026-09-11 23:21:35` · Session ‎#164 · `action_executor` → `error`

---

## ۰) خلاصه

**دیروز نوشتم اگر `BUTTON_FOUND`ها گیر کنند، آن مسئله از ‎#68 مهم‌تر
است. گیر کردند. و علتش Fiber است.**

مسیر Fiber از **اولویت ۴ به اولویت ۰** منتقل می‌شود.

---

## ۱) آنچه ‎#164 نشان می‌دهد

زنجیره تا آخرین قدمِ قابل‌انجام **بی‌نقص** کار کرد:

| مرحله | زمان | نتیجه |
|---|---|---|
| `chain_engine` | 23:21:32 | ✅ گره ASSET → مسیر قدیمی |
| `button_detection` | 23:21:35 | ✅ ۱ دکمه پیدا شد |
| `button_selection` | 23:21:35 | ✅ `deep_link` · اطمینان **۹۲٪** |
| `action_executor` | 23:21:35 | ❌ **`Fiber stack allocate failed`** |

انتخاب دکمه بی‌عیب بود:

```
🔘 دانلود 🔘 → https://t.me/FileechFreeBot?start=download-317326
+42 deep link · +40 متن دقیق دانلود · +10 نشانه‌ی payload = 92٪ HIGH
```

**هیچ ایرادی در منطقِ ما نیست.** تمام مراحل تحلیلی درست کار کردند.
تنها جایی که شکست، لحظه‌ای است که می‌خواهیم به تلگرام **وصل** شویم.

---

## ۲) ★ نکته‌ی حیاتی — تشخیص خودکار فعال بود

سه چیز را الان بررسی کردم:

**۱. پرچم `oom_diag` به‌طور پیش‌فرض روشن است**
`class-gs-flags.php:67-71` → `'default' => 1`

**۲. کد ثبت در همان نقطه‌ی شکست اجرا می‌شود**
`class-sti-mtproto.php:785` → `STI_Logger::error( 'OOM_DIAG ' . ... )`

**۳. و مهم‌تر از همه — هر سه عددِ گمشده‌ی «مرحله ۲» ثبت می‌شوند:**

| داده‌ی لازم | خط در `class-gs-env-diag.php` | وضعیت |
|---|---|---|
| `/proc/self/maps` (تعداد VMA) | L182-186 → `maps_count` | ✅ |
| `vm.max_map_count` | L189-191 → `max_map_count` | ✅ |
| `RLIMIT_DATA` | L106-112, L194-197 → `rlimit_data` | ✅ |

> **یعنی دقیقاً همان سه عددی که از ۱۰.۱۲.۱۵ منتظرشان بودیم، همین الان
> — ساعت ۲۳:۲۱:۳۵ — روی میزبان شما نوشته شده‌اند.**

این payload فقط **در لحظه‌ی شکست** نوشته می‌شود. تا امروز هیچ شکست
واقعی‌ای بعد از ۱۰.۱۲.۱۷ رخ نداده بود، برای همین دستمان خالی بود.

---

## ۳) 🙏 تنها درخواست من

در پنل، بخش **📝 دیدن گزارش‌ها** را باز کنید و دنبال ردیفی بگردید که
با این عبارت شروع می‌شود:

```
OOM_DIAG {"...
```

زمانش **`2026-09-11 23:21:35`** است (یا نزدیک آن).

**کل آن ردیف را کپی کنید و همین‌جا بفرستید.**

اگر خیلی طولانی بود و در پنل بریده شد، همین‌قدر هم بفرستید — من از
روی همان بخش اول هم می‌توانم `maps_count` و `max_map_count` را
بخوانم، چون در ابتدای JSON می‌آیند.

> نیازی به SSH، phpMyAdmin یا هیچ کار فنی نیست. فقط یک کپی از صفحه‌ی
> گزارش‌ها.

---

## ۴) با آن یک ردیف، چه چیزی قطعی می‌شود

من از ۱۰.۱۲.۱۵ گفته‌ام «تا این سه عدد را نبینم، بین دو فرضیه انتخاب
نمی‌کنم». آن ردیف انتخاب را انجام می‌دهد:

| اگر ببینیم | نتیجه | راه‌حل |
|---|---|---|
| `maps_count` نزدیک `max_map_count` (مثلاً ۶۵۰۰۰ از ۶۵۵۳۰) | **سقف VMA کرنل** | افزایش `vm.max_map_count` — یک درخواست ساده به هاست |
| `rlimit_data` عددی محدود (نه `unlimited`) | **سقف RLIMIT_DATA** | همان — تنظیم میزبان |
| هر دو راحت، حافظه هم آزاد | **معماری MTProto با این رانتایم ناسازگار** | مسیر Bot API به‌جای MTProto |

**سه فرضیه، سه راه‌حل کاملاً متفاوت.** بدون آن ردیف، انتخاب بینشان
حدس است — و من حدس نمی‌زنم.

---

## ۵) چرا این خبر، بد نیست

نکته‌ای که ممکن است گم شود:

- **`memory_limit` مقصر نیست.** مصرف اوج ۹۹ از ۲۸۰ مگابایت بود (۳۵٪).
  `zend_fiber_stack_allocate()` یک `mmap` خام است و اصلاً از بودجه‌ی
  PHP خرج نمی‌کند — پس بالا بردن `memory_limit` **درمان نیست**.
- **صف حالا واقعاً کار می‌کند.** ‎#164 تازه ساخته شده و بلافاصله تا
  آخرین مرحله رفت. سه روز پیش هیچ Session‌ای اصلاً حرکت نمی‌کرد.
- **این خطا از قبل هم وجود داشت**، فقط پشت گرسنگی صف پنهان بود. حالا
  که صف باز شده، خودش را نشان داد. **پیشرفت است، نه پس‌رفت.**

---

## ۶) تا رسیدن آن ردیف، چه نمی‌کنم

طبق قرار خودمان:

- ❌ `memory_limit` را تغییر نمی‌دهم (اثبات شد که ربطی ندارد)
- ❌ `fiber.stack_size` را دست نمی‌زنم
- ❌ Fiber را غیرفعال نمی‌کنم
- ❌ `testFibers()` را اجرا نمی‌کنم (تا ۱۰۰٬۰۰۰ فیبر می‌سازد — خطرناک)
- ❌ و هیچ Root Cause قطعی اعلام نمی‌کنم

---

## ۷) وضعیت ‎#68

با این کشف، ‎#68 و ‎#69 (۹۲ ساعت در `CHAIN_WAITING`) **اولویت دوم**
می‌شوند. آزاد کردنشان وقتی معنا دارد که مقصدشان کار کند — وگرنه فقط
سریع‌تر به همین دیوار می‌خورند.

**اول دیوار، بعد صف.**
